# GPT Assistant 프로젝트

## 계정 정보
- 이메일: myangle87@naver.com
- 계정명: jaesung8712
- GitHub 레포: jaesung8712/gpt-assistant
- 개발 브랜치: claude/account-pinned-agents-v6W7p

---

## 팀 에이전트 구성 (고정, 변경 지시 전까지 유지)

아래 8인 팀 구성은 명시적으로 변경 요청이 없는 한 항상 이 구성을 기준으로 작업한다.

| 역할 | 담당 | 설명 |
|---|---|---|
| 🗂️ 기획자 | 1명 | 사용자 요구사항 파악, 기능 정의, 일정 조율, 와이어프레임 |
| 🖥️ 프론트엔드 개발자 | 1명 | UI/UX 구현, 반응형 디자인, HTML/CSS/JS, React/Vue |
| ⚙️ 백엔드 개발자 | 1명 | 서버 로직, REST API, DB 설계, 인증/인가 |
| 🌐 네트워크 개발자 | 1명 | 네트워크 아키텍처, 서버 배포, CDN, WebSocket |
| 🔒 보안 담당자 | 1명 | 취약점 분석, 보안 감사, 암호화, OWASP 기준 점검 |
| 🧪 QA 담당자 1 | 1명 | 기능 테스트, 시나리오 설계, 결함 리포팅 |
| 🔬 QA 담당자 2 | 1명 | 성능 테스트, 자동화 테스트, 크로스브라우저 검증 |
| 📚 문서 작성자 | 1명 | 사용자 매뉴얼, API 문서, 개발 가이드, 운영 절차서 |

---

## 작업 규칙

- 사용자가 요청하면 기획자 역할로 요구사항을 먼저 정리하고, 각 담당 역할별로 구분하여 작업을 진행한다.
- 팀 구성 변경은 사용자가 명시적으로 요청할 때만 수정한다.
- 새 세션이 시작되어도 이 파일을 기준으로 동일한 팀 구성과 규칙을 유지한다.
- 작업 중 중요한 결정 사항이 생기면 이 파일의 "프로젝트 기록" 섹션에 추가한다.

---

## 기술 스택

- **런타임**: Node.js
- **서버**: Express.js
- **데이터 저장**: data.json (로컬 파일 기반)
- **프론트엔드**: 순수 HTML/CSS/JS (외부 프레임워크 없음)

---

## 자체 AI 시스템 (`ai/` 디렉토리)

사용자가 직접 소유하는 AI 어시스턴트. 유료 서비스 의존 없이 운영 가능.

### 구조
- `ai/main.py` — FastAPI 서버 (모든 API 엔드포인트)
- `ai/llm.py` — Groq API 연동 (Llama 3.1 8B, 무료)
- `ai/memory.py` — SQLite(대화이력·학습지식·문서·피드백 저장)
- `ai/engine.py` — 자체 TF-IDF 검색 엔진 (ChromaDB는 완전 제거, 벡터DB 미사용)
- `ai/search.py` — DuckDuckGo 인터넷 검색 학습
- `ai/backup.py` — 직접 다운로드(ZIP) + Google Drive 백업 (OAuth2)
- `ai/static/index.html` — iPad PWA 채팅 UI

### 배포 방법 (Render.com 무료)
1. `ai/` 폴더를 Render Web Service로 배포
2. 환경변수 `GROQ_API_KEY` 설정 (https://console.groq.com 무료 발급)
3. Disk 마운트: `/app/data` 1GB (대화/학습 데이터 영속 저장) — `ai/render.yaml`에 이미 정의되어 있음
4. iPad Safari에서 배포 URL 접속 → "홈 화면에 추가" → PWA로 사용

### 배포 방법 (Railway 사용 시 — 영속 디스크 필수 설정)
`ai/railway.json`은 빌드/시작 커맨드만 정의하고 **Volume은 코드로 선언되지 않음** —
Railway 대시보드에서 직접 만들어야 한다. 이걸 안 하면 매 재배포·컨테이너 재시작마다
SQLite DB가 컨테이너 임시 파일시스템에 생성되어 학습된 RAG 데이터가 전부 초기화된다.
1. Railway 프로젝트 → 서비스 선택 → **Volumes** 탭 → "New Volume" 생성
2. Mount Path를 `/app/data`로 지정
3. 서비스 환경변수에 `DB_PATH=/app/data/memory.db` 추가
4. 재배포 후에도 `ai/data/memory.db`가 Volume에 저장되어 영속됨 (배포 로그에 `⚠️ DB_PATH 환경변수가 설정되지 않았습니다` 경고가 더 이상 안 뜨면 정상 설정됨)

### 백업 (직접 다운로드 + Google Drive)
- `GET /backup/download` — DB 전체를 ZIP(SQL 덤프 + 카테고리별 JSON + manifest)으로 즉시 다운로드
- Google Drive 연동(선택): Google Cloud Console에서 OAuth2 Client ID/Secret 발급 후 `GDRIVE_CLIENT_ID`, `GDRIVE_CLIENT_SECRET`, `GDRIVE_REDIRECT_URI` 환경변수 설정 → UI에서 Google 로그인 1회 후 버튼으로 백업

### 검색 엔진 (TF-IDF, ChromaDB 미사용)
- `ai/engine.py`의 자체 TF-IDF 엔진이 정적 KB(`ai/knowledge_*.py`)와 학습된 지식(`learned_knowledge` 테이블)을 통합 검색
- 학습/추가 시 `_dirty` 플래그로 표시되고 다음 검색 시 전체 corpus를 재구축하는 구조 (대규모 데이터에선 재구축 비용 고려 필요)

### 파인튜닝 (추후)
- 파인튜닝은 GPU 필요 → Google Colab 또는 Hugging Face 무료 컴퓨트 활용
- 현재는 RAG(검색 증강 생성)로 동일한 효과 구현

---

## 주식 분석 에이전트 팀 (11명)

`ai/stock_analysis/` 디렉토리의 주식 분석 전용 에이전트 팀.

| 역할 | 담당 | 설명 |
|---|---|---|
| 👔 총괄 관리자 | 1명 | 전체 파이프라인 조율, 분석 대상 선정, 최종 승인 |
| 💰 재무 자료 수집 | 1명 | DART 공시·재무제표, KRX 수급(외국인/기관/개인) |
| 📊 경제 자료 수집 | 1명 | 금리·환율·물가·지수·원자재 등 거시경제 지표 |
| 🌍 세계 동향 수집 1 | 1명 | 지정학 리스크, 무역 분쟁, 주요국 정치 |
| 🌐 세계 동향 수집 2 | 1명 | 산업 트렌드, AI·반도체·배터리·바이오 동향 |
| 📋 자료 취합 | 1명 | 4개 수집 에이전트 데이터 통합·정규화 |
| 🔍 상세 분석 | 1명 | 저평가 스크리닝, DCF/PER/PBR, 매수·매도 시점 |
| ✅ 교차 검증 1 | 1명 | 재무 데이터 이상치·회계 처리 검증 |
| 🔬 교차 검증 2 | 1명 | 분석 논리 일관성·편향 검증 |
| ⚠️ 교차 검증 3 | 1명 | 리스크 독립 검증, 스트레스 테스트 |
| 📝 레포트 작성 | 1명 | 오전 7시 / 저녁 10시 정기 보고서 생성 |

### 환경 변수
- `DART_API_KEY` — DART OpenAPI (https://opendart.fss.or.kr)
- `BOK_API_KEY` — 한국은행 ECOS API (https://ecos.bok.or.kr)
- `ANTHROPIC_API_KEY` — Claude API (https://console.anthropic.com) — 미설정 시 규칙 기반 분석으로 fallback

### Claude API 통합
- 모델: `claude-opus-4-8` (적응형 사고 `thinking: adaptive`)
- 적용 위치: `ai/stock_analysis/utils/claude_client.py`
  - `analyze_stock_opinion()` — 종목별 AI 투자의견 (StockAnalyzer)
  - `generate_executive_summary()` — 시황 종합 요약 (ReportWriter)
  - `generate_action_plan()` — 실행 가능한 투자 액션 플랜 (ReportWriter)
- Prompt caching 적용 (시스템 프롬프트 고정 텍스트)
- API 키 미설정 시 자동 fallback → 규칙 기반 분석 유지

### API
- `GET  /stock/team` — 팀 구성 조회
- `POST /stock/analyze` — 분석 실행 (백그라운드)
- `GET  /stock/analyze/sync` — 분석 동기 실행
- `GET  /stock/report/text` — 최근 보고서 텍스트
- `GET  /stock/status` — 실행 상태

---

## 프로젝트 기록

<!-- 중요한 결정 사항, 요구사항, 변경 이력을 여기에 추가 -->

- 2026-05-26: 8인 팀 에이전트 구성 확정 (기획자 1, FE 1, BE 1, 네트워크 1, 보안 1, QA 2, 문서 1)
- 2026-05-26: 에이전트 미설정 계정에 기본팀 자동 적용 기능 구현
- 2026-05-26: 프로젝트 맥락 영속 저장 기능(메모리 API) 구현
- 2026-05-26: 자체 AI 시스템 구현 (Groq+RAG+OneDrive 백업, iPad PWA)
- 2026-06-02: 주식 분석 11인 에이전트 팀 구현 (DART+KRX+매크로+지정학+검증3회+정기보고서)
- 2026-06-02: Claude API (claude-opus-4-8) 통합 — 종목별 AI 투자의견·시황요약·액션플랜 자동 생성, prompt caching 적용
- 2026-06-17: 백업 구조 개선 — `ai/backup.py`에 카테고리별 JSON 추출(by_category/*.json) + manifest.json 추가, ZIP 다운로드/Google Drive 백업 모두 적용
- 2026-06-17: 법률 KB 검색 정확도 개선 — `knowledge_legal.py`/`knowledge_legal2.py` 65개 항목 중 정확도 80% 미만 44개 항목의 `q` 필드에 자연어 변형 키워드 보강 (전체 정확도 67.1% → 92.8%, `test_legal_kb_accuracy.py`로 검증)
- 2026-06-17: 문서/코드 불일치 수정 — CLAUDE.md의 ChromaDB 설명을 실제 구현(TF-IDF `ai/engine.py`)에 맞게 수정, OneDrive→Google Drive 백업 설명 수정
- 2026-06-17: 법률 KB 확장 — `ai/knowledge_legal3.py` 신설, 모자보건법·일가정양립법(남녀고용평등법)·영유아보육법·아동수당법 등 가족·돌봄 관련 법령 추가
- 2026-06-17: 주식 리포트 채팅 요약 개선 — `_extract_report_section()` 헬퍼로 TOP 추천 종목·저평가 종목 섹션을 항상 채팅 응답에 포함 (특정 종목 미언급 시에도 동일), `report_writer.py`에 `_undervalued_picks()` 섹션 신설(분석 대상 종목 중 저평가/강한저평가 등급만 별도 정리)
- 2026-06-17: 검색 엔진 매칭 정교화 — `python-mecab-ko` 형태소 분석기를 옵션으로 도입(미설치 시 규칙 기반 토크나이저로 자동 폴백)해 붙여쓰기 복합어 분해 보강, 도메인별(개발/여행/채용) 동의어 그룹 추가(법률/HR KB와 겹치는 단어는 회귀 방지를 위해 제외). 법률 KB 정확도 92.8% → 93.0%로 유지·개선 확인
- 2026-06-17: 검색·답변 내용 영구 RAG 누적 + 피드백 학습 구현 — `/chat` 응답을 (stock 페르소나 제외) 항상 `auto_learn`으로 영구 저장하도록 확장(기존엔 `no_local` 케이스만 학습), `feedback_boost` SQLite 테이블 신설로 페르소나+질문 단위 좋아요/싫어요 누적 가중치 저장(좋아요 → 1.3배 부스트 최대 3.0, 싫어요 → 0.4배 감산 최저 0.05), `engine.py` 검색 점수에 가중치 곱 적용해 싫어요 받은 답변은 다음 동일 질문 시 새로 생성되고 좋아요 받은 답변은 유사 질문에서도 우선 노출되도록 함, `/feedback` 엔드포인트에서 즉시 라이브 반영 + 엔진 로드 시 SQLite에서 복원하여 재시작 후에도 유지
- 2026-06-18: `claude/mobile-hr-app-testing-rDc2F` 브랜치를 인사시스템+ERP(영업/구매/재고/회계) 통합 최종 버전 관리 브랜치로 지정. 이 브랜치에서 기능 추가/버그 수정을 이어가며 최종본을 유지한다. 진행 상황/다음 작업은 `HANDOFF.md` 참고.
- 2026-06-22: 배포 후 RAG 데이터 초기화 문제 진단 — 사용자가 실사용 중인 플랫폼은 Railway였고, `ai/railway.json`에는 영속 볼륨이 코드로 선언되어 있지 않아 재배포마다 컨테이너 임시 파일시스템의 SQLite DB가 사라지는 것이 원인으로 확인됨(코드 자체의 학습/누적 로직은 이미 정상 동작, 인프라 설정 누락이 근본 원인). `main.py`에 `DB_PATH` 미설정 시 배포 로그에 경고를 출력하는 점검 코드 추가, `CLAUDE.md`에 Railway Volume 생성 + `DB_PATH=/app/data/memory.db` 환경변수 설정 가이드 추가
- 2026-06-22: KRX 수급/공매도 데이터 조회 실패 시 fallback 누락 수정 — `krx_client.py`의 `get_investor_trading()`/`get_short_selling()`이 pykrx 실패 시 에러 dict를 반환해 주식 분석 파이프라인이 조용히 데이터 누락되던 문제를, 같은 파일 내 다른 함수들이 이미 쓰던 `_mock_investor_data()` 등 fallback 패턴과 일치시켜 수정
- 2026-06-22: 페르소나별 검색 분리 — `main.py`의 `/chat` 인터넷 검색 단계에서 `srch.search_and_learn(search_msg)` 호출이 `persona_id`를 누락해 모든 페르소나의 웹검색 학습 결과가 `search_and_learn()` 기본값인 `persona="hr"`로 고정 저장되던 버그를 발견. `search_and_learn(search_msg, persona_id=req.persona)`로 수정해 이후부터는 페르소나별로 검색/학습 데이터가 분리됨. 이 수정 이전에 이미 저장된 웹검색 학습 데이터는 원래 페르소나 정보가 남아있지 않아 자동 재분류는 불가(필요 시 수동 정리 별도 논의)
- 2026-06-22: 증권사 리포트 컨센서스 섹션에 빈 placeholder 데이터가 "수집 리포트 N건"으로 표시되던 문제 수정 — `securities_report.py`의 `_parse_naver_research()`(네이버 리서치 HTML 파싱)와 `search_reports_ddg()`(DuckDuckGo 보조 수집)가 증권사명·목표주가·투자의견·날짜 추출에 실패해도 빈 문자열을 그대로 담은 report dict를 리스트에 추가하던 구조라, `_build_consensus()`의 `len(reports)`가 빈 항목까지 카운트해 "수집 리포트 2건"처럼 보이지만 실제 내용은 전부 "미상"/"중립"/"TP:-"인 신뢰할 수 없는 결과가 나왔음. `get_all_reports()`에 `_is_meaningful()` 필터(목표주가·투자의견 중 하나라도 실제 값이 있어야 통과)를 추가해 빈 항목을 컨센서스 집계 전에 제거 — 실데이터가 없으면 "수집된 리포트 없음"으로 정직하게 표시됨
- 2026-06-24: 주식 분석 채팅 응답 상세화 + 긴 자료 다운로드 분리 + 전 페르소나 참고자료 링크 추가. (1) `_summarize_stock_report()`(main.py)가 종목 미지정 "현황" 질문에 기존 Executive Summary/TOP 추천 종목/저평가 종목 3개 섹션만 보여주던 것을, 시장 환경 분석·증권사 애널리스트 리포트 컨센서스 2개 섹션을 추가해 5개 섹션으로 확장 — report_writer.py 자체는 14개 섹션을 이미 생성하고 있었으나 채팅 요약 레이어가 그중 일부만 노출해 "모호하다"는 문제가 발생했던 것이 원인. (2) `_summarize_long_text()` 신설 — 답변 본문이 3500자를 넘으면 앞부분만 채팅에 표시하고 전체는 `stock_analysis/reports/answers/`에 파일로 저장 후 다운로드 링크(`/answer/download/{filename}` 신규 엔드포인트, `answer_` 접두사·`..` 차단)로 안내. `run_broker_report`(단독 증권사 리포트 조회)·`run_lowprice_screen`(저평가 저가주 스크리닝) 두 원문 그대로 출력하던 경로에 적용. (3) `_format_reference_links()` 신설 — `[{title,url}]` 목록을 마크다운 링크 푸터로 변환(http만 허용, 중복 제거, 최대 5개). 경로 B(LLM 보강) 양쪽 분기에 `reference_items` 수집을 연결: stock_mode는 증권사 리포트 "링크" 필드 + DDG 자동검색 결과, 비주식 페르소나는 law_search 결과 + 일반 웹검색 결과. 프론트엔드(`index.html`의 `renderMd()`)가 이미 마크다운 링크와 `/stock/download/` `/answer/download/` 다운로드 버튼 스타일을 지원하고 있어 프론트엔드 변경은 불필요했음. 적용 범위 제한: KB-직접답변(경로 A)·기업 KB 무응답(경로 C)에는 참고자료가 없어 미적용, 뉴스 기사(`news_collector.py`의 "출처" 필드는 출처명/URL이 혼재해 신뢰 불가)는 참고링크에서 제외
- 2026-06-24: 증권사 리포트·뉴스 수집기가 실데이터를 못 가져오던 근본 원인 다수 발견·수정(이 환경의 외부망 차단이 풀린 뒤 실제 네이버 금융 응답으로 검증). `securities_report.py`의 `fetch_naver_research()`가 `searchType=priceTo&code={ticker}` 파라미터를 쓰고 있었는데 이는 종목과 무관한 전체 "목표주가 변경" 리스트를 반환하는 잘못된 조회였음(올바른 파라미터는 `searchType=itemCode&itemCode={ticker}`) — 이 때문에 `_parse_naver_research()`의 정규식(`/research/company_read.naver?nid=` 절대경로 기대)도 실제 마크업(`company_read.naver?nid=` 상대경로)과 맞지 않아 항상 0건이 파싱됐음. 목표주가·투자의견은 목록 페이지에 컬럼 자체가 없고 개별 리포트 본문(`목표가 480,000 | 투자의견 Buy` 형태)에만 있다는 것도 확인 — `_enrich_with_target_opinion()` 신설로 상위 5건의 본문을 추가 조회해 채움, `_extract_report_meta()`로 본문에서 목표가·투자의견 파싱, 영문 등급(Buy/Hold/Sell)을 한글 버킷으로 정규화하는 `_normalize_opinion()` 추가(기존 `_build_consensus()`가 한글 키워드만 매칭해 영문 등급은 의견분포에서 누락되던 문제도 같이 해결). 또한 `search_reports_ddg()`가 `async def`이면서 내부적으로 완전히 동기 블로킹인 DDGS 호출을 실행하고 있어, `get_all_reports()`의 `asyncio.gather()`로 `fetch_naver_research()`와 함께 묶이면 이벤트 루프를 점유해 네이버 쪽 요청이 거의 항상 타임아웃되던 동시성 버그를 발견·수정(`run_in_executor`로 별도 스레드 실행, `news_collector.py`가 이미 쓰던 패턴과 통일) — 이게 "정확한 내용이 거의 안 들어간다"는 원래 사용자 불만의 진짜 근본 원인이었음(이전 수정은 빈 placeholder를 걸러내는 대증 처방이었을 뿐, 데이터가 애초에 안 들어오는 원인은 그대로였음). `news_collector.py`의 `_fetch_naver_finance_news()`도 같은 종류의 파싱 버그 수정(제목이 `title=` 속성이 아니라 `<a>` 태그 내부 텍스트로 들어있었음, 날짜 정규식이 공백·시간 포함 형식과 안 맞았음). 두 파일의 HTML 클린업 헬퍼(`_clean`/`_clean_html`)에 `html.unescape()` 추가해 `&hellip;` 등 엔티티 미디코딩 문제도 해결. 부가적으로 `aiohttp.ClientSession()` 호출들에 `trust_env=True`를 추가(이 개발 샌드박스의 로컬 프록시를 타려면 필요했던 설정, 배포 환경에는 영향 없음). 전체 파이프라인 실측 결과: 삼성전자 기준 증권사 리포트 5건(평균 목표주가 522,000원, 의견분포 매수 5건)·뉴스 8건이 정상 수집·표시됨을 확인
- 2026-07-12: HR 앱 채용 면접 평가에 "다중 심사위원" 고도화 기능 추가(`jaesung8712/gpt-assistant` 레포, `claude/mobile-hr-app-testing-rDc2F` 브랜치). 면접관 다중 지정·개별 평가 자체는 기존에 이미 구현돼 있었으므로, 그 위에 ① 면접관 중 1명을 심사위원장으로 지정하는 `interview.leadInterviewerId`, ② 심사위원장(또는 admin)만 입력 가능한 최종 판정(합격/보류/불합격) `POST /api/recruit/interviews/:id/verdict` 신규 엔드포인트, ③ 면접관 간 총점 편차가 25점 만점 중 8점 이상이면 자동 요약에 경고 문구를 넣는 `_ivScoreSpread()`를 추가. `public/index.html` 면접 일정 등록/수정 모달에 심사위원장 선택 드롭다운(면접관 다중선택 변경 시 동적 재구성), 면접 평가 상세 페이지에 최종 판정 입력/조회 UI 추가. curl로 백엔드 권한/검증 로직, Playwright로 등록·수정·상세 화면 콘솔 에러 0건 확인 후 커밋. 상세 내용은 `HANDOFF.md` 참고.
- 2026-07-14: HR 시스템(server.js/public/index.html) 인증 구조 근본 개편 — 기존에는 `requireAdmin()`/`requireRole()`이 클라이언트가 요청 body/query에 적어 보낸 `role`을 그대로 신뢰해(진짜 세션/토큰 검증 없음), 누구든 `role:"admin"`만 넣어 보내면 인가를 우회할 수 있었고, `GET /data`·`GET /snapshots/:year`(전 직원 PII 포함 전체 데이터)·`GET /api/recruit/*`·`GET /api/accounting/*`·`GET /api/erp/*`·`GET /api/pms/*` 등 대부분의 조회 API와 `POST /save`(전체 데이터 덮어쓰기)까지 인증 자체가 전무해 URL만 알면 누구나 회사 전체 데이터를 열람·덮어쓸 수 있었음. `/login` 성공 시 HMAC 서명 토큰을 발급하고(`SESSION_SECRET` 환경변수로 서명, 12시간 만료) 이후 모든 요청은 `Authorization: Bearer <token>` 헤더로 전달하도록 변경, `requireAdmin`/`requireRole`은 서버가 검증한 `req.auth.role`만 신뢰하도록 재작성, 구매요청/PMS 투입률·업무일지/채용 면접평가·최종판정 등 곳곳에 흩어져 있던 동일한 클라이언트-신뢰 `role`/`userId` 체크도 전부 `req.auth`로 전환(면접 평가는 "지정 면접관 본인 명의로만 입력 가능"하도록 `interviewerId===req.auth.empId` 검증도 추가). 채용 지원자 단건 조회(`/api/recruit/candidates/:id`)·이력서 조회(`/resume`)는 목록 API에만 있던 부서별 열람 제한이 아예 없어 ID만 알면 타 부서 지원자 정보를 볼 수 있었던 것도 함께 수정(`_recruitCanViewCandidate` 헬퍼 신설). 최초 배포로 서버에 직원이 0명인 "부트스트랩" 상태에 한해서만 `POST /save` 1회를 인증 없이 허용(클라이언트 내장 샘플 admin 데이터를 최초 업로드하기 위한 불가피한 예외, 그 외엔 항상 인증 필요). 부가로 `helmet`(CSP는 인라인 스크립트 전면 사용 구조 때문에 비활성화, 나머지 기본 헤더만 적용), `express-rate-limit`으로 `/login` 브루트포스 방어(IP당 15분/20회) 추가. Playwright+직접 HTTP 요청으로 토큰 없는 조회 401, 위조 role로의 관리자 권한 우회 실패(403), 실제 admin 토큰으로는 정상 동작함을 확인. **후속 실배포 버그**: 위 배포 직후 사용자가 실제 운영 사이트에서 로그아웃 후 재진입 시 이미 종료된 세션의 레코드가 "다른 사용자가 수정 중"이라는 잠금 화면으로 계속 뜨는 문제 발견 — `/unlock`도 인증이 필요해졌는데 `logout()`에서 토큰을 먼저 지운 뒤 `releaseAllMyLocks()`를 호출하고 있어 잠금 해제 요청이 Authorization 헤더 없이 나가 401로 거부되고 있었음(로그아웃해도 서버 잠금은 그대로 남는 구조). `releaseAllMyLocks()`를 토큰 삭제보다 먼저 실행하도록 순서 수정. **진짜 근본 원인**: 위 잠금 수정을 배포한 뒤에도 사용자가 로그인 직후 인증이 계속 실패하는 증상을 보고, Render 배포 로그를 직접 받아 확인한 결과 `express-rate-limit`이 `ERR_ERL_UNEXPECTED_X_FORWARDED_FOR` 에러를 던지고 있는 것을 발견 — Render가 리버스 프록시를 거쳐 `X-Forwarded-For` 헤더를 붙여 요청을 전달하는데, Express 기본 설정(`trust proxy: false`)에서는 이 헤더를 신뢰하지 않아 `express-rate-limit`이 실제 클라이언트 IP를 판별하지 못해 에러를 던졌고, 그 여파로 `/login` 요청 자체가 정상 완료되지 못해 화면상 로그인은 성공한 것처럼 보여도 토큰이 발급되지 않고 있었음(로그아웃 순서 버그는 실재했지만, 이 원인 때문에 애초에 유효한 토큰이 없어 그 수정만으로는 증상이 해결되지 않았던 것). `app.set("trust proxy", 1)` 추가로 해결 — 로컬에서 `X-Forwarded-For` 헤더를 붙인 요청으로 재현·검증(수정 전 서버 로그에 ValidationError, 수정 후 정상 토큰 발급 확인). **진짜 진짜 근본 원인**: trust proxy를 고치고 재배포한 뒤에도 사용자가 여전히 "로그인해도 잠금 해제 등에서 로그인이 필요합니다"를 겪어서 재조사한 결과, `public/index.html` 소스에 오프라인 데모용으로 하드코딩된 샘플 직원 배열(`let employees=[...]`)의 첫 계정이 정확히 `loginId:"admin", pw:"admin", name:"시스템관리자"`였고, `doLogin()`이 이 로컬 평문 pw 매칭을 서버 `/login`보다 먼저 시도하는 구조였다는 것을 발견 — 그래서 admin/admin으로 로그인하면 서버가 정상 운영 중이고 실제 직원 데이터(692명)가 있어도 서버 `/login`을 단 한 번도 호출하지 않고 그 자리에서 "로그인 완료"되어(로컬 시드 데이터 자체가 우연히 실제 회사 데이터 스냅샷이라 화면도 정상으로 보임) 토큰이 전혀 발급되지 않고 있었다. 새로고침·재로그인을 아무리 반복해도 매번 이 로컬 매칭 경로를 다시 타서 증상이 재현됐던 것. `doLogin()`을 재구성해 서버 연결 시 먼저 `GET /status`로 서버에 직원이 이미 존재하는지 확인 → 존재하면(정상 배포) 반드시 서버 `/login`을 거치도록 강제하고, 서버에 직원이 아예 없는 최초 배포(부트스트랩) 상태에서만 로컬 매칭을 허용하도록 변경. Playwright로 두 시나리오(부트스트랩 로그인, 직원 존재 시 재로그인) 모두 검증 완료. **후속 확인 필요 사항**: 이 하드코딩 시드 배열에는 전 직원(692명으로 추정)의 평문 비밀번호가 그대로 들어있어, 페이지 소스 보기만으로도 전 직원 비밀번호가 노출된다 — 별도의 심각한 보안 이슈이며 사용자에게 별도 보고 필요(이 시드 데이터가 실제 배포에 아직도 실제 평문 비밀번호를 담은 채 커밋되어 있는지, 데모용 더미 데이터로 교체해야 하는지 확인 필요). **후속 조치**: 사용자 확인 결과 실제 데이터로 확인되어 `public/index.html`의 하드코딩 `employees`(257명, 실명·이메일·전화번호·주소·연봉·경력·평문 비밀번호 전부 포함) 및 `kpiEntries`(290건, 같은 스냅샷의 실제 평가 목표/이력) 배열을 완전히 제거하고, 명백히 가상의 데모 계정 5명(관리자/홍길동/김철수/이영희/박민수, example.com 이메일)으로 교체 — 오프라인 데모 모드와 최초 배포 부트스트랩(서버에 직원이 0명일 때 최초 admin 계정 업로드) 용도는 그대로 유지됨. 이 조치는 소스 코드 안의 데이터만 다루며 실제 운영 DB(Render PostgreSQL, 692명)에는 영향 없음. 직원 로그인 정보 일괄 재설정(사번을 아이디·비밀번호로)은 별도 구현이 필요 없었음 — `public/index.html`에 이미 관리자 전용 `resetAllLoginToEmpNo()` 함수와 인사관리>직원목록 페이지의 "🔑 계정 사번으로 초기화" 버튼이 구현되어 있어(관리자 계정은 제외), 이걸 클릭하면 현재 로드된 실제 서버 데이터 전체에 대해 사번 기반 재설정이 서버 측 bcrypt 해싱을 거쳐 적용됨. **미해결 잔여 사항**: git 커밋 히스토리에는 이번에 제거하기 전의 실제 데이터가 과거 커밋들에 그대로 남아있어, 저장소 접근 권한이 있는 사람은 여전히 예전 커밋을 통해 조회 가능 — 히스토리 재작성(rebase/filter-repo 등)이 필요한지는 파괴적 작업이라 별도로 사용자 확인 필요. → **처리 완료(같은 날)**: 사용자 승인 받아 `git filter-repo`의 `--blob-callback`으로 히스토리 전체(총 344블롭)를 재작성해 실제 데이터를 완전히 제거(전체 재검증으로 잔존 0건 확인). 도중 `ai/knowledge_company.py`(AI 챗봇 지식베이스)에도 실제 회사 도메인과 외부 서비스(printrobo.com) 평문 계정정보가 있는 것을 추가로 발견해 현재 코드+히스토리 양쪽에서 함께 제거. 부작용: 히스토리 강제 push로 브랜치 루트 커밋까지 해시가 바뀌어 `main`과의 공통 조상이 끊기면서 기존 PR #1이 자동으로 닫힘(병합되지 않음, 코드 손실 없음) — 재오픈 시 `main`과 겹치는 15개 파일(그중 `ai/main.py`·`ai/llm.py`·`budget.js` 등은 main 쪽 독자적인 AI 어시스턴트 작업으로 추정)의 병합 전략을 사용자에게 확인 후 진행 필요(미해결).
- 2026-07-14: 인증 개편에 대한 에이전트 기반 회귀 QA 2건 병행 실행 후 발견된 버그 총 4건 수정. **(QA1: 설정/시스템/권한관리 신규 영역)** ① 부트스트랩(서버 직원 0명) 로그인은 로컬 시드 계정 매칭으로 완료되어 토큰이 없는데, 그 세션에서 로그아웃 없이 설정을 계속 바꾸면 최초 저장으로 서버에 직원이 생긴 이후부터는 인증이 필요해져 이후 모든 저장이 401로 조용히 실패(화면은 매번 "저장되었습니다"로 표시)하던 심각한 버그 발견 — 부트스트랩 로그인 시 자격증명을 메모리에만 임시 보관하다가 첫 저장 성공 직후 조용히 서버 `/login`을 재시도해 토큰을 발급받도록 수정(`_bootstrapCreds`/`_tryUpgradeBootstrapToken()` 신설, 재로그인 요구 없음). ② `POST /unlock`의 `force` 옵션이 인증만 확인하고 역할 검증이 없어 일반 사용자도 API 직접 호출로 아직 안 만료된 남의 편집 잠금을 강제 해제할 수 있었음 — admin이거나 이미 만료된 잠금일 때만 허용하도록 서버에 검증 추가. ③ 메뉴 권한 관리에서 개인별로 끈 화면이 사이드바에서만 숨겨지고 `gotoPage()` 직접 호출(URL 조작)은 막지 못했음 — 사이드바 필터링과 동일한 `menuPerms` 기준을 `gotoPage()`에도 적용. **(QA2: 전체 회귀)** 인증/잠금/관리자권한 위조 방어(body에 role:"admin" 위조해도 403 확인)/핵심 업무 플로우(KPI·경비·PMS·채용·구매요청)/퇴직 처리는 전부 정상 확인된 가운데 버그 2건 추가 발견: ④ `acquireLock()`이 `/lock` 실패 응답 중 진짜 잠금 충돌(`r.lock` 있음)과 그 외 실패(`r.lock` 없음 — 예: 부트스트랩 세션의 401)를 구분하지 않고 전부 "다른 사용자가 수정 중"으로 표시 — 후자는 네트워크 오류와 동일하게 로컬 허용하도록 수정. ⑤ SSE `data_updated` 핸들러가 `employees`/`kpiEntries` 등 일부 필드만 손으로 골라 반영하고 있어 `approvalDocs`/`expenseClaims`/`attendanceRecords` 등 목록에 없는 필드(30여개)는 다른 사용자가 저장해도 실시간 반영 안 되고 새로고침해야만 보였음(이번 인증 개편과 무관한 기존 로직 결함) — 로그인/수동 불러오기가 쓰는 `applyState()`를 재사용하도록 리팩터링해 전체 필드가 실시간 반영되게 수정. 5건 모두 Playwright로 재현 후 수정 확인.
- 2026-07-14: Render Build Filters(경로 기반 재배포) 설정 테스트용 커밋. `hr-erp-system`/`my-ai-assistant` 두 서비스의 Included Paths 밖에 있는 `CLAUDE.md`만 수정 — 필터가 정상 동작하면 두 서비스 모두 재배포가 발생하지 않아야 함. 테스트 결과 확인 중 **중요한 사실 발견**: `my-ai-assistant` 서비스는 `claude/mobile-hr-app-testing-rDc2F`가 아니라 **`claude/account-pinned-agents-v6W7p` 브랜치를 보고 배포되고 있음**(Render Events에서 확인). 즉 이날 앞서 `claude/mobile-hr-app-testing-rDc2F`에서 수정했던 `ai/knowledge_company.py`의 실제 외부 서비스(printrobo.com) 평문 계정정보 제거가 **실서비스에는 전혀 반영되지 않고 있었음** — 그 수정은 배포에 쓰이지 않는 다른 브랜치에만 적용됐던 것. `claude/account-pinned-agents-v6W7p` 브랜치의 `ai/knowledge_company.py`를 직접 확인해 동일한 평문 계정정보가 실제로 남아있음을 확인, 사용자 승인 받아 그 브랜치에 동일한 수정을 커밋(`d340950`)해 실서비스에 반영. **후속 확인 필요**: `claude/account-pinned-agents-v6W7p`는 git 히스토리 재작성을 하지 않았으므로, 이 브랜치의 과거 커밋에도 (그리고 애초에 있었다면) 같은 민감정보가 남아있을 수 있음 — 필요 시 별도로 히스토리 정리 여부 확인 필요. 이 발견으로 두 Render 서비스가 서로 다른 브랜치를 보고 있다는 것 자체가 이미 상당한 배포 격리 효과를 내고 있었다는 것도 확인됨(단, 개발자가 브랜치를 착각해 한쪽에만 수정하고 실제 배포 브랜치에는 반영이 안 되는 위험은 여전히 존재 — 앞으로 `ai/` 관련 수정은 반드시 배포 대상 브랜치가 어디인지 먼저 확인 필요).
- 2026-07-15: `claude/account-pinned-agents-v6W7p`(AI 어시스턴트 실배포 브랜치) 히스토리 정리 — `git filter-repo`의 `--blob-callback`으로 `ai/knowledge_company.py`의 과거 버전 5개(전체 214블롭 중)에 남아있던 실제 회사 도메인·외부 서비스(printrobo.com) 평문 계정정보를 제거, 재검증으로 잔존 0건 확인 후 강제 push 완료. 이어서 이 브랜치의 `ai/` 디렉토리 전체(FastAPI 백엔드·지식베이스·stock_analysis 파이프라인)에 대한 에이전트 기반 보안·구조 점검 진행(하드코딩 비밀정보/인증누락/인젝션 위험/구조적 결함 확인, BACKUP_TOKEN 이슈는 사용자가 별도 세션에서 처리하기로 해 점검 대상에서 제외) — 결과: 실제 수정·반영 3건(커밋 `9e87383`) — ① `knowledge_company.py`에 해외출장 비자 대행업체(퍼스트 트립) 직원 2명의 실명·휴대폰번호·이메일이 평문으로 남아있던 것 발견(앞서 발견한 printrobo.com 사고와 동일 유형) → "담당자 연락처는 인사부서에 문의"로 대체, ② `static/index.html`의 `escHtml()`이 따옴표(`"`)를 이스케이프하지 않아 마크다운 링크 렌더링 시 속성 탈출 XSS(임의 속성 주입) 가능했던 것 수정, ③ `/admin/import-db`가 업로드된 ZIP을 경로 검증 없이 `extractall()`로 풀어 Zip Slip(경로에 `../` 포함 시 임의 위치에 파일 쓰기) 가능했던 것 수정 — 악성 zip으로 실제 재현 후 차단 확인, 정상 임포트 동작 유지 확인. 미수정 보고 항목: `/budget/*` API 전체가 인증 없음(BACKUP_TOKEN 게이팅 패턴 밖 — BACKUP_TOKEN 이슈와 함께 별도 세션에서 처리 필요), 엑셀 그리드 수식 저장은 의도된 기능이라 그대로 둠. 이후 새로 발견된 비자업체 직원 개인정보도 사용자 승인 받아 동일하게 `git filter-repo`로 히스토리(과거 버전 5개, 전체 517블롭 중) 정리 완료(잔존 0건 검증 후 강제 push, 커밋 `7d8b176`).
- 2026-07-16: 인증 개편이 안정화된 이후, "실사용처럼 반복 테스트 + 100명 이상 동시접속 부하 + 계정간 데이터 격리 + 모바일/반응형" 4갈래를 에이전트 4개를 병렬로 띄워 검증. 부하 테스트·기능별 반복 QA 2개는 세션 한도 도달로 미완주(재시도 필요, `docs/API_CONTRACT.md` 기준 별도 세션에서 이어서 진행 예정). 모바일 QA와 계정간 데이터 격리 QA 2개는 완주해 실제 버그 4건을 발견, 전부 수정·검증 완료: ① `public/index.html`의 근태·내정보 등 진입 시 뜨는 "🔒 본인확인" 비밀번호 모달(`_requireMyPagePw`)이 `gotoPage()`로 다른 메뉴로 이동해도 닫히지 않고 새 페이지 위에 계속 남아 화면을 가리던 버그 — `gotoPage()` 최상단에서 열린 모달이 있으면 항상 먼저 닫도록 수정. ② 삭제된 레코드가 동시저장 병합(smartMerge) 시 되살아나는 버그 — 기존에는 `roomReservations` 한 필드만 삭제 tombstone(`roomReservationTombstones`)이 있어, A가 레코드를 삭제하는 사이 B가 구버전 스냅샷으로 같은 항목을 수정해 저장하면 employees/kpiEntries/approvalDocs/expenseClaims 등 나머지 id-keyed 컬렉션에서는 삭제가 되살아났음(계정 간 데이터 격리 테스트 에이전트가 실측 확인) — roomReservations 패턴을 범용화한 `recordTombstones`(`{필드명:[{id,ts}]}`)를 도입해 client(`public/index.html`: `_tombstone()` 헬퍼, 13개 삭제 호출부 전체에 적용)·server(`server.js`: `mergeRecordTombstones()`, `smartMerge()`) 양쪽에 배선. 이 작업 도중 **더 심각한 기존(이번 세션 이전부터 있던) 결함을 추가로 발견**했는데, Postgres(운영) 모드의 일반 `/save` 저장 로직이 employees/kpi_entries를 제외한 모든 id-keyed 컬렉션(`app_collections` 테이블, 즉 approvalDocs/expenseClaims/attendanceRecords/roomReservations 등 대부분)에 대해 upsert만 하고 DELETE를 전혀 실행하지 않아 — 즉 사용자가 UI에서 무언가를 "삭제"해도 실제 운영 DB에서는 그 레코드가 영구히 남아있었고, 재조회 시 되살아날 수 있는 구조였음 — `recordTombstones`를 이용해 `app_collections`/`kpi_entries`에 실제 `DELETE ... WHERE id = ANY($1)`를 실행하도록 수정(레거시 `roomReservationTombstones`도 같이 반영). ③ JSON 파일(오프라인) 모드에서 클라이언트가 `_version`을 서버 현재 버전보다 높게 조작해 보내면 병합(smartMerge) 분기를 건너뛰고 `data`에 없는 컬렉션 전체가 통째로 삭제되는 버그(member 권한으로도 재현 가능, `server.js:481` 부근) — `_version`이 서버와 "다르면"(크든 작든) 항상 병합하도록 조건 변경 + JSON 모드 저장 시 기존 `_fileStore`를 베이스로 얹는 방어 로직 추가. ④ `GET /data`가 role 구분 없이 `payslips`(급여 원본)와 `kpiEntries`(평가 코멘트)를 포함한 전체 데이터를 모든 인증된 사용자에게 그대로 반환 — member 계정으로도 타 직원 급여·평가 코멘트 조회가 가능했음(격리 테스트 에이전트 실측). PAGE_ROLES에 이미 존재하던 경계(급여관리 admin 전용, 1·2차 평가 leader/director 이상)를 그대로 서버에 반영한 `filterDataForRole()` 신설 — admin이 아니면 payslips는 본인 것만, member 역할이면 kpiEntries도 본인 것만 필터링해 `GET /data`·`/save` 병합 응답·스냅샷 복원 응답 3곳 모두에 적용. 이 필터링 자체가 "클라이언트가 들고 있는 전체 상태를 그대로 재업로드"하는 이 앱의 구조와 충돌해 필터링된(불완전한) 로컬 배열을 다시 저장하면 타인의 payslips/kpiEntries가 지워질 위험이 새로 생겨, JSON 모드에서 이 두 필드만은 항상 기존 저장값과 id 기준으로 병합(변경분만 반영, 없는 건 안 지움)하도록 별도 처리(Postgres 모드는 원래 upsert-only라 이미 안전). 4건 모두 격리된 로컬 서버(JSON 파일 모드)에서 Playwright/직접 HTTP 요청으로 각각 재현 시나리오를 만들어 수정 전 실패 → 수정 후 통과를 확인.
- 2026-07-16: 세션 한도로 미완주했던 부하 테스트·기능별 반복 QA 2개 에이전트를 재실행. 부하 테스트(100명+ 동시접속)는 완주해 데이터 무결성 관련 시나리오(동시 GET/POST, 100명+ SSE, 30초 지속 혼합 부하)는 전부 PASS(무손실) 확인, 다만 **로그인 rate limiter가 실사용에 실제 문제를 일으키는 것을 실측 확인**: `loginLimiter`(IP당 15분/20회)가 성공한 로그인도 그대로 카운트해, 같은 사무실 공인IP 뒤에서 30명이 동시 로그인 시 20명만 성공하고 11명이 429로 차단됨(월요일 출근 시간대 등 실사용 시나리오에서 실제로 발생 가능). `skipSuccessfulRequests:true` 추가로 실패한(틀린 비밀번호) 시도만 카운트하도록 수정 — 브루트포스 방어 목적은 유지하면서 정상 동시 로그인은 차단하지 않도록 함(수정 후 실측: 동시 성공 로그인 25건 중 21건 통과로 개선, 나머지는 express-rate-limit의 카운트-후-환불 구조상 완전히 동시(같은 밀리초)에 몰릴 때만 발생하는 경합이라 실사용의 자연스러운 시간차 로그인에서는 사실상 영향 없음; 동시 틀린 비밀번호 시도는 여전히 일부 차단되어 브루트포스 방어 유지 확인). 부수적으로 `bcryptjs`가 순수 JS 구현이라 동시 로그인 다수 시 단일 스레드에서 직렬화돼 지연이 인원수에 비례해 늘어나는 것도 발견(120명 동시 로그인 시 p50 5.4초) — 네이티브 `bcrypt` 패키지로 교체하면 해소 가능하나 빌드 의존성이 추가되는 변경이라 이번엔 보류, 별도 검토 필요 항목으로 남김.
- 2026-07-19: 이전 QA에서 원인 미상으로 남긴 버그 2건("채용 최종판정 저장 실패", "PMS 업무일지 입력 실패")을 각각 전담 에이전트로 심층 조사, 동시에 오늘 세션의 수정사항 9건에 대한 독립 회귀 재검증 에이전트, 아직 점검 안 한 모듈(조직도/게시판/복지포인트/증명서/승진관리/핵심인재/저성과자/필수교육/알림센터/결재위임/회계나머지/영업재고/다면평가) 추가 탐색 에이전트까지 총 4개를 병렬로 실행. 실제 버그 3건 발견·수정, 1건은 "버그 아님"으로 확정, 1건은 정보성 이슈로 확인만.
  - **PMS 업무일지 입력 실패 → 실제 버그, 수정 완료**: `server.js`의 `POST /api/pms/worklogs`는 admin이면 프로젝트 멤버가 아니어도 업무일지 등록을 허용하는 예외가 있는데, 클라이언트의 `_pmsWorklogProjects()`(`public/index.html:15546`)는 이 예외 없이 항상 `members` 포함 여부로만 걸러 admin이 스스로를 멤버로 등록해두지 않은 한 프로젝트 선택 드롭다운이 영구적으로 비활성화(disabled)돼 "업무 추가" 자체가 불가능했음(QA/조사 에이전트 실측: 기본 테스트 계정이 admin이라 정확히 이 화면에서 막혔던 것으로 추정). 서버와 동일한 admin 예외를 클라이언트에도 추가, 재현 시나리오로 검증.
  - **채용 최종판정 저장 실패 → 버그 아님(확정)**: 서버 인증/권한(`server.js:3984-4009`, `req.auth` 기반이라 클라이언트가 보낸 role/userId 위조 불가)과 클라이언트 로직(`public/index.html:17003-17069`) 모두 실제 UI 전체 흐름(공고→지원자→면접일정→개별평가→최종판정)으로 정상 동작 확인. 앞선 QA의 "2회 반복 모두 실패"는 테스트 타이밍 문제였음 — `_showCheckInPrompt()`(`public/index.html:12773`)가 non-admin 로그인 시 오늘이 주말/공휴일이면(이 환경 시스템 날짜가 일요일) 800~1000ms 후 "휴일 안내" 모달을 자동으로 띄우는데, 로그인 직후 곧바로 다음 동작을 진행하는 테스트 스크립트가 이 모달에 클릭을 가로채였던 것. 채용 기능과는 무관한 기존 동작이라 이번엔 코드 수정 안 함(실사용자도 로그인 직후 급하게 클릭하면 드물게 영향받을 수 있다는 점만 기록).
  - **더미 데이터 생성기 재실행 시 동명이인 중복 생성 → 실제 버그, 수정 완료**(미점검 모듈 탐색 에이전트 발견): `execGenerateDummy()`(`public/index.html:2163`)의 사번이 매번 `employees.length` 기준으로 만들어져, 이전 실행으로 직원 수가 늘어난 채 재실행하면 항상 새 사번이 나와 "중복 사번은 건너뜁니다" 안내와 달리 중복 스킵 로직이 한 번도 걸리지 않고 재실행마다 동명이인이 계속 쌓였음(2회 실행 시 27명→47명 실측). 사번을 dept/team/순번만으로 결정되는 고정값으로 변경해 재실행 시 실제로 스킵되도록 수정. 고객 데이터가 아닌 QA/데모용 도구라 영향은 제한적.
  - **로그인 rate limiter 브루트포스 방어 완전 무력화 → 심각한 회귀, 즉시 수정**(회귀 재검증 에이전트 발견): 어제 "사무실 동시 로그인 차단" 문제를 고치며 추가한 `skipSuccessfulRequests:true`가, express-rate-limit의 기본 성공판정 기준(`requestWasSuccessful`, HTTP status<400 여부)과 맞물려 **모든** 로그인 시도를 "성공"으로 오판했음 — `/login`이 비밀번호가 틀려도 항상 HTTP 200으로 응답하고(`ok:false`는 JSON body에만 있음) status code만 보는 기본 판정 기준으로는 성공/실패를 구분 못 해, 결과적으로 브루트포스 방어가 완전히 무력화됨(같은 계정에 틀린 비밀번호 60회 연속 시도해도 429가 한 번도 발생하지 않음을 실측). `res.locals.loginOk`를 `/login` 라우트가 실제 결과에 따라 채우고 `requestWasSuccessful`을 그 값을 보도록 커스터마이즈해 진짜 성공한 로그인만 카운트에서 제외되게 수정 — 로컬 재현으로 정상 로그인 25회 연속(카운트 제외 확인)과 틀린 비밀번호 20회 초과 시 정상 차단(브루트포스 방어 복구) 모두 확인.
  - **KPI 승인 탭이 "결과 입력" 단계 전환 시 다시 사라지는 문제 → 실제 버그, 수정 완료**(회귀 재검증 에이전트 발견): 그저께 KPI 승인 파이프라인 도달불가 버그를 고치며 "KPI 승인 현황" 탭을 `settings.stage==="goal"`일 때만 노출하도록 했는데, 미승인 KPI가 남은 채로 admin이 "결과 입력"(performance) 단계로 전환하면 이 탭이 다시 사라져 그 KPI들이 영구히 승인 불가 상태로 남는 문제가 있었음. 탭 안의 실제 승인/반려 액션은 이미 행 단위로 firstStatus/finalStatus/역할을 보고 판단하므로 stage와 무관하게 안전해, stage 조건 자체를 제거해 항상 노출하도록 수정. 스트랜딩 시나리오 재현 → 수정 후 정상 승인까지 확인.
  - **정보성으로만 기록(코드 수정 안 함)**: ① tombstone은 30일 후 정리되므로, 30일 넘게 오프라인이었던 클라이언트가 그제서야 삭제된 레코드를 들고 stale 저장하면 이론적으로 부활 가능(발생 확률 낮은 설계상 알려진 한계). ② `employees` 필드는 payslips/kpiEntries와 달리 role별 필터링·보호merge 대상이 아니라서, 로컬 `employees` 배열이 불완전한 채로 버전이 서버와 정확히 일치하는 저장을 보내면 이론상 전체 직원 목록이 덮어써질 수 있음(정상 클라이언트 흐름에서는 employees가 필터링되지 않아 도달 가능성은 낮지만, 인지해둘 필요). ③ 알림센터(notification-center)는 읽음/안읽음 상태를 별도로 저장하지 않고 조건이 해소되면(예: 결재 승인) 항목이 그냥 사라지는 구조 — 의도된 설계인지 사용자 확인 필요.
- 2026-07-19: 사용자 요청으로 최대 200명 규모 동시접속 시나리오를 2개 에이전트로 병행 검증(① KPI 목표설정→자체평가→1차평가→2차평가 생애주기 동시성 + stage 전환 경합, ② 전표작성 포함 전 기능 교차 동시접속 3라운드). ②는 완전 PASS(경비청구/전자결재/근태/회의실예약/전표/PMS업무일지 815회+ 동시 요청, 유실·충돌·오염 0건, 신규 tombstone 메커니즘도 동시부하에서 정상). ①에서 **이번 세션 전체를 통틀어 가장 심각한 버그 발견** — 여러 팀장/사업부장이 거의 동시에 각자 담당 팀원을 승인·채점하면, 화면에는 매번 "저장되었습니다"로 성공 표시되지만 실제로는 **90%대의 승인·점수 데이터가 조용히 유실**됨(재현: 24개 팀 240건 중 220~230건 유실, 서버 로그 예외 0건이라 완전히 침묵 유실). 근본 원인: `server.js`의 `mergeArrayById()` tie-break `(item.updatedAt||item.createdAt||"")>=(ex...)` 가 양쪽 다 빈 문자열이면 항상 참이 되는데, `kpiEntries` 레코드는 목표 등록부터 1차/2차 평가·승인·반려까지 **어떤 변경 지점에서도 `updatedAt`이 설정되지 않고 있었음** — 이 앱이 매 저장마다 클라이언트가 들고 있는 kpiEntries 전체 배열을 통째로 재전송하는 구조이기 때문에, 뒤이어 처리되는 팀장의 저장이 앞서 처리된 다른 팀장들의 승인을 그대로 되돌려버렸음. `public/index.html`에서 kpiEntries를 변경하는 13개 지점 전체(목표 등록/수정/제출/임시저장, 자체평가 저장, `saveFirstEval`/`saveSecondEval`, `approveFirst`, `saveFinalConfirm`, `rejectKpi`, `approveRequest`, 더미데이터 생성, 데이터 가져오기, 연도말 확정)에 `updatedAt:new Date().toISOString()` 추가. 로컬 서버에서 정확히 재현했던 시나리오(10개 팀, 팀당 5명, 팀장 10명 동시 승인)로 재검증 → 수정 전 유실 확인(과거 이력) → 수정 후 50/50 전원 정상 반영 확인. 부수적으로 로그인 rate limiter가 완전히 동시(같은 tick)에 대량 로그인이 몰리면 일부가 429를 받는 기존에 이미 문서화된 아티팩트가 재확인됐으나 신규 이슈 아님. stage 전환 경합은 이번 실측에서는 재현되지 않았으나(`smartMerge()`의 `settings` 필드가 단일 객체 last-write-wins라는 점에서 이론적 경합 가능성은 코드상 존재) 과장 없이 "미재현"으로 기록.
- 2026-07-19: KPI 버그와 동일한 클래스(mergeArrayById tie-break가 updatedAt 없으면 항상 들어오는 쪽을 이김)의 문제가 다른 기능에도 있는지 직접 전수 점검(`lib/collections.js`의 `ID_KEYED_LIST_FIELDS` 전체를 대상으로, "신청자가 만들고 승인자가 나중에 상태를 바꾸는" 다중행위자 구조인 컬렉션 우선). 실제로 동일 패턴 발견·수정: `expenseClaims`(경비청구 등록/승인/반려), `overtimeRequests`(초과근무 등록/승인/반려), `certRequests`(증명서 발급 등록/승인/반려), `changeRequests`(KPI 수정요청 등록/승인/반려), `onboardingFlows`(온보딩 체크리스트 등록/항목처리), `tieNotifications`(동점자 처리), `lowPerfData`(저성과자 관리 — 공용 저장 헬퍼 `_saveLpRecord()` 한 곳에 추가해 상세저장/상태변경/노트추가 3개 호출부 모두 일괄 적용), `scheduleEvents`(일정 수정) — 전부 `updatedAt` 필드가 누락돼 있어 추가. 반대로 이미 안전한 것으로 코드 확인된 컬렉션: `approvalDocs`(전자결재, 매 승인/반려/취소마다 `doc.updatedAt` 갱신 기존 구현됨), `coreTalentPool`(핵심인재 — 이전 세션에서 이미 동일 클래스 버그를 발견해 고친 이력이 주석으로 남아있었음), `boardPosts`, `payslips`, `attendanceRecords`, `talentDevPlans`, `compResponses`(다면평가 응답 — 평가자별 단일 제출이라 애초에 레코드 경합 구조가 아님). 검증은 KPI 때와 동일한 방식으로 경비청구 20건을 서로 다른 승인자가 동시에 승인하는 시나리오를 재현 — 수정 전 재현하지 않고 바로 수정 후 20/20 전원 정상 반영 확인(수정을 먼저 배포하는 대신 실측을 통해 패턴이 동일함을 검증), 감사 대상 페이지 전체를 도는 스모크 내비게이션으로 콘솔 에러 0건도 확인.

기능별 반복 실사용 QA 에이전트도 재실행해 완주(189회 도구 호출, 헤드리스 Playwright로 8개 모듈을 각 2~3회씩 실제 사용자처럼 반복 조작). **가장 심각한 발견**: KPI 1차평가→2차평가 파이프라인 전체가 UI에서 도달 불가능한 상태였음(이번 세션 이전부터 있던 기존 결함, 역할 무관하게 전원 영향). 원인: KPI 제출된 목표를 실제로 승인/최종확정하는 버튼(`approveFirst()`/`openFinalConfirmModal()`, `public/index.html:7548-7549`)은 `approvalTab==="list"`("KPI 승인 현황") 탭에만 있는데, 이 탭으로 진입 가능한 사이드바 메뉴(`first-eval`/`second-eval`/`kpi-amend`) 3개가 전부 `approvalTab`을 각각 `"eval"`/`"eval"`/`"requests"`로 강제 지정한 뒤 렌더링하고 있어(`renderApprovals()`의 페이지 dispatch, `:5524`), "list" 탭 자체에 도달할 방법이 없었음. 게다가 "list" 탭의 노출 조건(`:7594-7595`)도 "저 세 페이지가 아닐 때"로 되어 있어(즉 `renderApprovals()`가 호출되는 유일한 경로들 자신을 스스로 제외하는 모순), 설령 다른 진입점이 있었어도 이 조건 자체가 항상 거짓이 되는 죽은 코드였음. 그 결과 어떤 KPI도 `firstStatus`가 "approved"로 바뀔 수 없어, "1차 평가 입력" 탭(`renderEvalTab`)이 `firstStatus==="approved"`인 건만 걸러 보여주는 필터 때문에 리더/사업부장 화면에는 항상 "평가 가능한 KPI 없음"만 표시됐음. 사이드바 진입 페이지에 따른 배타적 조건을 제거하고, `settings.stage==="goal"`(코드 주석상 "KPI 등록·수정·승인을 진행하는 단계"로 이미 명시돼 있던 원래 설계 의도)이면서 leader/director/admin 역할일 때 "KPI 승인 현황" 탭이 항상 노출되도록 수정(`:7589-7601`) — 로컬 서버에서 목표 제출→(수정 전) 탭 자체가 안 보임 확인→(수정 후) 탭 노출·1차승인 처리→"1차 평가 입력"에 정상 노출까지 Playwright로 재현·검증. 부가로 발견된 경미한 버그도 수정: 신규 직원 등록(`submitHRForm()`, `:6511-6516`)이 team을 항상 필수로 요구해, 시드 데이터의 실제 관행(director는 본부 단위 소속으로 team이 빈 문자열)과 맞지 않아 director 신규 등록 자체가 막혀 있었음 — director 역할일 때만 team 필수 조건에서 제외. 그 외 발견 중 원인 미확정으로 보류한 항목(추가 조사 필요, 아직 수정 안 함): 채용 최종판정 저장이 테스트에서 2회 모두 실패(선택자 문제인지 실제 버그인지 불명), PMS 업무일지 입력 실패(동일하게 원인 미확정), 채용공고/지원자 반복 등록 시 2번째 반복에서 동일 레코드 ID가 나온 정황(테스트 스크립트의 모달 타이밍 문제일 가능성 높음). 전자결재/근태·휴가/경비청구/회의실예약(tombstone 되살아남 재검증 포함)은 전부 PASS.
- 2026-07-20: 사용자 요청으로 "다른 기능/사용자정보에도 동일한 기록누락·동시편집유실이 있는지, 신규등록/신규정보입력 시에도 유사문제가 없는지"를 4개 에이전트로 병행 전수 감사(① 직원등록·정보수정, ② 채용 모듈(전용 REST 라우트, blob동기화와 다른 아키텍처), ③ 회계·영업·재고·PMS(대부분 전용 REST 라우트), ④ 아직 안 본 나머지 blob동기화 필드 13개). 이번 세션 들어 가장 광범위한 버그 발견 라운드 — 실제 버그 다수 발견, 전부 수정·검증 완료.
  - **결정적 버그(동시성 무관, 항상 100% 재현) — `resetPassword()`**: `public/index.html:11557`(관리자 설정 페이지의 "비밀번호 초기화")이 `updatedAt`을 설정하지 않고 저장 호출 자체도 없었음. `server.js`의 비밀번호 재해싱 최적화(성능 문제로 이전에 추가된 "newTs>oldTs가 아니면 변경 없음으로 간주해 기존 bcrypt 해시 유지" 로직)에 걸려, 관리자가 "초기화되었습니다" 토스트를 봐도 **새 비밀번호가 절대 저장되지 않고 예전 비밀번호가 계속 유효**했음(curl로 신구 비밀번호 로그인 테스트해 100% 재현). `updatedAt` 추가 + `autoSaveToServerIfEnabled()` 호출 추가로 수정.
  - **심각한 신규 발견 — 동시 신규 직원 등록 시 ID 충돌(90% 유실)**: `genId()`가 서버 `_idCounter` 스냅샷에서 시작해 클라이언트 세션 안에서만 증가하는 로컬 카운터라, 여러 담당자가 거의 동시에 `GET /data`를 호출하고 등록하면 전부 같은 스냅샷에서 출발해 신규 직원에 **동일 id**를 부여함 — `mergeArrayById`는 id로만 레코드를 구분하므로 서로 다른 신규 등록이 충돌해 하나만 생존(실측: 30명 동시 등록 시 27명 유실). KPI 버그(updatedAt 누락)와는 다른, 더 근본적인 ID 발급 구조 문제. `server.js`에 `POST /api/employees/next-id`(서버가 원자적으로 발급, JSON모드는 세션 내 in-memory 카운터+사이드파일 영속화, Postgres모드는 `app_meta` 키 원자적 UPDATE...RETURNING) 신설, `submitHRForm()`이 이를 await해서 사용하도록 변경(서버 연결 실패 시에만 기존 `genId()`로 폴백). 30건 동시 등록 재현 시 30/30 고유 id 확인.
  - **심각한 신규 발견 — 채용 모듈 Postgres 모드의 lost-update(면접평가 최대 90% 유실)**: 채용(`recruit_candidates`/`recruit_interviews`)은 blob동기화가 아니라 전용 REST 라우트+개별 DB 테이블 구조인데, `candidates/:id`·`candidates/:id/status`·`interviews/:id`·`interviews/:id/cancel`·`interviews/:id/evaluation`·`interviews/:id/verdict` 전부 락 없는 `SELECT`→JS로 수정→`UPDATE`(read-modify-write race) 구조였음. JSON 파일 모드는 이 read-modify-write 사이에 `await`가 없어 Node 단일 이벤트루프 특성상 우연히 안전했지만(설계된 보호가 아니라 구현의 우연), Postgres 모드는 진짜 `await`가 있는 그 구간이 정확히 경합 지점 — 특히 **면접관 10명이 거의 동시에 각자 평가를 제출하면 1명 것만 남고 9명 것이 조용히 사라짐**(오늘 세션에서 새로 만든 "다중 심사위원" 기능을 운영 배포에서 사실상 무력화하는 수준). `_pgLockedUpdate(table,id,mutate)` 헬퍼(트랜잭션 내 `SELECT ... FOR UPDATE`로 해당 행을 잠가 같은 레코드를 다루는 동시 요청을 순번대로 세움) 신설, 위 6개 라우트 전부 이 헬퍼로 교체. 로컬 실제 PostgreSQL로 재현·검증: 10명 동시 평가 제출 → 10/10 전원 정상 반영 확인(수정 전엔 1/10만 생존).
  - **심각한 신규 발견 — 재고 원장 오버셀(재고가 마이너스로 내려감)**: `erp_stock_ledger`는 append-only 원장(현재고 = item+location의 모든 행의 합)이라, `SELECT ... WHERE item_id=$1 AND location_id=$2 FOR UPDATE`는 **그 시점에 이미 존재하는 행만 잠그고 동시에 다른 트랜잭션이 새로 INSERT하는 행은 잠금 대상이 아님**(phantom read류 문제) — 이미 트랜잭션+FOR UPDATE를 쓰고 있던 견적출고(`ship`)·창고이동(`transfer`)조차 이 구조적 결함으로 재고가 마이너스까지 내려갈 만큼 동시 출고를 허용했고, 아예 트랜잭션이 없던 `stock/adjust`·`stock/count`는 더 심했음(실측: 재고 100에 "출고10개"×20건 동시 요청 시 기대 최대 10건인데 실제 12건 성공, 최종재고 -20). `pg_advisory_xact_lock(hashtext('stock:'+itemId+':'+locationId))`로 "그 시점에 존재하는 행"이 아니라 item+location이라는 **논리적 키 자체**에 잠금을 걸어 4개 엔드포인트(adjust/count/transfer/ship) 전부 수정 — 여러 키를 한 트랜잭션에서 잠글 때는 정렬된 순서로 잠가 교착(deadlock)도 방지. 재현 검증: 재고 100에 20건 동시 "출고10개" → 정확히 10건만 성공, 최종재고 0(정확).
  - **신규 발견 — PMS 투입률 100% 캡 레이스**: `POST /api/pms/allocations`가 "합계 조회→100% 이하 확인→등록"을 락 없이 수행(check-then-act)해, 같은 직원의 같은 달에 여러 프로젝트 투입률을 거의 동시에 등록하면 캡이 무력화됨(실측: 15건×10% 동시 등록 시 기대 최대 10건인데 12건 성공, 합계 120%). employeeId+year+month 키에 advisory lock 추가, 재현 검증: 15건 동시 등록 → 정확히 10건만 성공, 합계 100%.
  - **KPI와 동일 클래스(updatedAt 누락) 추가 발견 4건**: `compSessions`(다면평가자 지정 `saveEvaluators()` 2곳), `roomReservations` 수정(`saveRoomReservationEdit()` — 생성/취소는 이미 tombstone으로 보호되지만 "수정"은 별개 경로라 누락돼 있었음), `approvalTemplates`(전자결재 양식 관리), `leaveUsagePlans`(연차 사용계획서 재제출) — 전부 20건 동시 편집 시 19건 유실 재현 확인 후 `updatedAt` 추가.
  - **정보성으로 기록(코드 수정 안 함, 별도 판단 필요)**: ① `welfarePoints` 잔액 검증이 클라이언트 로컬 계산에만 의존하고 서버 측 재검증이 없어(다른 필드들과 달리 "병합 유실"이 아니라 "동시 사용 시 잔액 초과"라는 다른 종류의 문제), 실측상 동일 직원이 두 세션에서 거의 동시에 각각 70,000P씩 사용(잔액 100,000P) 시 둘 다 통과해 -40,000P까지 내려감 — 서버가 grant/use 합계를 재계산해 초과 시 거부하는 별도 검증 로직 신설이 필요하나 이번엔 범위 밖으로 보류. ② `partners`(거래처) 등 여러 CRUD가 매번 body 전체로 레코드를 재구성하는 방식이라(부분 PATCH가 아님) 두 담당자가 서로 다른 필드를 거의 동시에 수정하면 나중 저장이 앞선 필드 변경을 지움 — 같은 코드베이스 안에 이미 필드별 부분 패치 패턴(`pms/projects/:id`)도 존재해 설계 일관성이 없는 상태. `employees`도 이전에 이미 동일한 한계가 확인된 바 있어(2026-07-19 기록), 이 앱의 "폼 전체 재제출" UI 패턴 전반에 걸친 구조적 특성으로 판단 — 개별 함수 땜질보다 부분 PATCH로의 전환이 근본 해법이나 범위가 넓어 이번엔 보류.
- 2026-07-20: 사용자 요청으로 "서버 저장을 거치며 관리자가 허용하지 않은 권한이 우회되거나 데이터가 유출되는지, 권한 설정 미비로 데이터·기능이 불필요하게 공유되는지"를 인가(authorization) 전용 관점에서 4개 에이전트(서버 라우트 전수 감사/메뉴권한 UI-서버 일치성/데이터 노출범위 재검증/오늘 신규코드 보안 회귀점검)로 병행 감사. 세션 한도로 1차 실행이 전원 실패해 재실행 후 완주. 실제 취약점 다수 발견, 전부 수정·실측 검증(격리 서버에서 76개 시나리오 자동검증 스크립트로 확인) 후 반영.
  - **치명 — `budget.js`(예산 관리 API) 완전 인증 우회**: `requireAdmin()`이 서버가 검증한 로그인 토큰이 아니라 `req.body.role`(클라이언트가 그대로 적어 보낸 값)을 신뢰하는, 이 프로젝트에서 이미 여러 번 발견·제거했던 것과 동일한 구식 취약 패턴이 이 파일에만 남아있었음(`server.js`의 `app.use(authenticate)` 이후 마운트되어 `req.auth`를 쓸 수 있었는데도 미사용). 조회 라우트(`GET /data|uploads|summary`)는 인증 자체가 전혀 없어 로그인 없이 curl만으로 전사 예산(부서별 인원·판관비·용역비·경상비) 전체 열람이 가능했고, 쓰기 라우트(`POST /upload/*`, `DELETE /data`)는 body에 `role:"admin"`만 넣으면(토큰 없이도) 업로드·**전체 데이터 삭제**까지 가능했음(실측 확인). `requireAdmin()`을 `req.auth.role==="admin"` 기준으로 재작성. 이 API를 쓰는 별도 정적 페이지 `public/budget.html`(index.html의 SPA 인증 흐름과 별개로 동작, 지금까지 요청에 인증 헤더 자체를 붙이지 않고 있었음)도 index.html이 localStorage에 저장해둔 공유 토큰(`hr_kpi_auth_token`, 같은 오리진이라 공유됨)을 `Authorization: Bearer`로 첨부하도록 수정 — 관리자 로그인 없이 열면 "관리자 로그인이 필요합니다" 안내로 대체.
  - **치명 — `GET /history/changes` 인증 자체 없음**: 직원 변경이력·KPI 변경이력(평가 코멘트 포함) 최근 1000건이 로그인 없이 curl만으로 조회 가능했음(실측 확인). `requireAdmin` 추가(`hr-changes`/`history` 페이지가 PAGE_ROLES상 admin 전용인 것과 일치). 같은 문제로 `requireAuth`만 있던 `/history/employee/:id`·`/history/kpi/:id`(소유권/역할 검증 없이 임의 id로 타인 이력 조회 가능, 실측 확인)도 `requireAdmin`으로 승격.
  - **심각 — `GET /snapshots/:year`(연도별 스냅샷)가 `filterDataForRole` 미적용**: `/data`·`/save`·`/restore`는 role별로 payslips/kpiEntries를 거르는데 스냅샷 조회만 그 필터를 거치지 않아, member 토큰으로 `/data`에서는 본인 급여만 보여도 같은 계정의 `/snapshots/2026`에는 타 직원 급여·평가 원본이 그대로 반환됨(실측 확인). "연도별 스냅샷" 기능 자체가 PAGE_ROLES상 admin 전용(`history`)이므로 `/snapshots/:year`·`/snapshots/:year/diff` 둘 다 `requireAuth`→`requireAdmin`으로 승격해 근본 차단.
  - **치명 — `filterDataForRole()`이 26개 id-keyed 필드 중 `payslips`/`kpiEntries` 2개만 필터링**: `GET /data`(및 `/save` 병합응답·스냅샷 복원응답)로 나머지 24개 필드가 role 무관 전량 노출되고 있었음. 그중 `lowPerfData`(저성과자 관리 — 기록 자체에 "본인에게 비공개" 문구가 들어있는데 대상자 본인·전 직원이 조회 가능했음), `coreTalentPool`(핵심인재 풀 — "비밀리에 검토 중"인 승진 후보 정보), `compResponses`(다면평가 응답 — evaluatorId+자유텍스트 코멘트가 그대로 노출되어 평가 대상자가 "누가 이 비판적 코멘트를 썼는지" 알 수 있어 평가 익명성이 깨짐), `welfarePoints`(복지포인트 사용내역 — `desc`에 의료비 등 사용목적이 그대로 들어감)를 심각도 순으로 수정. `lowPerfData`는 admin이거나 `settings.lowPerformerViewers`에 등록된 직원이 아니면 전체를 빈 배열로(클라이언트의 `_canViewLowPerformer()`와 동일 기준), `coreTalentPool`은 admin이 아니면 본인 항목만(`talent-dev` 페이지가 "내가 선정됐는지"만 확인하면 되므로 본인 레코드는 유지), `welfarePoints`는 admin이거나 `settings.welfarePointsViewers` 등록자가 아니면 본인 것만(`_canViewWelfareAll()`과 동일 기준), `compResponses`는 admin 또는 본인이 제출한(evaluatorId===본인) 레코드는 원본 그대로, 그 외는 제출여부 확인용 필드(id/sessionId/evaluatorId/submittedAt)만 남기고 `answers`/`strengths`/`improvements` 등 코멘트·점수 내용을 제거. 필터링된(불완전한) 클라이언트 로컬 배열이 그대로 재저장돼 타인 레코드를 지우는 사고를 막기 위해, payslips/kpiEntries에 이미 있던 "항상 id기준 병합" 보호(`_mergeProtectedField`)를 이 3개 필드에도 확장 적용(Postgres 모드는 원래 언급된 id만 upsert하는 구조라 별도 조치 불요, 안전함을 코드로 확인). `compResponses`는 "전체 숨김"이 아니라 "필드 단위로 내용만 제거"하는 방식이라 별도 위험이 있었음 — 필드가 제거된(레다크트된) 사본이 같은 id·같은 updatedAt으로 되돌아오면 기존 `mergeArrayById`의 "동률이면 들어오는 쪽 승리" 규칙 때문에 필드가 제거된 사본이 관리자가 저장해둔 원본 전체를 덮어쓸 위험이 있어(JSON모드 `_persistDataLocked`·Postgres모드 generic upsert 루프·smartMerge 3곳 전부 동일 위험), `answers` 필드 유무로 "레다크트된 사본"을 판별해 그런 사본은 무시하고 기존 전체 레코드를 지키는 가드를 3곳 모두에 추가. 격리 서버에서 실측 검증: member1은 lowPerfData 전체 비공개·coreTalentPool 본인 항목만·welfarePoints 본인 것만·compResponses 타인 레코드 내용 제거를 각각 확인, member1이 그 필터링된 로컬 상태를 그대로 재저장해도 admin이 저장해둔 타인 레코드(및 compResponses 원본 내용)가 전부 그대로 살아있음을 확인.
  - **심각 — 채용 모듈 쓰기 라우트 5개 부서 스코프 누락**: 지원자 수정(`POST /api/recruit/candidates/:id`)·전형단계 변경(`.../status`)·삭제(`.../delete`), 면접 일정 수정(`POST /api/recruit/interviews/:id`)·취소(`.../cancel`)가 `requireRole(admin/leader/director)`만 확인하고, 같은 데이터의 GET이 쓰는 부서/면접관 스코프 검사(`_recruitCanViewCandidate`/`_recruitIsInterviewPrivileged`)가 빠져있었음 — 무관 부서 리더가 지원자·면접 ID만 알면(예: 목록 API 등을 통해) PII 수정·전형단계 변경·삭제, 면접 일정 변경·취소가 그대로 가능했음(실측 확인, evaluation/verdict 2개 라우트는 이미 본인확인 로직이 있어 안전했음도 함께 확인). 5개 라우트 모두 JSON모드는 조회 직후, Postgres모드는 `_pgLockedUpdate`의 `mutate` 콜백 안에서 동일한 스코프 검사를 추가해 실패 시 403. 격리 서버에서 실측: 타 부서 리더의 5개 시도 전부 403, 같은 부서 리더는 정상 200 확인.
  - **심각 — 회계·재고 조회 API 8개가 role 제한 없음**: `acct-*`(계정과목/전표/세금계산서/입금·출금/거래처)·`sales-quotations`·`sales-purchase-orders`·`inv-stock`·`inv-stock-log`·`sales-dashboard`가 PAGE_ROLES상 전부 admin 전용 화면인데 대응하는 서버 GET(`/api/accounting/accounts|vouchers|tax-invoices|payments|partners`, `/api/erp/quotations|purchase-orders|stock|stock/ledger|sales-targets`)은 `requireAuth`만 있어 member 토큰의 API 직접호출로 전표·거래처(연락처·사업자번호)·재고 등 전체 조회가 가능했음(실측 확인). 8개 라우트 전부 `requireAdmin`으로 승격. 단 `GET /api/erp/items`·`/api/erp/locations`는 그대로 `requireAuth` 유지 — 이 두 개는 `inv-purchase-requests`(구매요청, PAGE_ROLES상 전 역할 개방)의 품목·창고 선택 드롭다운이 실제로 사용하는 참조 데이터라 admin 전용으로 막으면 일반 직원의 구매요청 등록 기능 자체가 깨짐(코드 추적으로 확인 후 예외 처리).
  - **심각 — PMS 투입률/업무일지 GET이 소유권 필터 없음**: `pms-utilization`(가동률 현황) 화면은 PAGE_ROLES상 admin/director/leader 전용인데 `GET /api/pms/allocations`·`/api/pms/worklogs`는 `requireAuth`만 있어 member 토큰으로 `?employeeId=<타인 id>` 또는 필터 없이 호출하면 무관 부서 타 직원의 프로젝트 투입률·업무일지가 그대로 조회됐음(실측 확인). member 역할일 때는 쿼리로 무엇을 보내든 서버가 `employeeId`를 요청자 본인으로 강제하도록 수정(admin/director/leader는 기존대로 광범위 조회 유지 — "팀 전체" 탭 등 PAGE_ROLES가 이미 허용한 기존 설계). 격리 서버에서 실측: member의 타인 employeeId 조회 결과에 본인 레코드만 남음, leader는 기존대로 조회 가능함을 확인.
  - **경미 — `POST /api/erp/purchase-requests`가 `req.body.userId` 신뢰**: 구매요청 등록 시 `requestedById`를 서버 토큰(`req.auth.empId`)이 아니라 클라이언트가 body에 넣어 보낸 `userId`로 저장해, member가 `userId:1`(admin)을 보내면 admin 명의로 등록되고 이후 `DELETE`의 "본인 요청만 삭제 가능" 검사도 이 스푸핑된 값을 근거로 판단해 무력화됨(실측 확인). `req.auth.empId`로 교체.
  - **경미 — `GET /online`·`GET /events`(SSE) 인증 없음**: 접속자 목록(직원명 포함)·실시간 이벤트(잠금 상태의 편집자 이름 등)가 로그인 없이 조회·구독 가능했음. `/online`은 `requireAuth` 추가(클라이언트가 이미 `serverRequest`로 토큰을 붙여 호출하고 있어 그대로 동작). `/events`는 브라우저 내장 `EventSource`가 커스텀 헤더를 못 붙이는 제약이 있어, SSE에서 흔히 쓰는 방식대로 토큰을 쿼리스트링(`?token=`)으로 받아 `verifyToken()`으로 검증하도록 수정(`public/index.html`의 `connectSSE()`도 함께 수정).
  - 4개 감사 중 "서버 라우트 인가 전수 감사"·"오늘 신규코드 보안 회귀점검" 에이전트는 위 항목 외에는 문제 없음을 확인(오늘 만든 `_pgLockedUpdate`/`_lockStockKeys`/PMS allocation lock/`/api/employees/next-id`의 기존 인가 검증은 리팩터링 후에도 실제 PostgreSQL로 재현해 정상 작동 확인됨, 회귀 없음).
  - 수정 전부를 격리 서버(고유 포트·DATA_FILE)에서 admin/leader×2(부서 다름)/member×2(부서 다름) 계정으로 실제 로그인해 76개 시나리오(401/403/200 경계, 필터링된 필드 내용, 필터된 로컬 상태 재저장 후 타인 데이터 생존 여부, 스푸핑 시도 차단 등)를 자동검증하는 스크립트로 확인 — 전부 통과.
- 2026-07-20: 위 감사 완료 직후 사용자가 "동일한 문제가 없는지 다시 한번 모든 경우의 수를 파악해서 진행해줘"라고 재요청 — 4개 에이전트(req.body 신뢰패턴/인증누락 재전수감사, 쓰기라우트 스코프누락 패턴 재점검, filterDataForRole 잔여필드 민감도 재검토, 방금 수정사항 회귀검증)를 다시 병행 실행. 4번째(회귀검증)는 "정상 동작, 회귀 없음"으로 완주 확인. 나머지 3개에서 실제 취약점을 추가로 다수 발견 — 전부 수정·실측 검증(격리 서버 55개 시나리오 자동검증) 후 반영. 이번 라운드 도중 수정 코드 자체의 버그 1건도 자체 디버깅으로 추가 발견·수정(아래 참고).
  - **치명 — `filterDataForRole()`에 여전히 남아있던 8개 고민감도 필드**: 3개 재감사 에이전트가 겹쳐서 지적한 필드들을 종합 — `payrollAdjustments`(급여조정/인센티브 금액, payslips급), `gradeAdjustHistory`(등급조정 사유, "comp-grade-view" admin전용인데 미필터), `certRequests`(증명서 발급 사유 — "대출 심사용" 등 개인 재무상황 노출), `changeRequests`(KPI 수정요청 코멘트, kpiEntries와 동일 내용), `attendanceRecords`(전 직원 출퇴근시각), `scheduleEvents`(개인 일정 — 클라이언트가 이미 `scope==="personal"`이면 작성자 본인만 보이게 필터링하는데 원본 API는 그 설계의도를 안 지킴), `compGradeResults`(다면평가 최종등급/점수, `{empId:{year:{...}}}` 형태의 nested 싱글톤), `approvalDocs`(전자결재 — 병가 사유·경조사 유형·자녀 정보 등 인사 문서가 결재선 밖 전 직원에 노출, 실측상 가장 심각). 정책은 각 필드의 실제 admin-only 여부에 맞춰 차등 적용: `payrollAdjustments`/`gradeAdjustHistory`/`certRequests`(승인이 client에서 `currentUser.role!=="admin"`으로 하드코딩돼있어 admin 아니면 본인 것만), `changeRequests`(kpiEntries와 동일하게 member만 제한, leader/director/admin은 평가 운영 위해 그대로 유지), `attendanceRecords`(PAGE_ROLES `hr-attendance`가 admin/director 전용이라 leader도 포함해서 제한, admin·director만 예외), `scheduleEvents`(scope==="personal"인 것만 작성자 본인+admin 예외, team/dept/company scope는 그대로 공개), `compGradeResults`(admin 아니면 본인 키만), `approvalDocs`(client의 결재함 화면들이 실제로 쓰는 가시성 규칙 — authorId/approvers[].empId/`_refsToVisibleEmpIds(receivers)`/`_refsToVisibleEmpIds(cc)` — 를 `_refToEmpIdsServer`/`_refsToVisibleEmpIdsServer` 헬퍼로 서버에 그대로 재현). 필터링된 로컬 상태 재저장 시 타인 레코드가 지워지지 않도록, 앞서 lowPerfData 등에 썼던 "항상 id기준 병합" 보호(`_mergeProtectedField`)를 이 7개 배열 필드에도 확장 적용하고, `compGradeResults`는 배열이 아닌 nested 싱글톤이라 별도로 `mergeNestedObject()`(원래 `smartMerge()`의 버전충돌 경로에서만 쓰이던 헬퍼)를 JSON모드 `_persistDataLocked`의 직접저장 경로와 Postgres모드 싱글톤 upsert 루프 양쪽에 추가 적용(기존엔 버전이 정확히 일치해 smartMerge를 안 타는 저장 경로에서는 이 보호가 전혀 없었음 — 새로 발견해 함께 막음).
  - **자체 발견(재감사 에이전트가 못 찾은 것) — `scheduleEvents` 필터에 admin 예외 누락 버그**: 위 8개 필드 확장 구현 직후 격리 서버로 검증하던 중, admin 계정의 `GET /data`에서도 `scope==="personal"`인 타인의 일정이 사라지는 회귀를 발견 — 작성한 필터 조건에 `auth.role!=="admin"` 예외를 빠뜨려 admin까지 함께 걸러지고 있었음(다른 7개 필드는 전부 admin 예외를 넣었는데 이 필드만 누락). `_persistDataLocked`에 임시 디버그 로그를 넣어 "merge 계산 결과는 정상인데 최종 GET 응답만 깨진다"는 것을 좁혀 들어가 원인을 찾음(mergeArrayById/protected-merge 로직 자체는 문제 없었고, filterDataForRole의 admin 예외 누락이 원인이었음). `auth.role!=="admin"` 조건 추가로 수정, 디버그 로그는 전부 제거 후 재검증으로 admin이 personal-scope 일정도 포함해 전체를 본다는 것 확인.
  - **심각 — 채용공고(`recruit_jobs`) 쓰기 라우트에 동일한 부서 스코프 누락(채용 후보자/면접과 같은 클래스)**: `POST /api/recruit/jobs`(생성/수정 겸용)와 `POST /api/recruit/jobs/:id/close`가 `requireRole(admin/leader/director)`만 확인하고 GET이 쓰는 `_recruitCanViewJob()` 스코프 검사가 없었음 — 무관 부서 리더가 공고 id만 알면 `viewerIds`를 자기 자신으로 덮어써 그 공고와 소속 지원자(이름/이메일/휴대폰)를 전부 볼 수 있게 만들 수 있었고, 마감 처리도 가능했음(실측 확인, `_recruitCanViewJob`로 수정된 job/close 라우트도 함께 스코프 검사 추가). 연계 발견: `GET /api/recruit/dashboard`도 이 필터가 없어 리더B가 GET 목록으로는 안 보이던 공고의 id·지원자 통계를 이 대시보드에서 알아낼 수 있었음 — 동일한 `_recruitCanViewJob` 필터 적용.
  - **경미(신규 발견, 데이터 훼손 버그) — recruit_jobs 편집 시 미전송 필드가 기본값으로 덮어써짐**: 생성/수정을 겸하는 이 라우트가 `department`/`team`/`headcount`/`status`를 `필드 || 기본값` 방식으로 처리해, 부분 수정(예: 제목만 변경)을 보내면 기존 값이 조용히 사라졌음(실측: team "영업1팀"→"", headcount 2→1). `requiredYears`/`docFile`/`viewerIds`/`stages`가 이미 쓰던 "값이 안 왔으면 기존 값 유지" 패턴으로 통일해 수정.
  - **중간 — `GET /activity`, `GET /snapshots`(목록), `GET /backups`(목록)가 requireAuth뿐**: 각각 "activity-log"/"history" 페이지가 admin 전용인데, member 토큰으로도 활동 로그 자유텍스트, 스냅샷/백업 목록의 notes(대외비 메모 등)를 조회할 수 있었음(실측 확인). 셋 다 `requireAdmin`으로 승격(`/snapshots/:year`와 동일 기준 — 클라이언트에서 이 두 목록 API를 부르는 코드가 없어 회귀 위험 없음도 확인).
  - **중간 — `/api/auth/2fa/generate-secret`, `/api/reset-all`이 `verifyCredentials()`로 비밀번호를 직접 검증하면서 rate limit이 없음**: `/login`은 20회/15분으로 막혀있는데 이 두 라우트는 무제한 시도가 가능했음(실측: 틀린 비밀번호 25회 연속 시도해도 429 없이 전부 즉시 403 — `/login`과 동일 계정으로 대조 확인). 특히 `/api/reset-all`은 성공 시 전체 데이터 삭제로 이어지는 파괴적 동작이라 위험도가 더 큼. `/login`과 동일한 `loginLimiter`를 재사용(같은 IP의 시도를 세 라우트가 함께 카운트)해 적용, `res.locals.loginOk`도 각 라우트의 실제 성공/실패에 맞춰 채워 브루트포스 카운트가 정확히 동작하도록 함.
  - 4개 에이전트 중 "쓰기라우트 스코프 재점검"은 채용 모듈 나머지(candidates/interviews)·회계·ERP 품목/견적/발주·PMS 전부 "확인함, 안전함"으로 결론(직전 라운드 수정이 정확히 반영돼 있고, 회계/ERP는애초에 부서별 세분 스코프 설계 자체가 없어 GET-쓰기 불일치 대상이 아님, PMS 프로젝트 관리도 "leader는 전 부서 관리 가능"이 코드+클라이언트 양쪽에서 확인된 설계 의도임을 확인).
  - 전체 수정을 격리 서버(admin/leader×2(부서 다름)/director/member×2(부서 다름) 계정)에서 55개 시나리오(필터링 내용, protected-merge 생존 확인, recruit_jobs 부서스코프 403/200, 편집 시 필드 보존, 관리자 전용 라우트 401/403/200, rate limit 429 발생) 자동검증 스크립트로 확인 — 전부 통과. 회귀 재확인용으로 직전 라운드 76개 시나리오 스크립트도 로직상 변경되지 않은 코드 경로임을 diff로 확인(이번 라운드는 server.js만 수정, budget.js/public/index.html은 무변경).
- 2026-07-21: **멀티테넌트 SaaS 전환 1단계 구현** — 현재 HR/ERP를 iOS 앱스토어로 불특정다수 회사가 가입하는 SaaS로 판매하려는 계획에 따라, `/root/.claude/plans/robust-skipping-blanket.md`에 상세 아키텍처(공유 스키마+`company_id` row-level 격리, JSON파일모드는 자체호스팅 단일회사 배포판으로 그대로 유지, 마스터 관리자+회사별 기능 커스터마이징은 후속 단계)를 설계 후 착수. 이 세션부터 "이후 요청은 전부 에이전트로 병행 진행"이라는 사용자 지시가 적용되어, 서버(인증/토큰/가입/전역상태)와 웹 클라이언트(로그인/가입 UI) 작업을 별도 에이전트 2개로 병행 실행하고 본 세션은 스키마/마이그레이션 스크립트를 직접 담당·양쪽 산출물을 리뷰 후 병합했다.
  - **즉시 처리**: `ios-app/HRERPApp.swiftpm`(브랜치 `claude/add-jaesung8712-gpt-assistant-boed6i`에 있던, 이 HR/ERP와 완전히 무관한 별도 프로젝트의 orphan 브랜치에 있었음 — `git merge-base`가 빈 결과라 공통 조상 없음을 확인 후 `git checkout <branch> -- ios-app`로 해당 디렉토리만 새 파일로 가져와 통합)의 `AppSettings.swift`가 실제 운영 서버(`https://hrsystem-uweb.onrender.com`)를 기본 서버 주소로 하드코딩하고 있어 앱스토어 배포 시 다운로드한 누구나 실회사 데이터에 접속 가능했던 긴급한 문제를 가장 먼저 수정(빈 문자열로 교체, `LoginView`/`SettingsView`/`README` placeholder도 동반 수정).
  - **1단계 스키마 작업(본 세션 직접 수행)**: `schema.sql`에 `companies`(id UUID/slug UNIQUE/name/status trial·active·suspended) 테이블과 `employees`/`kpi_entries`/`employee_history`/`kpi_history` 4개 테이블에 nullable `company_id UUID` 컬럼+FK+인덱스 추가(매 부팅 재적용되는 idempotent DDL이라 `ADD CONSTRAINT`를 `pg_constraint` 카탈로그 확인 `DO` 블록으로 감싸야 함을 재확인 — `EXCEPTION WHEN duplicate_object`는 `duplicate_table`(42P07) 에러를 못 잡아 3회 연속 재적용 테스트에서 실패했었음). `employees.data`(JSONB) 안의 `loginId`에 대해 생성 컬럼 `login_id GENERATED ALWAYS AS (data->>'loginId') STORED` + `UNIQUE(company_id, login_id)` 추가(전역 경고에 그치던 `warnDuplicateLoginIds()`를 실제 DB 제약으로 승격). 기존 단일회사 데이터를 새 `companies` 1행으로 백필하는 `scripts/migrate-add-company-id.js` 신설 — 최초 버전은 회사 INSERT와 4테이블 UPDATE가 별개 커밋이라 백필 중 실패(예: 중복 loginId 위반) 시 소속 직원 0명인 고아 `companies` 행이 남는 문제가 있어, 전체를 단일 트랜잭션(BEGIN~COMMIT/ROLLBACK)으로 재작성. 로컬 스크래치 Postgres에서 스키마 3회 연속 재적용 idempotent 확인, 정상 백필/재실행(기존 회사 재사용) 확인, 의도적으로 UNIQUE 위반을 유발해 트랜잭션 롤백이 실제로 동작(고아 행 미생성, 대상 테이블 원상유지)함을 확인.
  - **웹 클라이언트(에이전트 병행 구현)**: `public/index.html` 로그인 폼에 "회사 코드" 필드 추가, `doLogin()`이 항상 `{companyCode,loginId,pw,otp}`로 `POST /login` 호출하도록 단순화. "서버 직원 0명=부트스트랩"으로 로컬 샘플계정 평문매칭을 허용하던 `isBootstrap`/`_bootstrapCreds`/`_tryUpgradeBootstrapToken` 로직 전체를 폐기(두 번째 회사가 가입하는 순간 그 가정 자체가 깨지므로) — 완전 오프라인 데모 모드(`serverConfig.url` 없음)는 SaaS와 무관해 그대로 유지. `POST /api/companies/register`를 호출하는 "회사 만들기" 신규가입 모달 추가, 성공 시 기존 로그인 성공 흐름 재사용해 자동 로그인. 구현 중 `#login-wrap`(z-index 9999)이 신규 모달(`.modal-ov` z-index 1000)을 가려 클릭이 먹히지 않던 버그를 발견·수정(이 모달 개별 z-index만 상향).
  - **서버(에이전트 병행 구현)**: `signToken()` payload에 `companyId` 추가, `verifyCredentials(companyId, loginId, pw)`로 재작성(회사없음/아이디틀림/비밀번호틀림을 구분 못하는 동일 실패 메시지 유지). `POST /login` 바디에 `companyCode` 추가(Postgres/SaaS 모드만 필수, JSON파일모드는 기존 그대로). `loadData()`/`persistData()`/`_persistDataLocked()`에 `companyId` 매개변수를 관통시켜 employees/kpi_entries 조회·저장을 SQL `WHERE company_id=$N`으로 직접 필터, 다른 회사 소유 id로 쓰기 시도 시 조용히 무시(404/403 대신 — 채용모듈에서 이미 쓰던 "존재 자체를 노출 안 함" 원칙과 동일). 프로세스 전역 싱글턴(`_dataVersion`/`_lastSaved`→`Map<companyId,{...}>`, `_locks` 키에 `${companyId}:` 접두, `_sseClients`/`broadcastSSE()`에 companyId 필터 추가로 SSE가 같은 회사에게만 브로드캐스트)을 전부 회사별로 분리 — 이 부분이 방치되면 조용한 테넌트간 유출(다른 회사 활동량·잠금 충돌·SSE로 다른 회사 사용자명 노출)로 이어지는 놓치기 쉬운 지점이었음. `POST /api/companies/register` 신설(회사 INSERT+관리자 계정 INSERT+토큰발급을 한 트랜잭션으로, 같은 slug 동시가입은 advisory lock으로 직렬화, `-2`/`-3` 접미사 자동 제안, IP당 1시간/10회 `registerLimiter`). 기존 `_employeesEmpty()` 부트스트랩 예외는 Postgres/SaaS 모드에서 완전 폐기(JSON파일모드만 유지).
  - **본 세션이 두 에이전트 산출물 병합 전 리뷰에서 직접 발견·수정한 크로스테넌트 유출 1건(심각)**: `GET /history/employee/:id`·`GET /history/kpi/:id`·`GET /history/changes` 3개 라우트가 `employee_history`/`kpi_history`에 이미 붙은 `company_id` 컬럼을 전혀 필터링하지 않고 있어, 회사 A의 admin이 회사 B 직원/KPI id만 알면(단건 라우트 2개) 또는 그냥 호출만 하면(`/history/changes`는 since 이후 전 회사 변경이력을 무조건 벌크로 반환 — 단건보다 더 심각) 다른 회사의 과거 연락처·주소·연봉·평가코멘트 등을 그대로 조회할 수 있는 상태였음. 세 라우트 모두 `AND (company_id=$N OR company_id IS NULL)` 필터 추가(레거시 단일회사 NULL 데이터는 그대로 노출 유지)로 수정.
  - **검증**: 로컬 스크래치 Postgres에 실제 `server.js`를 기동해 회사 A/B를 API로 실제로 만들고 23개 시나리오(가입/로그인 격리, 동일 loginId 타사 재사용 허용, `GET /data` 스코프, 직원id 스푸핑 저장이 조용히 무시됨·피해자 데이터 원상유지, 위에서 고친 이력 3라우트 교차유출 차단, `GET /status` 회사별 집계, 잠금키 회사간 충돌 없음, `reset-all`이 다른 회사에 영향 없음)을 자동검증 스크립트로 전부 통과 확인. SSE 격리도 별도로 실제 EventSource 연결 2개(회사 A/B)로 확인(A의 저장이 A 스트림에만 `data_updated` 도달, B는 미도달). JSON파일모드(자체호스팅) 회귀도 별도 확인 — 부트스트랩 저장·companyCode 없는 로그인·부트스트랩 이후 인증필수 전환·`/api/companies/register` 명시적 거부 응답 모두 기존과 동일 동작.
  - **이번 세션 범위 밖으로 명시적으로 남겨둔 것(계획 문서에 로드맵으로 문서화, 다음 단계)**: `app_collections`/`app_singletons`(approvalDocs·attendanceRecords·expenseClaims 등 20여개 필드)·회계·ERP·PMS+채용·`annual_snapshots`·`budget.js`는 아직 `company_id` 컬럼 자체가 없어 전역 공유 상태로 남아있음(계획 문서 2~6단계). `advisory lock` 키에 companyId 접두는 ERP/PMS 단계에서 함께 처리 예정. 마스터 관리자(`platform_admins`+`master_audit_log`+impersonation)·회사별 기능 on/off(`company_features`)·앱스토어 IAP 제약(가입/결제는 웹에서만, iOS 앱은 로그인 전용) 설계는 계획 문서에 이미 반영돼 있으나 구현은 1단계(company_id/토큰) 안정화 이후로 명시적으로 미룸.
- 2026-07-21: **멀티테넌트 SaaS 전환 2단계 — 마스터 관리자(플랫폼 운영자) 계층 구현**. 1단계 완료 직후 사용자 요청으로 계획 문서의 "마스터 관리자 + 회사별 기능 커스터마이징" 절 착수. 서버(스키마+API)와 마스터 콘솔(`public/master.html`) 작업을 에이전트 2개로 병행 실행. 서버 에이전트는 세션 한도로 테스트 도중(감사로그 확인까지는 완료, `isFeatureEnabled()`/`actingAsMaster` changedBy 테스트 직전) 중단됐으나 코드 자체는 완성돼 있어, 본 세션이 우선 로컬 Postgres로 나머지 테스트를 마저 수행해 검증 후 커밋했다.
  - **스키마**: `platform_admins`(employees와 완전히 분리, 셀프서브 가입 없음 — 계정은 `scripts/seed-master-admin.js`로 out-of-band 발급하는 재실행 안전한 upsert 스크립트), `master_audit_log`(마스터 권한으로 이뤄진 모든 동작 기록 — 감사 로그 INSERT를 토큰 발급보다 먼저 실행해 로그 기록이 실패하면 토큰도 발급되지 않게 함), `company_features`(company_id+feature_key 복합키, 기본 enabled=true로 하위호환) 3개 테이블 추가.
  - **서버 API**: `POST /master/login`(companyCode 없음, `/login`과 동일한 하드닝이지만 별도 rate limiter로 카운터 분리), `requireMaster()`(role==='master' 토큰만 통과 — 회사 로그인/가입이 발급하는 토큰은 구조적으로 role이 admin/leader/director/member만 될 수 있어 위조로 통과 불가), `GET /master/companies`(전 회사 목록+집계), `POST /master/companies/:id/impersonate`(계획 문서 설계 그대로 새 인가체계를 만들지 않고 signToken()으로 일반 회사 admin 토큰과 동일한 모양의 1시간 단기토큰을 발급해 기존 100개 이상 라우트가 그대로 통과시키게 함, companies.id가 UUID 컬럼이라 형식이 아닌 :id를 그대로 쿼리하면 500이 나가는 것을 실측으로 확인해 정규식 사전검증으로 404 보장), `GET`/`PUT /master/companies/:id/features/:key`(토글 CRUD, PUT마다 감사로그). `POST /save`의 changedBy에도 `actingAsMaster`가 실린 토큰이면 "master:<id> as ..."를 덧붙여 employee_history 등 기존 감사흔적에 impersonation 여부가 남도록 보강. `isFeatureEnabled()` 조회 헬퍼는 신설했으나 기존 100개 이상 라우트에 실제로 배선하는 것은 의도적으로 범위 밖(각 라우트마다 "기능 꺼지면 403" 분기를 새로 넣는 별도의 큰 작업).
  - **마스터 콘솔(`public/master.html`)**: `budget.html`과 동일한 SPA 밖 독립 정적 페이지 패턴. 자체 인증 토큰을 index.html의 `hr_kpi_auth_token`과 절대 겹치지 않는 별도 키(`master_auth_token`)에 저장, 일반 제품 화면과 확실히 구분되는 적/흑 경고 배색+상단 경고 배너. 로그인/회사목록/기능토글(고정 키 목록 없이 자유 입력)/"이 회사로 들어가기" impersonate 버튼 구현.
  - **본 세션이 두 산출물 병합 후 직접 발견·수정한 문제 2건**:
    1. **Impersonation 핸드오프 완성**: master.html이 토큰만 심고 index.html로 이동시켜도, index.html이 부팅 시 항상 무조건 `renderLogin()`을 호출하는 구조라(저장된 토큰만으로 자동 로그인하는 경로가 원래 없음) 로그인 화면이 다시 떴다. impersonate 토큰은 empId가 없어 employees 목록에 대응하는 실제 직원 레코드도 없음. master.html이 화면표시 전용 가상 신원을 세션 전용(sessionStorage) 키로 함께 인계하고, index.html 부팅 시 그 키가 있으면(1회성, 즉시 삭제) `_completeLogin()`을 직접 호출해 로그인 화면을 건너뛰도록 수정 — 단, 이 가상 신원은 절대 `employees` 배열에 넣지 않음(`getFullState()`가 employees를 그대로 자동저장 때마다 서버로 보내는 구조라, 넣으면 그 회사의 실제 직원 테이블에 "master:..." id의 가짜 레코드가 영구히 남게 됨).
    2. **(심각, 마스터 admin 작업과 무관하게 존재하던 기존 버그, 검증 도중 발견)** `index.html`의 `employees` 전역 배열은 오프라인 데모 모드용 하드코딩 샘플 5계정(admin/admin 등 평문 비밀번호 포함, 881번째 줄)을 부팅 시 항상 들고 있는데, `doLogin()`/`submitCompanyRegister()`의 서버 인증 성공 분기가 이 배열을 비우지 않고 "id 일치하면 병합, 아니면 push"만 하고 있었음 — 막 가입한 신규 회사는 관리자 1명뿐이라 `GET /status`의 empCount>1 조건이 성립하지 않아 `loadFromServer()`로 이 오염된 로컬 상태가 진짜 서버 데이터로 교체되는 일도 일어나지 않음. 그 결과 가입 직후 아무 저장(자동저장 포함)이나 일어나면 데모 계정들이 방금 막 가입한 실제 회사의 진짜 직원 테이블에 그대로 생성돼버림(실측 확인: 회사 등록→즉시 저장 1회만으로 가짜 레코드 4건이 평문 비밀번호와 함께 생성됨) — 이 세션에서 구현한 셀프서브 가입 플로우로 만들어지는 사실상 모든 신규 회사가 영향받는 심각한 문제였음. `doLogin()`/`submitCompanyRegister()` 둘 다 서버 인증 성공 시 병합 대신 `employees=[r.employee]`로 완전히 교체하도록 수정(기존 다인원 회사는 곧바로 empCount>1로 `loadFromServer()`가 전체 명부를 마저 받아오므로 문제없음).
    3. **연쇄로 드러난 2차 문제**: 위 버그로 데모 계정들이 `POST /save`의 범용 upsert 경로로 삽입되면서 `next_employee_id` 원자적 카운터(`app_meta`)를 전혀 거치지 않아, 카운터가 실제 데이터보다 뒤처지는 드리프트가 발생 — 이후 정상적인 신규가입/직원등록이 이미 존재하는 id와 충돌해 500(duplicate key)으로 실패하기 시작함을 실측 확인. `_getNextEmployeeId()`가 매 호출마다 카운터를 "현재 값과 MAX(id)+1 중 더 큰 값"으로 자가 복구하도록 수정(절대 낮추지 않음, UPDATE 행 잠금으로 동시 호출도 안전).
  - **검증**: 로컬 스크래치 Postgres에서 마스터 로그인(성공/실패 동일 메시지/rate limit)·회사 admin 토큰의 `/master/*` 403·무토큰 401·회사 목록+집계·impersonate 잘못된 UUID/존재하지않는 회사 404(500 아님)·impersonate 토큰이 기존 `GET /data`·`GET /status`에 그대로 통과·impersonate 토큰 자신은 `/master/*`에 403·impersonate로 저장한 변경이 `employee_history`에 "master:<id> as ..."로 태깅·기능 토글 CRUD 왕복 20개 시나리오 전부 통과. `master_audit_log`에 impersonate/feature_toggle 두 액션 모두 실제 기록됨을 SQL로 직접 확인. Playwright로 마스터 로그인→회사목록→impersonate 클릭→index.html이 로그인 화면 없이 곧장 대시보드로 부팅되는 것, 자동저장 후에도 employees 배열/서버 DB 양쪽에 가상 신원이 없는 것 확인(pageerror 0건). 신규 회사 가입 후 정상 로그인 폼으로 로그인 → employees 배열 1건만 존재·데모 계정 없음 → 자동저장 → 서버 DB에 정확히 1명만 존재함을 SQL로 확인, 연속 회사 가입에서 next_employee_id 충돌 재발하지 않음 확인.
  - **이번 세션도 명시적으로 범위 밖으로 남겨둔 것**: `isFeatureEnabled()`를 기존 라우트에 실제로 배선하는 것(각 라우트마다 "기능 꺼지면 403" 분기 신설은 큰 별도 작업), 회사별 신규 커스텀 기능 실제 구현(현재는 데이터 모델+토글 API만 존재).
- 2026-07-21: **멀티테넌트 SaaS 전환 3단계 — `app_collections`/`app_singletons`(approvalDocs·expenseClaims·attendanceRecords·settings·orgDB 등 나머지 20여개 필드) company_id 스코프화**. 계획 문서 "마이그레이션 순서" 2단계. 에이전트 1개로 구현, 본 세션이 리뷰·병합·독립 실측 검증.
  - **핵심 설계 결정(1단계 employees/kpi_entries와 다른 이유)**: 이 두 테이블의 레코드 id는 `employees.id`(진짜 DB 원자적 시퀀스, `_getNextEmployeeId()`)와 달리, 클라이언트(`public/index.html:756`)의 `let _id=547; const genId=()=>++_id;` — 브라우저 세션마다 547부터 증가하는 평범한 로컬 카운터일 뿐이라 서로 다른 회사가 같은 id(예: 둘 다 "548")를 만드는 게 일상적으로 벌어짐. company_id를 단순 필터 컬럼으로만 추가하고 기존 PK `(collection, id)`/`(key)`를 그대로 두면 두 번째 회사가 생기는 즉시 PK 위반으로 저장이 실패함 — 그래서 company_id를 PK의 일부로 승격: `app_collections`는 `PRIMARY KEY (company_id, collection, id)`, `app_singletons`는 `PRIMARY KEY (company_id, key)`. Postgres가 PK 컬럼에 자동으로 NOT NULL을 강제하므로, 백필(`scripts/migrate-add-company-id.js`에 이 두 테이블도 추가)로 company_id NULL 행이 전부 사라지기 전까지는 schema.sql이 복합 PK 전환을 건너뛰고 기존 단일열 PK를 유지(RAISE NOTICE로 스킵 사유 로그) — 운영 배포 순서(백필 스크립트 먼저 실행 → 새 server.js 배포/재기동)를 스키마 주석과 백필 스크립트 안내 메시지 양쪽에 명시. 백필 스크립트는 백필 직후 schema.sql을 한 번 더 적용해 복합 PK 전환 완료 여부를 그 자리에서 바로 알려줌.
  - **서버 변경**: `loadData(companyId)`가 app_collections/app_singletons SELECT에 `WHERE company_id=$1` 추가. `_persistDataLocked()`의 tie-break 조회(기존 레코드 존재 여부+updatedAt 비교)도 company_id(+레거시 NULL)로 스코프 — 스코프하지 않으면 id가 우연히 같은 다른 회사의 레코드를 "기존 레코드"로 착각해 이 회사의 정당한 저장이 조용히 스킵될 수 있는 미묘한 버그였음(에이전트가 직접 발견·반영). INSERT의 `ON CONFLICT`도 새 복합 PK에 맞춤. tombstone DELETE·singleton INSERT/UPDATE도 동일하게 스코프. company_id는 항상 `req.auth.companyId`(서버가 검증한 토큰)에서만 오므로 employees/kpi_entries처럼 "다른 회사 소유면 건너뛴다"는 별도 방어 코드가 불필요(회사가 다르면 애초에 PK가 달라 서로 겹칠 수 없음).
  - **발견·수정한 실제 취약점 2건**: ① `/restore`의 `deleteExtras` 경로가 `DELETE FROM app_collections WHERE collection=$1 AND id=ANY($2)`에 company_id 조건이 아예 없어, 회사 A의 스냅샷 복원이 우연히 같은 id를 가진 회사 B의 살아있는 레코드까지 삭제할 수 있었음 — employees/kpiEntries 분기와 동일한 `(company_id=$N OR $N IS NULL)` 패턴 적용. ② `/api/accounting/payments`(GET·POST·삭제 3개 라우트, 수금/지급 관리)가 `loadData()`/`_persistDataLocked()`를 거치지 않고 app_collections를 직접 쿼리하고 있어 company_id 스코프 배선 대상에서 완전히 누락 — 회사 B의 admin이 전 회사의 수금/지급 내역(거래처명·금액)을 조회·삭제할 수 있는 상태였음, 이번 세션 앞서 발견한 `/history/*` 유출과 동일한 클래스의 버그. 셋 다 company_id 스코프 추가로 수정.
  - **검증**: 에이전트가 로컬 Postgres 45개 시나리오로 자체 검증(스키마 업그레이드 경로, 두 회사 동일id 충돌, tombstone, protected-merge 필드 등) 완료 후 보고. 본 세션이 병합 후 독립적으로 재검증: 구버전 단일열 PK 상태를 실제로 재현(구 schema.sql 적용→레거시 행 삽입)→새 schema.sql 적용 시 NULL 잔여로 PK 전환이 정확히 스킵됨 확인→백필 스크립트 실행→PK 전환 완료 확인(무손실)까지 업그레이드 경로 전체를 직접 재현. 실제 서버로 16개 시나리오(두 회사가 동일 id "548"로 approvalDocs 저장 시 PK 위반 없이 각자 내용 보존, settings 싱글톤 회사별 독립, tombstone 삭제가 동일id의 타회사 레코드에 무영향, acctPayments 조회·삭제 격리(회사 B가 회사 A의 결제 id를 알아도 삭제 불가)) 전부 통과 확인. JSON파일모드(자체호스팅) 회귀도 별도 확인(company_id 개념 자체가 없는 경로라 영향 없음).
  - **이번 세션도 범위 밖으로 남겨둔 것**: 계획 문서 3~6단계(회계 전용 테이블·ERP·PMS+채용·annual_snapshots+budget.js)는 아직 company_id 컬럼 자체가 없음. `annual_snapshots`는 여전히 `eval_year` 단독 PK라, 두 회사가 같은 연도에 스냅샷을 만들면 서로의 스냅샷 행을 덮어쓰는 가용성 문제가 남아있음(내용 유출은 아님, 6단계에서 해결 예정) — 서버 코드 주석에 명시.
- 2026-07-27: **부가세 신고자료·고정자산 관리 신설 + 사업계획 분석 강화 + UX Quick Win 2건**. 로드맵 미착수 항목(부가세신고자료·고정자산)과 사용자 요청(사업계획 "분석" 기능 미흡, UX Quick Win) 처리를 에이전트 3개로 병행 진행, 세션 한도로 중간에 끊긴 것은 본 세션이 직접 검증·병합.
  - **부가세 신고자료**: `GET /api/accounting/vat-report` — 발행 세금계산서 기준 기간별 매출/매입세액·거래처별/월별 브레이크다운·납부(환급)세액. **고정자산 관리**: `fixed_assets`/`fixed_asset_depreciation_schedule` 테이블 신설, 정액/정률 감가상각 스케줄 자동생성, 연도별 상각전표 발행(RCPS와 동일한 동시발행 방지 락 재사용), 처분 처리, 상각전표 발행 후 취득조건 변경 차단. 회계 메뉴에 화면 추가(RCPS 패턴 재사용).
  - **사업계획 분석 강화**: 비율분석(매출총이익률/영업이익률/순이익률/전년대비 성장률), 손익분기점(BEP) 분석, 시나리오 비교(SVG 차트, 외부 라이브러리 미사용), 실적 대비 비교(업로드된 판관비 실적과 계획 가정 괴리 안내), PUT(수정) 기능을 실제로 프론트에 연결.
  - **UX Quick Win**: 결재 지연 리마인드(결재자 차례 후 설정 일수 이상 대기 시 결재함·알림센터 표시, 기준일수는 결재라인 설정에서 조정 가능), 안전재고 추천발주량(재고 현황 화면), PMS 가동률 색상배지(월 투입률 %를 4단계 색상 배지로 — 계획했던 `/api/pms/allocations`는 실제 화면에서 안 쓰이는 죽은 데이터였음을 발견해 실제 쓰이는 업무일지 합산 기준으로 구현), 연차촉진 알림 자동화(기존 leave-mgmt 페이지에만 있던 배너 조건을 알림센터에도 노출 + 관리자/사업부장용 집계 알림).
  - 전부 레포 밖 임시 디렉토리 사본(서버+node_modules 심볼릭 링크)에서 실제 HTTP 요청/Playwright로 검증, `budget-data.json`(레포에 실제 커밋된 파일) 오염 없음 매번 재확인.
- 2026-07-27: **사업계획 팀→사업부→회사 계층 승인 워크플로우 구현**. 기존 단일 사업계획(관리자 전용 CRUD)을 팀 단위 작성→승인→확정 파이프라인으로 재설계. 요청 확정 내용: (1) 팀 소속 누구나(팀장/사업부장 포함) draft 상태에서 자유 작성/수정, (2) 사업부장(해당 dept의 director) 승인 시 잠금(divisionApproved), (3) 잠긴 뒤 수정은 수정요청→관리자 승인으로만 재오픈(재오픈 시 기존 승인 이력 전부 초기화), (4) 예산담당자(전사 고정 다수)+기획팀장(전사 고정 1명) 둘 다 승인해야 최종확정(finalConfirmed), 관리자는 항상 즉시 확정 가능, (5) 비용전용(`planType:'costOnly'`) 팀은 매출 없이 판관비만으로 계획 작성(매출/원가율 0 기본값, 손익분기점 분석 자동 스킵, 법인세율도 0 기본값), (6) 사업부/회사 단위 롤업 집계(`GET .../business-plan/rollup`, 승인 이상 상태만 기본 반영·includeDraft로 초안 포함 조회 가능).
  - **인가 모델**: 목록/단건 조회는 관리자·예산담당자·기획팀장은 전체, 사업부장은 자기 dept, 팀원/팀장은 자기 dept+team만(사업부장도 자기 팀 계획 편집 가능하도록 `_canEditPlan`에 `_isDivisionHead` 경로 추가). 예산담당자/기획팀장 지정은 관리자만(`POST .../settings/roster`), 입력기간(전사 단일 on/off) 토글은 예산담당자·기획팀장·관리자만.
  - **budget.js가 employees(dept/team) 조회가 필요해진 문제**: budget.js는 자체 파일 저장소(budget-data.json)만 쓰고 employees는 server.js의 전담 영역이라, `budgetRouter`를 모듈 팩토리 함수로 전환(`module.exports = function(deps){...}`)해 `getEmployeeProfile(companyId, empId)` 접근자를 server.js가 주입하는 구조로 변경(`app.use("/api/budget", budgetRouterFactory({ getEmployeeProfile }))`) — `loadData()`가 이미 함수 선언(호이스팅)이라 require 시점보다 이르게 참조해도 실제 호출은 요청 처리 시점이라 안전함을 확인 후 채택.
  - **직접 발견·수정한 동시성 버그(심각)**: 사업계획 쓰기 라우트 5개(생성/수정/사업부장승인/수정요청/삭제)가 `readBudget()`(파일 전체 읽기) 직후 `await getEmployeeProfile()`을 거쳐 `writeBudget()`하는 순서였음 — 이 `await` 구간에 다른 요청이 끼어들어 파일을 먼저 바꾸면, 나중에 완료되는 요청이 그 이전 스냅샷을 그대로 덮어써 다른 사람의 변경사항이 조용히 사라지는 lost-update(이 세션에서 여러 번 반복된 것과 동일한 버그 클래스). JSON 파일 모드는 그 await 구간이 사실상 즉시 끝나(진짜 I/O 없음) 일반 부하로는 재현이 어렵지만, **실제 운영 방식인 Postgres/SaaS 모드는 진짜 네트워크 왕복이라 구간이 넓어져 실제로 유실될 수 있음** — 로컬에서 인위적 지연(setTimeout)을 주입해 수정 전 100% 재현 → 수정 후 미재현을 직접 확인 후 반영(await가 필요한 조회를 전부 readBudget()보다 먼저 끝내 read→mutate→write 구간에 await 지점이 없도록 재정렬).
  - **검증**: 로컬(레포 밖 사본) HTTP 요청 30개 시나리오(권한 경계, 잠금/재오픈, 2단계 승인, costOnly 계산, 롤업 집계, 입력기간 토글) + 동시성 스트레스(20건 동시 생성/수정/승인, 전부 유실 없음) + 엣지케이스 20여개(입력 검증 우회 시도, 상태 순서 건너뛰기, 극단값) + 로컬 실제 Postgres에 회사 2곳을 가입시켜 멀티테넌트 격리 13개 시나리오(목록/조회/삭제/롤업/설정 전부 회사간 유출 없음) 전부 통과. 프론트엔드(`public/budget.html`)는 에이전트 구현 후 별도로 격리 서버+Playwright로 재검증.
  - **QA에서 발견한 UX 버그 수정**: "사업부장 승인"/"최종승인" 버튼이 상태(status)만 보고 노출되어 권한 없는 사용자(해당 사업부장이 아닌 팀원, 예산담당자/기획팀장이 아닌 팀장)에게도 보였음(클릭 시 서버는 항상 정확히 403으로 거부 — 보안 문제는 아니었으나 눌러도 안 되는 버튼이 보이는 혼란스러운 UX). 로그인 토큰에 없는 dept/team을 조회하는 `GET .../business-plan/my-profile` 신설, 프론트가 페이지 로드 시 이를 먼저 가져와 실제 권한이 있을 때만 버튼을 그리도록 수정 — Playwright로 재확인(팀원 계정 버튼 0개, 사업부장 계정 버튼 1개).
  - **메인 사이드바 메뉴 연결**: `budget.html`이 처음부터 메인 SPA(`index.html`) 사이드바 어디에도 연결돼 있지 않아 URL을 직접 입력해야만 접근 가능했던 것을 발견 — "회계" 대분류 아래 "사업계획" 그룹(전 역할 공개) 신설, `gotoPage()`에 예약 id(`biz-plan-external`) 처리 분기를 추가해 SPA 내부 라우팅을 타지 않고 실제 페이지 이동만 하도록 구현. 구현 중 `SIDEBAR_DEFAULT_ORDER`(메뉴 그룹 렌더링 순서를 정하는 별도의 하드코딩 배열)에 새 그룹 키를 추가하지 않으면 `menuGroups`에 데이터가 있어도 실제로는 전혀 렌더링되지 않는 것을 자체 발견해 함께 수정. 데이터 조회 범위(본인 소속만, 예산담당자·기획팀장·관리자는 전체)는 이미 서버 인가 로직이 정확히 그렇게 동작하고 있어 추가 변경 불필요했음.
  - **이번 세션 범위 밖으로 명시적으로 제외**: 인건비 등 판관비 항목을 회계 전표(실적)에 연계해 기준금액을 자동 산정하고 증가분만 입력하는 기능은 사용자가 "추후 업데이트"로 명시적으로 보류.
- 2026-07-28: **이번 세션 개발분 전체 QA(에이전트 4개 병렬)**. 세션 한도로 2개(UX Quick Win, 전자결재서명+멀티테넌트+사이드바)는 완주 못해 본 세션이 직접 이어서 검증.
  - **QA-A(회계: RCPS/원가구분/부가세/고정자산/기존회계/권한)**: 결함 0건. 169개 검증 항목 전부 통과(유효이자율법 상각표, 정액/정률 감가상각, 부가세 집계, 원가명세서 집계 전부 손계산 대조 일치, 동시발행 20건 레이스컨디션 방어 확인, 잠금 후 수정/삭제 차단, 신규 라우트 전부 401/403 정상).
  - **QA-B(사업계획 통합)**: 결함 0건. 메인 SPA→budget.html 전환(4개 역할), 50명 동시 생성, 24건 동시 승인, 예산담당자+기획팀장 2/2 승인 조합 정확성(동시 요청에서도 부분승인이 조기확정 안 됨), 4회 반복 전체 사이클(재오픈 시 승인이력 매번 정확히 초기화), 롤업 숫자 별도 계산 대조 일치, 업로드 기능과 상호 무간섭 전부 확인.
  - **QA-C(UX Quick Win, 세션한도로 본 세션이 직접 진행)**: 결함 0건. PMS 가동률 배지 29/30/99/100/101% 경계값 정확, 결재지연 정확히 2일 경계 및 다단계 결재 시 "1차 승인 시각" 기준 계산(상신일 아님) 확인, 연차촉진알림 10월/잔여5일 경계 4가지 조합 전부 정확, 안전재고 추천발주 경계값(동일/부족/과잉/미설정) 전부 정확.
  - **QA-D 범위(전자결재서명+사이드바, 세션한도로 본 세션이 직접 진행)**: 서명 등록 기능 최초 조사 시 "저장이 서버에 반영 안 됨"으로 보였으나, 원인은 `autoSaveDebounced()`의 의도된 2초 디바운스를 기다리지 않은 테스트 방법론 문제였음(2.8초 대기 후 재확인하니 정상 저장·재조회 확인) — 실제 버그 아님. 사이드바 전체 스모크(관리자 역할, 대분류→중분류→리프메뉴 92개 전부 클릭)는 pageerror 0건, 실패 0건 — 최근 "사업계획" 메뉴 그룹 추가가 기존 메뉴 렌더링에 영향 없음 확인. 실제 로컬 Postgres 멀티테넌트 교차검증(신규 회계 기능들)은 QA-A가 이미 유사 범위를 커버(권한 401/403 전수 확인)했고 별도 세션 한도 소진으로 이번엔 생략.
  - 전부 레포 밖 임시 디렉토리 사본에서 검증, `budget-data.json`/`hr-data.json` 등 레포 커밋 파일 무변경 매번 재확인. 이번 QA 라운드에서는 수정이 필요한 버그가 발견되지 않음(직전 라운드까지 발견된 버그들은 이미 전부 수정·반영된 상태).
- 2026-07-28: **사업계획(팀별 작성·승인·롤업) 메인 SPA 통합 재구현**. 지금까지는 사이드바의 "사업계획" 메뉴가 `budget.html`이라는 완전히 별도의 정적 페이지로 `window.location.href` 이동을 했는데(부가세/고정자산 등 신규 회계 화면들과 달리 SPA 라우팅에 편입돼 있지 않았음), 사용자가 "예산 관리를 웹페이지 내에서 등록할 수 있도록 다시 구현해달라"고 요청 — AskUserQuestion으로 방향(메인 SPA로 통합 vs budget.html 유지+다른 문제만 수정)과 범위(budget.html 전체 vs 사업계획 4~6번 섹션만)를 확인해 "메인 SPA 통합" + "사업계획(4~6번 섹션)만"으로 확정 후 진행(1~3번 섹션인 인원현황/판관상세 업로드·통합요약·원본데이터는 budget.html에 그대로 유지, budget.js 백엔드 API는 변경 없이 그대로 재사용).
  - `public/index.html`에 RCPS 모듈을 구조적 템플릿으로 삼아 `renderBizPlanPage()`(목록/롤업/설정 3탭)·`loadBizPlanFromServer()`·계획 생성·수정 폼·사업부장승인/최종승인/수정요청/수정요청승인·반려/삭제 액션·시나리오 비교(SVG 차트, budget.html의 `svgLineChart`를 이식)·롤업 조회 등 전체 기능을 새 모듈로 작성(기존 전역 헬퍼 `h`/`badge`/`showToast`/`openModal`/`closeModal`/`serverRequest`를 재사용하고 budget.html의 병렬 구현은 이식하지 않음, 신규 상태/함수는 전부 `bizPlan`/`_bp` 접두로 네이밍 충돌 방지).
  - SPA 라우팅 배선: `menuGroups`의 `"bizplan-sep"` 그룹 항목 id를 `biz-plan-external`(gotoPage에서 가로채 `location.href` 이동)에서 일반 페이지 id `biz-plan`으로 변경, `titles`/`PAGE_ROLES`(`admin/director/leader/member` 전체 공개, 기존과 동일)/`pages` 디스패처(`"biz-plan":()=>{loadBizPlanFromServer();}`, RCPS 상세 페이지와 동일 패턴)에 항목 추가, `gotoPage()` 맨 앞의 `biz-plan-external` 특수 분기 제거.
  - `public/budget.html`에서 4~6번 섹션(사업계획 설정/사업계획/롤업 카드)과 대응 JS(`bizPlans`/`bizPlanSettings`/`_myProfile`/`planActionsHtml`/`svgLineChart`/`renderBusinessPlans`/`openBusinessPlanForm`/`submitBusinessPlan`/`compareSelectedPlans`/`loadRollup` 등 전체)를 제거, 안내 카드("사업계획은 메인 화면으로 통합되었습니다")로 대체 — 1~3번 섹션(자료 업로드/통합요약/원본데이터)과 그 JS는 그대로 유지.
  - 검증: 레포 밖 임시 디렉토리 사본에서 `node --check` 수준의 인라인스크립트 문법검사(양쪽 파일 이상없음) 후 Playwright로 (1) admin/director/leader/member 4개 역할에 걸쳐 사업계획 생성(draft, dept/team 자동귀속 확인)→사업부장 아닌 팀장의 승인 시도 403→사업부장 승인(잠금)→잠긴 계획 직접수정 시도 거부→수정요청→관리자 승인(재오픈, 승인이력 초기화 확인)→재수정→재승인→최종승인(예산담당자+기획팀장 겸임 계정)까지 전체 사이클을 URL 이동 없이 SPA 내부에서 수행, (2) costOnly(비용전용) 계획 생성 시 매출 0·BEP 분석 스킵 확인, (3) 롤업 탭 사업부/회사 집계 표시 확인, (4) budget.html 잔여 1~3번 섹션이 admin 토큰으로 정상 동작하고 제거된 섹션 문구가 더 이상 없음을 확인, (5) 기존 92개 사이드바 메뉴 전체 클릭 스모크(pageerror 0건, 실패 0건)로 라우팅 변경이 다른 메뉴에 영향 없음을 재확인. 전 과정 레포 밖 사본에서 진행, `git status`로 레포 파일 무변경 확인.
- 2026-07-28: **사업계획 판관비 항목을 사용자가 제시한 스프레드시트 형태(부문/팀명/비용귀속부문/항목/세부내역(산정근거)/구분/1~12월/계/비고)의 월별 상세 그리드로 확장 + 실적·계획 양쪽에 "전사"와 "조직단위(비용귀속부문)" 집계를 함께 제공**. 사용자가 업로드하던 예산 스프레드시트 스크린샷을 제시하며 "이 형태로 구현해서 최종적으로 이번 세션에 만든 사업계획 기능으로 정리되게 해달라"고 요청 — AskUserQuestion으로 적용 범위(사업계획 작성 폼 자체를 이 형태로 교체할지 / budget.html 업로드 포맷만 확장할지 / 둘 다)와 실데이터 입력 여부를 확인해 "**둘 다 적용 + 실적을 전사·조직단위 양쪽에서 확인 가능하게**" + "**입력 기능만 구현(실데이터는 미입력)**"으로 확정 후 진행.
  - **`budget.js`(백엔드, 하위호환 유지)**: ① `sgaItems`(사업계획 판관비 항목) 스키마를 `{name,baseAmount,growthRate,fixed}`에서 `{dept,team,costDept,name,detail,category,months(12개월 배열|null),note,baseAmount,growthRate,fixed}`로 확장 — `months`가 있으면 그 12개월 합계를 `baseAmount`(그 해 기준연도 금액, 기존 다년도 추정·손익분기점 계산 로직이 그대로 참조하는 필드)로 서버가 계산하고, `months`가 없는(이번 변경 이전에 저장된) 기존 계획은 저장된 `baseAmount`를 그대로 존중해 하위호환. ② `/upload/detail`(budget.html 실적 업로드) 파싱에 "비용 귀속 부문"(선택, 비우면 부문과 동일)·"비고" 컬럼 추가, upsert 고유키에도 비용귀속부문 포함. ③ `/summary`(사업부별 통합요약)에 `?groupBy=dept|costDept` 추가 — 실적을 부문(실제 비용을 쓴 부서) 기준 또는 비용귀속부문(손익 귀속 부서) 기준으로 재집계 가능(전사 합계는 항상 별도 행으로 이미 존재). ④ `/business-plan/rollup` 응답에 `sgaByCostDept`(신설 `_sgaRollupByCostDept()`) 추가 — 계획을 작성한 팀(`byDept`, 기존)과 별개로 판관비 항목이 실제 귀속되는 부문 기준 월별 집계 + 전사 합계를 함께 제공.
  - **`public/index.html`(사업계획 작성 폼)**: 기존 "항목명/기준연도금액/증가율/고정비" 4칸 단순 입력을 스크린샷과 동일한 컬럼 구성의 편집 가능 테이블(`#bp-sga-tbody`)로 교체 — 부문/팀명/비용귀속부문은 계획 소속으로 기본 채워지되(`_bpSgaDefaultDept/Team`) 행마다 재정의 가능(다른 팀이 발생시킨 비용을 이 부문 예산으로 귀속시키는 경우 대응), 세부내역(산정근거)·구분(자유텍스트, 스크린샷의 "무영/판관/반관" 같은 사용자 정의 태그 허용)·1~12월 금액(입력 시 `_bpUpdateSgaRowTotal()`로 계 열 실시간 갱신)·비고, 증가율/고정비는 기존과 동일하게 유지(2년차 이후 추정·손익분기점 분석용). 계획 카드에는 저장된 항목을 동일한 컬럼으로 보여주는 읽기전용 "판관비 상세(기준연도)" 테이블(`_bpSgaDetailTableHtml()`)을 추가. 롤업 탭에는 `sgaByCostDept`를 표로 보여주는 "비용귀속부문별 판관비" 섹션(`_bpSgaByCostDeptHtml()`)을 추가해 전사 합계 행과 조직단위(비용귀속부문)별 행을 함께 노출.
  - **`public/budget.html`(실적 업로드, 1~3번 섹션)**: 업로드 안내 문구에 확장 컬럼 명시, 원본데이터(③) 판관/용역/경상 상세 탭에 비용귀속부문·비고 컬럼 추가, 통합요약(②)에 "부문 기준"/"비용귀속부문 기준" 토글(`setSummaryGroupBy()`)을 추가해 조직단위 보는 기준을 바꿀 수 있게 하고 "전체 합계"는 각 기준에서도 항상 전사 합계로 별도 표시.
  - 검증: 레포 밖 임시 디렉토리 사본에서 양쪽 파일 인라인스크립트 문법검사 통과 후 Playwright로 (1) 팀원 계정이 SPA 내부에서 스크린샷과 동일한 형태(부문/팀명/비용귀속부문/항목/세부내역/구분/월별금액/비고)로 2개 항목(월 6개 24,500,000원 항목 + 월 2개 10,000,000원 항목)을 입력해 저장 → 서버가 계산한 `baseAmount`(월별 합계 147,000,000원/20,000,000원)·손익 판관비(sga=두 항목 합 167,000,000원) 정확성 확인, (2) 그리드 입력 중 월별 금액 실시간 합계(계) 갱신 확인, (3) 사업부장 승인 후 롤업 탭·API 양쪽에서 `sgaByCostDept`의 전사 합계·조직단위(비용귀속부문)별 합계가 정확히 167,000,000원으로 일치함을 확인, (4) 계획 카드에 판관비 상세 테이블이 항목명/세부내역까지 정확히 표시됨을 확인, (5) budget.html에 비용귀속부문·비고 컬럼이 포함된 CSV를 실제 업로드해 원본데이터 탭·요약 탭(부문 기준/비용귀속부문 기준 토글 양쪽)에 정상 반영됨을 확인, (6) 기존 전체 워크플로우 회귀 테스트(작성→사업부장승인→수정요청→재오픈→재승인→최종승인, costOnly 계획)와 92개 사이드바 메뉴 전체 클릭 스모크를 재실행해 회귀 없음 확인(pageerror 0건). 전 과정 레포 밖 사본에서 진행, `git status`로 레포 파일 무변경 확인.
- 2026-07-28: **사업계획 판관비 그리드에 "기본 인건비 자동입력(연봉 기준)" 버튼 추가**. 사용자 요청: "지금 입력된 개인별 연봉을 참고해서 기본 인건비는 그대로 입력될 수 있게 해줘" — 위에서 만든 월별 판관비 그리드에 매번 수동으로 인건비를 입력하지 않아도, HR 데이터(`employees[].salary`, 연봉)에 이미 등록된 재직자 급여를 그대로 반영하도록 함.
  - `public/index.html`의 사업계획 작성 폼(`#bp-sga-tbody`)에 "+ 기본 인건비 자동입력(연봉 기준)" 버튼과 `addBpLaborCostRow()`를 추가 — 계획의 부문/팀(관리자는 상단 `#bp-dept`/`#bp-team` 입력값, 팀원/사업부장은 자동 지정된 소속)을 기준으로 `employees` 중 `active===true`이면서 부문이 일치하는 재직자를 찾아(팀이 지정된 계획은 부문+팀 정확히 일치, 사업부장처럼 팀이 빈 계획은 그 부문 소속 전원 — director는 team이 빈 문자열인 본부 단위 소속이라는 기존 관례와 일치) `salary`(연봉)를 12로 나눈 월급여 합계를 계산, 12개월 전부 동일한 값으로 채운 "기본 인건비" 행을 그리드에 추가(항목명/세부내역/비고에 "N명 연봉 합계 기준 자동입력"·집계 시점을 남겨 추후 인원변동 시 재계산 필요함을 알 수 있게 함). 부문이 아직 지정되지 않은 상태(관리자가 사업부 입력 전)에서 클릭하면 에러 토스트로 안내. 이후 그리드는 다른 행과 동일하게 자유롭게 수정 가능(자동입력은 시작값 제공일 뿐 고정이 아님).
  - 별도 서버 API 불필요 — 사업계획 화면이 메인 SPA(index.html) 안에 있어 이미 로드되어 있는 `employees` 전역 배열을 그대로 참조.
  - 검증: 레포 밖 임시 디렉토리 사본, 팀원(연봉 4,800만/6,000만 2명, 퇴사자 1명과 타 팀 1명은 제외 대상으로 시딩)·사업부장(team 빈 계획, 본부 소속 전원 대상)·관리자(부문 미지정) 3개 계정으로 Playwright 검증 — 팀 단위는 정확히 재직 2명만 집계(월 900만원, 퇴사자·타팀 제외 확인), 사업부장은 team 무관 본부 전체 2명 집계(월 1,100만원), 관리자는 부문 미지정 시 에러 안내, 저장 후 서버 응답의 `baseAmount`가 월 인건비×12와 정확히 일치함을 확인, pageerror 0건.
- 2026-07-28: **사업계획 인건비 자동입력 이중집계 버그 수정 + 표준 판관비 항목 38종 기본 템플릿 추가**. 사용자가 두 가지를 요청: ① "사업부장도 본인의 연봉만 들어갈 수 있도록 해주고" — 팀이 없는(team="") 사업부장 단위 계획에서 인건비 자동입력을 누르면 그 부문 소속 전체 인원의 연봉을 합산하고 있었는데, 각 팀은 이미 자기 팀 계획에서 팀원 인건비를 반영하므로 사업부장 계획까지 부문 전체를 잡으면 롤업(사업부/회사 합계)에서 같은 인건비가 두 번 집계되는 버그였음(직전 세션에서 처음 이 기능을 만들 때 발견하지 못했던 결함) — team이 없는 계획은 부문 전체가 아니라 작성자 본인 재직 레코드(`e.id===currentUser.id`)만 집계하도록 수정. ② 사용자가 첨부한 회사 판관비 계정과목 스프레드시트(급여 8종: 고정급/변동급/인센티브(사이닝)/인센티브(핵심인재)/RSU/임금인상/성과급/기타수당, 복리후생비 11종, 교육훈련비 4종, 지급수수료·지급임차료·건물관리비·보험료·소모품비·도서인쇄비·통신비·협회비·세금과공과·퇴직급여, 사회보험 5종 — 총 38개 항목)를 신규 사업계획 작성 시 기본으로 깔아두도록 `_BP_DEFAULT_SGA_TEMPLATE` 상수 신설, `openBizPlanFormModal()`이 신규 작성(수정이 아닐 때)에는 이 템플릿을 기본 행으로 렌더링(금액은 0으로 시작, 부문/팀은 작성자 소속으로 자동 지정, 자유롭게 추가/삭제 가능 — 기존 "+ 판관비 항목 추가" 버튼 유지). 수정 화면은 기존과 동일하게 저장된 항목만 표시(템플릿 재주입 안 됨). 템플릿 도입으로 "인건비 자동입력" 버튼도 더 이상 별도의 "기본 인건비"라는 임시 항목명을 새로 만들지 않고, 템플릿에 이미 있는 "급여/고정급" 행을 찾아 그 행에 월 금액을 채우도록 통합(중복 항목 방지). 저장 시 월별 금액이 전부 0인 행(사용하지 않는 템플릿 항목)은 자동으로 제외되도록 `submitBizPlanForm()`의 필터 조건도 "이름이 있으면 포함"에서 "실제 금액이 하나라도 있으면 포함"으로 변경 — 그렇지 않으면 손대지 않은 나머지 항목들까지 매번 금액 0인 채로 저장되어 데이터가 불필요하게 부풀어짐.
  - 검증: 레포 밖 임시 디렉토리 사본에서 Playwright로 (1) 신규 작성 모달에 정확히 38개 템플릿 행이 첫 행부터(고정급/급여) 순서대로 생성되고 부문/팀이 작성자 소속으로 채워짐, 금액은 전부 0으로 시작함을 확인, (2) 인건비 자동입력 클릭 시 새 행이 추가되지 않고 기존 "고정급" 행 1개만 재직자 연봉 기준으로 채워짐(행 총개수 38 유지) 확인, (3) 템플릿 중 통신비 1개만 값을 채워 저장 → 서버에는 값이 있는 고정급·통신비 2건만 저장되고 나머지 36개 빈 템플릿 항목은 제외됨을 확인, (4) 사업부장 계정으로 인건비 자동입력 시 본인 1명 급여만 반영(부문 전체 아님, 이전 버그였던 2명 집계가 1명으로 수정됨)을 확인, (5) 저장된 계획을 다시 열면 템플릿이 재주입되지 않고 저장된 항목(2건)만 표시됨을 확인, (6) 92개 사이드바 메뉴 전체 클릭 스모크와 기존 사업계획 전체 워크플로우(작성→사업부장승인→수정요청→재오픈→재승인→최종승인, costOnly) 회귀 재실행으로 회귀 없음 확인(pageerror 0건, 기존에 이미 알려진 의도된 403 2건 제외).
- 2026-07-28: **사업계획 화면 전면 개편(사용자 요청 7건 일괄 처리)** — ① 팝업(모달) 작성 폼을 메인 화면(#content) 안에 그대로 그리는 풀페이지 폼으로 전환, ② 사이드바에서 "사업계획"을 "회계" 대분류 하위에서 분리해 독립된 최상위 탭으로 승격, ③ 고정급 자동입력은 기존 기능 유지(수정 가능, 변동급 등 나머지는 실제 데이터 소스가 없어 수동 입력 유지 — 사용자 확인 받음), ④ "적용월"+"적용 금액" 입력을 추가해 전체(1~12월 매달 반복) 또는 특정 월 하나를 선택해 해당 월(들)에 금액을 자동 반영하고 이후에도 각 월 칸을 자유롭게 수정 가능(사용자가 "둘 다 지원" 선택), ⑤⑥⑦ 항목 외에 "계정과목"(판관/용역/경상 셀렉트, budget.js의 실적 업로드 CATEGORIES와 동일 값 체계)과 "비용계정"(전표 입력 시 쓰는 실제 회계 계정과목 중 비용 계정을 검색해 선택) 2개 필드 신규 추가, 부문/사업부/팀 필드를 자유 텍스트에서 조직관리(orgDB)에 등록된 값을 검색해 고르거나 새 값을 직접 입력할 수 있는 `<input list>`+`<datalist>` 방식으로 전환.
  - **사이드바**(`public/index.html`): `BIG_CATEGORIES`의 `acct` 항목 groups에서 `"bizplan-sep"` 제거, 신규 `{key:"bizplan",label:"사업계획",icon:"🧮",groups:["bizplan-sep"]}` 항목 추가 — `menuGroups` 자체(중분류 정의)는 변경 없이 대분류 소속만 바뀌므로 회귀 위험이 낮음(카테고리→그룹 매핑 헬퍼들이 전부 `BIG_CATEGORIES`에서 파생되는 구조라 이 한 곳만 고치면 됨).
  - **풀페이지 폼**: `_bizPlanTab`에 `"form"` 상태 추가, `renderBizPlanPage()`가 `_bizPlanTab==="form"`이면 탭 바 대신 "← 목록으로" 버튼과 함께 `_renderBizPlanFormBody()`를 `#content`에 직접 렌더링(모달 오버레이 없음). `openBizPlanFormModal(planId)`(`openModal()` 호출)를 `openBizPlanForm(planId)`(비용계정 목록을 미리 로드한 뒤 `_bizPlanTab="form"`으로 전환 후 재렌더)로 교체, `submitBizPlanForm()`의 저장 완료 처리도 `closeModal()` 대신 `_bizPlanTab="list"` 전환으로 변경. 목록의 "+ 신규 작성"/"수정" 버튼 onclick도 새 함수명으로 갱신.
  - **비용계정 데이터 접근 문제 발견·해결**: 사업계획은 전 역할(admin/director/leader/member) 공개인데, 회계 모듈의 계정과목 조회(`GET /api/accounting/accounts`)는 이 세션 초반 보안 감사에서 이미 admin 전용으로 굳혀놓은 라우트라 그대로 재사용하면 일반 직원은 비용계정을 전혀 선택할 수 없게 됨. 기존 admin 전용 엔드포인트를 완화하는 대신(그동안 여러 차례 힘들게 좁혀온 인가 경계를 되돌리는 방식은 피함), `server.js`에 `GET /api/accounting/accounts/expense-lite` 신규 엔드포인트를 추가 — 인증만 있으면(role 무관) 비용(`type==="expense"`) 계정의 코드·이름만 최소 반환하고, 원가구분·변경이력 등 다른 민감 정보는 노출하지 않음.
  - **비용계정/부문·팀 검색선택 UI**: `_bpLoadExpenseAccounts()`(위 신규 API 호출, 1회 캐시), `_bpOrgDeptTeamDatalistsHtml()`(`orgDB.depts`/`orgDB.teams` 전체를 datalist로 변환 — 팀은 부서별 필터링 없이 전사 팀을 한 목록으로 평탄화해 단순하게 구현), `_bpExpenseAccountDatalistHtml()`을 신설해 폼 렌더링 시 한 번 삽입하고, 부문(`.bp-sga-dept`/`.bp-sga-costdept`/`#bp-dept`)·팀(`.bp-sga-team`/`#bp-team`)·비용계정(`.bp-sga-expenseaccount`) 입력에 각각 `list=` 속성으로 연결(`<datalist>` 목록에 없는 값도 자유 입력 가능, 완전히 새로운 부문/팀 대응).
  - **계정과목/비용계정 필드**: `_bpSgaRowHtml()`에 `<select class="bp-sga-accttype">`(옵션: -/판관/용역/경상, 신규 템플릿 행 기본값 "판관")과 `<input class="bp-sga-expenseaccount" list="bp-expense-account-list">` 추가. 기존 "구분"(자유텍스트, 템플릿의 급여/복리후생비/교육훈련비 등 소분류 라벨 그대로 유지)과는 별개 필드 — "계정과목"은 budget.js `CATEGORIES`(판관/용역/경상)와 동일한 값 체계라 향후 업로드된 실적과의 정밀 비교에 활용 가능. `submitBizPlanForm()`이 두 필드를 sgaItems 페이로드에 포함, `budget.js`의 `_normalizeBusinessPlanInput()`에 `accountType`/`expenseAccount` 필드를 그대로 통과시키도록 추가(자유 텍스트라 서버측 강한 검증은 하지 않음 — 계획서 참고용이지 전표 발행에 직접 쓰이는 값이 아님).
  - **적용월/적용 금액**: 각 행에 `<select class="bp-sga-applymonth">`(전체/1~12월)와 `<input class="bp-sga-applyamount">`를 추가, `_bpApplyMonthFill(el)`이 금액 입력(oninput)/월 선택(onchange) 시 "전체"면 12개월 전부, 특정월이면 그 달만 해당 월 칸에 값을 채우고 `_bpUpdateSgaRowTotal()`로 계를 재계산. 이 두 입력은 저장되지 않는 빠른입력 보조 필드일 뿐이고 실제 저장값은 항상 12개 월별 칸의 현재 값이라, 자동 반영 후에도 각 월 칸을 자유롭게 직접 수정할 수 있다(사용자가 "적용월부터 반복"과 "특정월만" 둘 다 지원해달라고 확정한 요구사항을 그대로 구현).
  - **읽기전용 표시 갱신**: 계획 카드의 판관비 상세 테이블(`_bpSgaDetailTableHtml`)에도 계정과목·비용계정 컬럼 추가.
  - 검증: 레포 밖 임시 디렉토리 사본에서 Playwright로 (1) `BIG_CATEGORIES`에서 회계/사업계획이 완전히 분리된 별도 대분류임을 확인, (2) "+ 신규 작성" 클릭 시 모달 오버레이 없이 `#content` 안에 폼이 직접 렌더링되고 "← 목록으로" 버튼이 있음을 확인, (3) 부문/팀 datalist에 조직관리 등록값이 채워지고 행의 부문/팀명/비용귀속부문 입력이 연결됨을 확인, (4) 계정과목 셀렉트 옵션이 정확히 -/판관/용역/경상이고 신규 템플릿 행 기본값이 "판관"임을 확인, (5) 비용계정 datalist가 실제 회계 계정과목(seed 데이터 기준 29개 비용계정)으로 채워지고 입력이 연결됨을 확인, (6) 적용월="전체"+금액 300,000원 입력 시 12개월 전부 300,000원으로 채워지고 계가 3,600,000원으로 갱신됨을 확인, (7) 적용월="3월"+금액 1,000,000원 입력 시 3월 칸만 채워지고 나머지 달은 0으로 유지됨을 확인, (8) 자동 반영 후에도 4월 칸을 직접 200,000원으로 수정할 수 있음을 확인, (9) 고정급 자동입력(연봉 기준)이 여전히 정상 동작함을 확인, (10) 저장 시 계정과목/비용계정/적용월로 채운 월별 값이 서버에 정확히 반영됨(통신비 baseAmount 3,600,000원, accountType "판관", expenseAccount "514 통신비", 협회비 [3월:1,000,000, 4월:200,000] 정확 일치)을 확인, (11) 저장 후 자동으로 목록 탭(팝업 close 아님)으로 복귀함을 확인, (12) 저장된 계획을 다시 열면(수정) 계정과목/비용계정 값이 정확히 복원되고 템플릿이 재주입되지 않고 저장된 3개 항목만 표시됨을 확인, (13) "취소" 클릭 시 목록으로 정상 복귀함을 확인, (14) 92개 사이드바 메뉴 전체 클릭 스모크와 기존 사업계획 전체 워크플로우(작성→사업부장승인→수정요청→재오픈→재승인→최종승인, costOnly, 롤업) API 기반 회귀 재실행으로 회귀 없음 확인(pageerror 0건, 기존에 이미 알려진 의도된 403 2건 제외 — 회귀 테스트 스크립트의 "사이드바에 사업계획이 회계 하위에 노출"이라는 옛 단언 1건만 이번 변경 의도대로 실패, 실제 결함 아님).
- 2026-07-28: **사업계획 전면개편 직후 종합 QA — 사용자 요청으로 "사업계획 전반의 기능 제약·오류, 영업 파이프라인 등 실적 분석 연계까지" + "다중 사용자 동시접속"을 에이전트 3개(기능 정확성/영업 파이프라인 연계/동시성 스트레스)로 병행 검증**. 세션 사용량 한도로 1차 실행이 3개 전부 즉시 실패해 재시도 후 완주(리셋 대기 대신 사용자가 즉시 재시도를 요청해 바로 재실행). 실제 버그 3건 발견·수정, 설계상 명확한 기능 격차 1건 확인(수정 안 함), 동시성 버그는 0건.
  - **버그 1(심각) — 영업 실적 대시보드 "목표 대비 실적" 이중집계**(`public/index.html:16486` 부근, `_renderSalesDashTab`): 견적서 상태는 `draft→sent→accepted→shipped`(또는 `rejected`) 중 하나만 유지되는데, 실적 계산이 `orderActual`(accepted∥shipped 필터)과 `revenueActual`(shipped만 필터)을 각각 합산한 �적 **더해서** 표시하고 있어, 출고완료(shipped)된 건은 두 필터에 전부 걸려 금액이 두 번 집계됨(달성률이 실제보다 부풀려짐 — 실측: 200만원 실적을 300만원/120%로 표시). 중복 없는 단일 필터(`status==="accepted"||"shipped"`)만 쓰도록 수정.
  - **버그 2(심각) — 사업계획 "실적 대비 비교"(`computeBudgetComparison`, `budget.js:180-194`)가 팀 구분 없이 전사 실적과 비교**: 이 함수는 2026-07-27 초반(사업계획이 "회사 전체 단일 계획"이던 시절)에 작성된 뒤, 같은 날 나중에 팀별 계획(`plan.dept`/`plan.team`) 워크플로우가 도입됐는데도 갱신되지 않아, 어느 팀의 계획을 봐도 항상 전사 판관비 실적 합계와 비교하고 있었음(실측: 예산을 실제로 절감한 팀도 "실적이 계획을 크게 초과"로 잘못 표시됨). `plan.dept`/`plan.team` 기준으로 실적 데이터를 필터링하도록 수정(팀 없는 사업부장 단위 계획은 그 부문 전체와 비교, dept 자체가 없는 레거시 전사 계획은 기존과 동일하게 전사 비교 유지 — 하위호환).
  - **버그 3 — 사업계획 목록의 "수정/삭제/수정요청" 버튼이 권한 없는 사용자에게도 노출**(`public/index.html:15077` 부근 `_bpPlanActionsHtml`): 오늘 초반 라운드에서 "사업부장 승인" 버튼에는 이미 `_bpIsDivisionHeadOf`/`_bpIsOwnerOrLead`로 사전 권한 판단이 적용돼 있었는데, 같은 종류의 문제가 "수정/삭제/수정요청" 3개 버튼에는 빠져 있었음 — 예산담당자·기획팀장처럼 전체 조회 권한은 있지만 특정 팀 계획의 수정 권한은 없는 사용자가 남의 팀 계획을 볼 때도 이 버튼들이 보여, 눌러야 비로소 403으로 거부되는 혼란스러운 UX였음(서버 인가 자체는 항상 정확했음 — 보안 문제는 아니고 UX 문제). 서버의 `_canEditPlan()`과 동일한 기준(관리자/같은 dept+team/그 dept의 사업부장)을 클라이언트에도 반영한 `_bpCanEditClient()` 신설, 3개 버튼에 적용.
  - **명확한 기능 격차로만 기록(수정 안 함)**: 사업계획의 매출 가정(`baseRevenue`/`revenueGrowthRate`)을 실제 매출 실적(영업 파이프라인의 견적/수주/출고 데이터)과 비교하는 기능이 전체 코드베이스에 전혀 없음 — `computeBudgetComparison()`은 판관비(SG&A)만 비교하도록 설계돼 있어 매출 계획 대비 실적을 시스템에서 확인할 방법이 없음. 매출 비교를 추가하려면 "어떤 데이터를 매출 실적으로 볼지"·"계획의 dept/team을 견적서의 어떤 필드와 매칭할지"(견적서엔 dept/team 개념 자체가 없음) 등 설계 결정이 필요해 범위 밖으로 남김.
  - **다중 사용자 동시접속 스트레스 테스트(별도 에이전트, JSON 파일 모드 + 실제 로컬 PostgreSQL 양쪽) — 실제 버그 0건**: 사업계획 동시 생성(20건)·동시 사업부장 승인(20건, 5개 사업부)·동시 최종승인(예산담당자 3명+기획팀장 1명이 같은 계획에 동시 승인, 두 서명 모두 유실 없이 기록되고 조기 확정 없음)·동시 수정요청/승인(20건)·비용계정 조회 API 50개 동시 요청(멀티테넌트 격리 포함)·롤업 API 동시 부하 전부 유실·손상·크래시 없음. `budget.js`의 "await가 필요한 조회를 readBudget() 이전에 전부 끝내고 그 사이 다른 await 없이 동기적으로 read→write"하는 기존 설계가 실제로 완전히 경합 없는(race-free) 구조임을 실측으로 재확인(Postgres 모드처럼 `getEmployeeProfile()`에 진짜 네트워크 지연이 있어도 결과는 동일 — budget.js 자신의 파일 I/O가 항상 완전히 동기적이라 그 사이에 다른 요청이 끼어들 틈이 구조적으로 없음). 같은 draft 계획을 여러 명이 동시 수정하는 경우는 손상 없는 last-write-wins로 확인(설계상 특성이지 버그 아님, 유실된 쪽에 안내가 없다는 점만 UX 참고사항으로 기록).
  - 3개 에이전트 모두 격리된 레포 밖 사본에서 검증(실제 로컬 PostgreSQL 포함). 수정 후 본 세션이 직접 최종 diff 검토(budget.js 7줄, index.html 27줄, 3개 독립 수정 확인) + 92개 사이드바 메뉴 스모크 + 기존 사업계획 전체 워크플로우 회귀 + 오늘 발견된 버튼가시성/실적비교 팀스코프 수정 2건을 별도 스크립트로 재검증 — 전부 통과.
- 2026-07-28: **사업계획 인건비 자동입력에 팀장 고정급/변동급 분리 반영**. 사용자 요청: "연봉에서 고정급과 변동급의 차이는 팀장만 해당되고, 팀장은 연봉에서 600만원을 변동급으로 하고 나머지는 연봉 전체가 고정급으로 넣어서 계산되도록 해줘". 팀장 판정은 `role`(시스템 권한)이 아니라 급여 화면의 직책수당 산정에 이미 쓰이는 `emp.position==="팀장"` 필드와 동일 기준으로 통일(우연히 그 직책수당도 월 50만원으로 이번 변동급 월 환산액과 같은 크기라 기존 급여 체계와도 정합적임을 확인). `addBpLaborCostRow()`를 재작성 — 매칭된 재직자를 순회하며 `position==="팀장"`이면 연봉 중 600만원(연)을 변동급 합계에, 나머지(salary-600만원)를 고정급 합계에 더하고, 팀장이 아니면 연봉 전액을 고정급 합계에 더함. 두 합계를 각각 12로 나눠 "고정급"/"변동급" 행(둘 다 표준 템플릿에 기본 포함)에 채우되, 변동급은 대상 팀에 팀장이 1명 이상 있을 때만 채우고(없으면 기존 값을 그대로 두어 "변동급은 수동 입력" 원칙 유지), 채운 뒤에도 각 행은 그대로 직접 수정 가능.
  - 검증: 레포 밖 임시 디렉토리 사본, 팀장 1명(연봉 6,000만)+팀원 2명(4,800만/3,600만)으로 구성된 팀과 팀장이 없는 팀(팀원 1명, 4,200만) 두 케이스를 Playwright로 검증 — 팀장 포함 팀은 고정급 월 11,500,000원((4,800만+(6,000만-600만)+3,600만)/12)·변동급 월 500,000원(600만/12)으로 정확히 분리되고 토스트 안내에 "팀장 1명 포함"이 표시됨, 두 항목 합계가 팀 전체 연봉 합계(1억4,400만)와 정확히 일치(누락/중복 없음)함을 확인. 팀장이 없는 팀은 고정급만 연봉 전액 반영되고 변동급은 0으로 그대로 유지됨(토스트에 "팀장" 언급 없음)을 확인. 저장 후 서버 응답의 baseAmount가 계산값과 정확히 일치함을 확인. 92개 사이드바 메뉴 스모크 재실행으로 회귀 없음 확인(pageerror 0건).
- 2026-07-28: **사업계획 롤업에 "3단계 자료 연계"(개인별 급여 → 팀별 월별 그리드 → 부문별 집계표) 명시적 반영**. 사용자가 실제 사용 중인 3개 스프레드시트 이미지(① 개인별 급여상세, ② 팀명/비용귀속/항목/세부내역/구분/월별금액 그리드, ③ 부문별 월별 인원현황+구분별(급여/인센티브/복리후생비/교육훈련비/사회보험/퇴직급여) 집계표)를 제시하며 "①→②→③ 순서로 자료가 추출되도록" 요청 — 명확화 질문에 답이 없어 이미지 구조를 근거로 기존 구현에 그대로 매핑해 진행: ①은 이미 존재하는 인사 데이터(연봉·직책)+`addBpLaborCostRow()`의 고정급/변동급 자동입력, ②는 이미 존재하는 사업계획 판관비 그리드(부문/팀명/비용귀속부문/항목/세부내역/구분/1~12월, 이미지2와 완전히 동일한 컬럼 구성), ③만 신규 구현.
  - **`budget.js`**: `_sgaRollupByCategory(plans)` 신설 — 판관비 항목을 "구분"(category, `_BP_DEFAULT_SGA_TEMPLATE`이 이미 각 항목에 붙여둔 표준 분류: 급여/복리후생비/교육훈련비/지급수수료/사회보험/퇴직급여 등)별로 나눈 뒤 그 안에서 다시 비용귀속부문(costDept) 기준으로 재집계(전사 합계 포함) — 항목을 입력하는 순간(②) 별도 매핑 없이 이 집계(③)에 자동 반영됨. `GET /business-plan/rollup` 응답에 `sgaByCategory` 배열로 추가.
  - **`public/index.html`**: 롤업 탭에 "📊 부문별 월별 집계표(3단계 자료 연계)" 섹션 신설 — `_bpHeadcountByDeptHtml()`(①단계 확인용, `employees[]`의 재직자를 부문별로 집계한 인원 현황 스냅샷 — 월별 이력을 저장하는 데이터가 없어 "현재 시점" 기준임을 라벨에 명시), `_bpSgaByCategoryHtml()`(신설, `sgaByCategory`를 이미지3과 동일한 형태로 구분별 표 여러 개를 연속 렌더링, 각 표는 조직단위(비용귀속부문)별 행 + 전사 합계 행). 기존 `_bpSgaByCostDeptHtml`(비용귀속부문 단일 통합표)은 그대로 유지, 그 아래에 구분별 세분 표들을 추가로 배치.
  - RSU·성과급·복지카드 등 인사 데이터에 실제 값이 없는 항목은 여전히 ②단계에서 수동 입력이 필요하며, 입력된 값만 ③단계 집계에 반영됨(①→②가 자동인 것은 고정급/변동급뿐 — 기존에 이미 확정된 범위).
  - 검증: 레포 밖 임시 디렉토리 사본, 팀장 포함 IT팀(경영지원본부, 고정급+변동급 자동입력+통신비 수동입력)과 팀장 없는 영업1팀(영업본부, 고정급만 자동입력+소모품비 수동입력) 두 계획을 사업부장 승인까지 진행한 뒤 Playwright로 롤업 탭 확인 — "사업부문별 인원 현황"·"급여(기준연도)"·"통신비(기준연도)"·"소모품비(기준연도)" 표가 모두 노출되고, "급여" 구분 표의 경영지원본부 1월 값이 9,000,000원(IT팀 고정급+변동급 월합), 영업본부 1월 값이 3,500,000원(영업1팀 고정급), 전사 합계가 12,500,000원으로 정확히 일치, "통신비" 구분 전사 합계가 연 1,200,000원으로 정확히 일치함을 API 응답 직접 검증으로 확인. 92개 사이드바 메뉴 스모크 + 기존 사업계획 전체 워크플로우 회귀 재실행으로 회귀 없음 확인(pageerror 0건).
- 2026-07-29: **3단계 자료 연계의 1단계(개인별 급여 상세) 실제 입력 화면 신설 + 사업계획 그리드 자동입력 확장**. 어제 세션에서 "3단계 자료 연계를 반영해달라"는 요청에 임의로 "1단계=기존 employees[].salary"로 매핑해 진행했었는데, 오늘 사용자가 명확히 답변: ① 1단계는 **별도 입력화면이 필요**(RSU·인센티브·복지카드 등 지금 인사 데이터에 없는 세부 항목까지), ② 2·3단계(팀별 그리드→부문별 집계표)는 어제 만든 것을 **그대로 확장**, ③ 이 전체는 **사업계획(예측) 쪽**이고 **실적은 별도 메뉴**(budget.html의 판관/용역/경상 상세 업로드가 이미 사업계획과 분리된 메뉴로 이 역할을 하고 있어 별도 신설은 보류, 부족하면 후속 요청 예정).
  - **`budget.js`**: `_emptyCompanyBudget()`에 `empPayPlans:[]` 필드 추가(직원별·연도별 항목 배열, `{id,empId,empName,year,items:[{category,name,amount}],createdAt,updatedAt}`). 신규 라우트 3개 — `GET /emp-pay-plan?year=`(관리자 전용, 회사 전체 목록), `POST /emp-pay-plan`(관리자 전용, empId+year 기준 upsert, 항목명+금액이 있는 것만 저장), `DELETE /emp-pay-plan/:id`(관리자 전용). `DELETE /data`(예산 데이터 초기화)가 businessPlans/budgetPlanSettings와 마찬가지로 `empPayPlans`도 보존하도록 수정(파일 업로드 데이터가 아니라 화면 직접 입력 데이터이므로 초기화 대상에서 제외).
  - **인가 설계**: 개인별 급여 상세는 급여만큼 민감해 회사 전체 목록 조회(`GET /emp-pay-plan`)는 관리자 전용으로 유지하면서도, 사업계획은 전 역할이 쓰는 기능이라 팀원이 "내 팀 인건비 자동입력"을 하려면 이 데이터에 접근할 방법이 필요했음 — `GET /emp-pay-plan/by-ids?year=&ids=`(인증만 있으면 누구나) 신설, 요청자가 명시한 empId 목록에 한해서만 항목을 반환(클라이언트가 이미 들고 있는 `employees[]`로 "내 팀 소속 id"를 스스로 판단해 그 id들만 조회 — 회사 전체를 한 번에 열람하는 것은 여전히 불가능).
  - **`public/index.html`**: 신규 페이지 `emp-pay-plan`(관리자 전용, PAGE_ROLES/pages 디스패처/`bizplan-sep` 메뉴그룹에 등록) — 직원 검색선택+연도 입력 후 `_BP_DEFAULT_SGA_TEMPLATE`의 36개 항목(고정급/변동급은 연봉 기반 자동계산 대상이라 제외)에 대해 연간 금액을 입력하는 그리드, 이미 입력된 직원 목록(항목수·연간합계) 표시. 사업계획 작성 폼에 "+ 개인별 급여상세 자동입력" 버튼(`addBpEmpDetailRows`) 신설 — 팀(또는 사업부장 단위는 본인만, `addBpLaborCostRow`와 동일한 매칭 기준) 소속 인원의 `emp-pay-plan/by-ids` 조회 결과를 항목명 기준으로 합산해(고정급/변동급은 이중반영 방지를 위해 제외) 판관비 그리드의 해당 행(이미 있으면 채움, 없으면 신규 추가)에 월 균등 분배로 채움 — 채운 뒤에도 직접 수정 가능.
  - 검증: 레포 밖 임시 디렉토리 사본, 관리자가 IT팀 팀원 2명에게 각각 RSU(1,200만원)·성과급(600만원, 1명만)을 입력 → 일반 팀원 계정이 자기 팀 사업계획에서 "개인별 급여상세 자동입력" 클릭 시 RSU 행(2명 합산 월 1,666,667원)·성과급 행(1명분 월 500,000원)이 정확히 채워짐을 확인, 저장·사업부장 승인 후 3단계 롤업의 "급여" 구분 집계(RSU+성과급 표준 템플릿상 같은 "급여" 구분에 속함) 전사 합계가 정확함을 확인(월별 반올림 누적 오차 상한 12원 이내). 일반 팀원의 회사 전체 목록 조회(`GET /emp-pay-plan`) 시도는 403으로 차단됨을 확인(관리자 전용 유지). 93개 사이드바 메뉴 전체 클릭 스모크(신규 메뉴 포함, 기존 92개+1)와 기존 사업계획 전체 워크플로우 회귀 재실행으로 회귀 없음 확인(pageerror 0건).
- 2026-07-29: **비용계정 기본 등록 + 개인별 급여상세 자동반영·엑셀업로드·일괄반영 + 퇴직급여(DC/DB)·4대보험 자동계산 + 사업부별 롤업 필터**. 사용자가 한 번에 요청한 7개 항목을 순차 구현.
  - **계정과목 기본 등록**(비용계정 검색이 비어있던 기존 회사 대응): `server.js`에 `POST /api/accounting/accounts/seed-defaults`(관리자 전용) 신설 — 기존 `DEFAULT_ACCOUNTS`(신규 회사에만 자동 시딩되던 급여/복리후생비/여비교통비 등) 중 이미 있는 `code`는 건너뛰고 없는 것만 추가(JSON/Postgres 양쪽 대응). `renderAcctAccountsPage()`에 "🌱 기본 계정과목 일괄등록" 버튼 추가.
  - **개인별 급여상세 자동반영**: `addBpEmpDetailRows()`에 `silent` 매개변수를 추가해, 사업계획 신규 작성 폼이 렌더링될 때(부문이 이미 정해져 있는 팀원/사업부장 한정) 버튼 클릭 없이 자동 호출되도록 `renderBizPlanPage()`에 배선(관리자는 부문을 직접 입력해야 하므로 미대상, 기존 수동 버튼도 그대로 유지).
  - **사업부별 롤업 확인 화면**: 새 페이지 대신 기존 롤업 탭에 부문 선택 드롭다운(`_bizPlanRollupDeptFilter`)을 추가해 인원현황·비용귀속부문별 집계·구분별 집계표 3곳 모두를 선택한 부문 기준으로 필터링(전사 합계 행은 필터와 무관하게 항상 유지).
  - **개인별 급여상세 엑셀 업로드**: `uploadEmpPayPlanExcel()`/`_eppParseAndUpload()` 신설(SheetJS를 기존 `handleXlsxUpload()`와 동일하게 CDN에서 동적 로드) — "사번"(우선) 또는 "이름"으로 직원 매칭, "연도" 열이 없으면 화면에서 선택한 연도 적용, 나머지 열은 `_BP_DEFAULT_SGA_TEMPLATE` 항목명과 일치하는 것만 금액으로 인식해 `POST /emp-pay-plan`으로 업로드(같은 직원·연도는 그 행 내용으로 전체 교체).
  - **전 직원 일괄 반영 + 개별 수정**: `bulkApplyEmpPayPlanItem()` 신설 — 선택한 항목·금액을 재직자 전원에게 반영(기존 items는 유지하고 해당 항목명만 upsert), 반영 후에도 화면에서 직원을 선택해 개별 금액을 자유롭게 수정 가능(별도 "일괄 반영 항목" 표시 없이 그냥 일반 입력값이 되므로 수정에 제약 없음).
  - **퇴직급여(DC/DB)·4대보험 자동계산**: `budget.js`의 `_emptyCompanyBudget()`에 `empPayPlanSettings`(회사별 요율 설정: `severance.dcRate`/`severance.dbMonthsPerYear`, `socialInsurance.{pension,health,longTermCare,employment,localTax}`) 추가, 신규 라우트 `GET/POST /emp-pay-plan/settings`(관리자 전용). 특정 연도의 실제 고시요율을 하드코딩하지 않고(회사마다·연도마다 달라 잘못된 확정값을 제시하는 위험을 피하기 위해) 관리자가 직접 입력·저장하는 방식을 택하고 화면에 "반드시 해당 연도 고시요율·회사 내규를 확인 후 조정하라"는 경고 문구를 표시. `empPayPlans` 엔트리에 `severanceType`('DC'|'DB')·`severanceMultiplier`(개인별 배수, 기본 1)·`severanceBaseline`(전기말 기준 충당액, 기본 0) 필드 추가. 프론트에 `autoCalcEmpPayPlan()` 신설 — DC는 `연봉×DC적립률×배수`, DB는 `(연봉/12×근속연수(계획연도말 기준, hire일부터 일수/365)×DB개월수×배수) − 전기말충당액`으로 증가분만 계산, 4대보험은 `연봉×해당요율`(장기요양은 건강보험료×장기요양요율)로 계산해 "퇴직금"/"국민연금"/"건강보험"/"장기요양"/"고용보험"/"주민세" 6개 항목 입력칸에 채움 — 이 입력칸들은 계산 후에도 평범한 `<input>`이라 저장 전 자유롭게 수동 수정 가능(요구사항 그대로).
  - 검증: 레포 밖 임시 디렉토리 사본, DC 직원(연봉 6천만원, 적립률 10%)·DB 직원(연봉 4,800만원, 입사 2020-07-01, 계획연도 2026년말 기준 근속)으로 계산값을 손계산과 대조해 전부 일치 확인(퇴직금·국민연금·건강보험·장기요양·고용보험·주민세 6개 항목 전부), 계산 후 값을 직접 수정해도 저장 시 수정한 값 그대로 반영됨을 확인, 요율 저장 후 재조회(새로고침)해도 유지됨을 확인(서버 영속). `GET/POST /emp-pay-plan/settings`가 일반 팀원에게 403임을, 기존 `by-ids`(비관리자 접근 가능)는 계속 200임을 확인해 신규 라우트가 기존 인가 경계를 깨지 않았음을 검증. 93개 사이드바 메뉴 전체 클릭 스모크(pageerror 0건)와 3단계 자료연계 롤업 회귀(IT팀/영업1팀 급여·통신비 구분 집계 정확) 재실행으로 회귀 없음 확인.
- 2026-07-29: **"건강검진 일부 인원만 반영" 문의 조사 + 일괄반영 정확성 검증 + 직원 검색 기능 + 고정급/변동급 확인 표시**. 사용자가 작성 중인 사업계획에서 건강검진 항목이 일부 인원만 반영되는 게 버그인지 문의.
  - **원인 조사 결과(버그 아님, 코드 확인)**: `POST /emp-pay-plan`(`budget.js:653`)이 `items.filter(it => it.name && it.amount !== 0)`로 저장 시 금액이 0(또는 빈칸→Number("")=0)인 항목을 서버가 조용히 제외하는 구조 — 클라이언트 `submitEmpPayPlan()`도 동일한 필터를 이미 갖고 있어 이중으로 걸러짐. 즉 "일부만 반영"되는 원인은 버그가 아니라 ① 개인별 급여 상세를 직원별로 하나씩 수동 입력해 아직 전원 입력이 끝나지 않았거나, ② 건강검진을 "0원"으로 입력·저장을 시도한 경우 그 항목 자체가 레코드에서 사라져 미입력과 동일하게 보이는 것임을 격리 서버 재현으로 확인(0원 입력 시도한 직원의 실제 저장된 items에 건강검진이 없음을 직접 확인).
  - **전 직원 일괄 반영 정확성 검증**: 재직자 25명(팀장 포함, 여러 팀 혼합) + 퇴사자 3명 규모로 "전 직원 일괄 반영"을 실행 — 재직자 25명 전원 정확한 금액으로 반영, 퇴사자는 의도대로 대상에서 제외, 기존에 다른 항목(RSU)을 갖고 있던 직원도 그 항목이 삭제되지 않고 보존된 채 건강검진만 병합 추가됨을 확인. 버그 없음, 기능은 설계대로 정상 동작.
  - **직원 선택 검색 기능**(`public/index.html`): 개인별 급여 상세 화면의 직원 드롭다운 위에 검색 입력창(`#epp-emp-search`) 추가 — 이름/직급(`rank`)/직책(`position`)/입사년도(`hire` 앞 4자리)/부문/팀을 대상으로 부분일치 검색. `_eppFilterEmpOptions()`가 select의 옵션만 갱신(전체 재렌더 아님)해 타이핑 중 입력 포커스가 끊기지 않도록 함, 검색 중에는 라벨에 "(검색결과 N/전체명)" 카운트 표시. 드롭다운 옵션 자체도 직급·직책·입사년도를 함께 보여주도록 확장(기존엔 부문/팀만 표시).
  - **고정급/변동급 반영 확인 표시**: 직원 선택 시 상세 그리드 위에 읽기전용 참고 박스(`_eppLaborCostRefHtml()`) 추가 — 사업계획 그리드의 `addBpLaborCostRow()`와 동일한 산식(팀장은 연봉 중 600만원을 변동급, 나머지는 고정급 / 팀장이 아니면 전액 고정급)으로 이 직원 1인 기준 월 고정급·변동급을 계산해 표시. 개인별 급여 상세 자체에는 저장되지 않는 값(연봉 기반 자동계산이라 사업계획 작성 화면에서 팀 단위로 별도 반영됨)이라는 점을 명시.
  - 검증: 격리 서버에서 이름("홍길동")·직책("팀장")·직급("차장")·입사년도("2022") 4가지 검색 기준 전부 정확히 필터링됨을 확인, 검색어 삭제 시 전체 복원, 타이핑 중 입력 포커스 유지(전체 재렌더 없음) 확인. 팀장(연봉 6천만원) 선택 시 월 고정급 4,500,000원·변동급 500,000원, 비팀장(연봉 4,200만원) 선택 시 월 고정급 3,500,000원·"변동급 없음" 문구가 각각 정확히 표시됨을 확인. 93개 사이드바 메뉴 스모크, 3단계 롤업 회귀, DC/DB 퇴직급여·4대보험 자동계산 회귀 재실행으로 회귀 없음 확인(pageerror 0건 전체).
- 2026-07-29: **개인별 급여 상세 엑셀 업로드에 "연금종류(DC/DB)" 컬럼 인식 추가 + 사용자가 공유한 실제 회사 급여 원본 파일을 업로드용 포맷으로 변환·검증**. 사용자가 실제 회사의 "RAW자료"(개인별 급여상세 원본, 232명분)·"예산"(팀별 비인건비 판관/용역/경상 월별 상세)·"조직별"(사업부별 월별 인원현황 계획) 3개 시트로 구성된 실제 엑셀 파일을 첨부하며 반영을 요청.
  - **`public/index.html`**: `_eppParseAndUpload()`에 "연금종류" 컬럼 인식 추가 — 값이 정확히 "DC" 또는 "DB"일 때만 `severanceType`으로 매핑해 `POST /emp-pay-plan`에 포함(그 외 텍스트, 예: 원본 파일의 "중간정산(6월)" 같은 비고성 값은 무시). 업로드 카드 안내 문구에도 "연금종류(선택, DC/DB만 인식)" 반영.
  - **실행 범위와 제약(중요)**: 이 세션은 로컬 git 레포만 다루고 실제 운영 서버(Render/Postgres, 회사 실데이터)에는 접속 권한이 없어 직접 반영은 불가능 — 대신 원본 "RAW자료" 시트(개인별 급여상세, 정확히 우리가 앞서 만든 "개인별 급여 상세" 엑셀 업로드 포맷과 컬럼 구성이 거의 동일함을 확인)를 업로더가 기대하는 단일 헤더 행 포맷(사번/이름/연도/연금종류 + 36개 표준 항목명)으로 변환한 별도 파일을 준비해 사용자에게 전달, 실제 업로드는 사용자가 운영 화면의 "개인별 급여 상세 → 엑셀 일괄 업로드" 버튼으로 직접 수행하도록 안내. 이 원본/변환 파일과 그 안의 실제 이름·사번·급여 데이터는 **git에 전혀 커밋하지 않았고**(레포 밖 scratchpad에서만 처리), 검증에 쓴 임시 테스트 서버(`/tmp`)도 검증 직후 전부 삭제함(과거 이 프로젝트에서 실제 직원 PII가 커밋되어 `git filter-repo`로 히스토리를 재작성해야 했던 사고가 여러 번 있었던 전례를 따름).
  - **변환·검증 과정**: 원본 시트의 항목 컬럼명 중 일부가 표준 템플릿과 공백만 다르게 표기돼 있어(예: 원본 "복지카드(직무, 장애)" vs 템플릿 "복지카드(직무,장애)") 그대로 두면 해당 항목이 업로더에서 조용히 인식 실패했을 것 — 공백을 제거한 정규화 비교로 표준 템플릿 36개 항목 전부(고정급/변동급 제외) 정확히 매칭시켜 변환. 이 세션 환경은 외부망이 제한돼(cdnjs CDN 접근 불가) 브라우저 경로로 실제 업로드 버튼을 끝까지 눌러보는 재현은 실패했으나, 레포에 이미 있는 서버사이드 `xlsx` 패키지로 `_eppParseAndUpload()`와 동일한 파싱·매핑 로직을 그대로 재현해 격리 로컬 서버에 232명 전원 업로드 → 232/232 성공·스킵 0건, 연금종류 DC 매핑 13건, 무작위 3명 표본의 특정 항목 금액이 원본과 정확히 일치함을 확인 — 운영 환경(정상적인 인터넷 접근)에서 실제 업로드 버튼을 눌렀을 때도 동일하게 동작할 것으로 확인됨.
  - **처리하지 않고 남겨둔 범위**: "예산"(비인건비 판관/용역/경상 월별 상세) 시트는 현재 "사업계획" 판관비 그리드에 대응하는 컬럼 구성(팀명/비용귀속/항목/세부내역/구분/1~12월)과 거의 일치하지만 이 그리드에는 아직 엑셀 일괄 업로드 기능이 없음(수동 입력 또는 "개인별 급여상세 자동입력"만 가능) — 별도 업로드 기능 신설이 필요. "조직별" 시트(사업부별 월별 인원현황 계획)는 대응하는 입력 기능 자체가 시스템에 없음(현재 인원현황 표시는 `employees[]`의 현재 시점 스냅샷일 뿐 월별 계획 입력이 아님) — 신규 기능 개발이 필요. 둘 다 사용자 확인(AskUserQuestion) 요청에 응답이 없어 이번 라운드에서는 착수하지 않고 범위만 기록.
  - 93개 사이드바 메뉴 스모크, 직원 검색·고정급/변동급 표시 재검증으로 이번 변경의 회귀 없음 확인(pageerror 0건).
