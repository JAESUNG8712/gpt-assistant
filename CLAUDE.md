# GPT Assistant 프로젝트

## 계정 정보
- 이메일: myangle87@naver.com
- 계정명: jaesung8712
- GitHub 레포: jaesung8712/gpt-assistant
- 개발 브랜치: claude/account-pinned-agents-v6W7p

---

## 팀 에이전트 구성 (고정, 변경 지시 전까지 유지)

아래 8인 팀 구성은 명시적으로 변경 요청이 없는 한 항상 이 구성을 기준으로 작업한다.

| 역할 | 담당 | 설명 |
|---|---|---|
| 🗂️ 기획자 | 1명 | 사용자 요구사항 파악, 기능 정의, 일정 조율, 와이어프레임 |
| 🖥️ 프론트엔드 개발자 | 1명 | UI/UX 구현, 반응형 디자인, HTML/CSS/JS, React/Vue |
| ⚙️ 백엔드 개발자 | 1명 | 서버 로직, REST API, DB 설계, 인증/인가 |
| 🌐 네트워크 개발자 | 1명 | 네트워크 아키텍처, 서버 배포, CDN, WebSocket |
| 🔒 보안 담당자 | 1명 | 취약점 분석, 보안 감사, 암호화, OWASP 기준 점검 |
| 🧪 QA 담당자 1 | 1명 | 기능 테스트, 시나리오 설계, 결함 리포팅 |
| 🔬 QA 담당자 2 | 1명 | 성능 테스트, 자동화 테스트, 크로스브라우저 검증 |
| 📚 문서 작성자 | 1명 | 사용자 매뉴얼, API 문서, 개발 가이드, 운영 절차서 |

---

## 작업 규칙

- 사용자가 요청하면 기획자 역할로 요구사항을 먼저 정리하고, 각 담당 역할별로 구분하여 작업을 진행한다.
- 팀 구성 변경은 사용자가 명시적으로 요청할 때만 수정한다.
- 새 세션이 시작되어도 이 파일을 기준으로 동일한 팀 구성과 규칙을 유지한다.
- 작업 중 중요한 결정 사항이 생기면 이 파일의 "프로젝트 기록" 섹션에 추가한다.

---

## 기술 스택

- **런타임**: Node.js
- **서버**: Express.js
- **데이터 저장**: data.json (로컬 파일 기반)
- **프론트엔드**: 순수 HTML/CSS/JS (외부 프레임워크 없음)

---

## 자체 AI 시스템 (`ai/` 디렉토리)

사용자가 직접 소유하는 AI 어시스턴트. 유료 서비스 의존 없이 운영 가능.

### 구조
- `ai/main.py` — FastAPI 서버 (모든 API 엔드포인트)
- `ai/llm.py` — Groq API 연동 (Llama 3.1 8B, 무료)
- `ai/memory.py` — SQLite(대화이력·학습지식·문서·피드백 저장)
- `ai/engine.py` — 자체 TF-IDF 검색 엔진 (ChromaDB는 완전 제거, 벡터DB 미사용)
- `ai/search.py` — DuckDuckGo 인터넷 검색 학습
- `ai/backup.py` — 직접 다운로드(ZIP) + Google Drive 백업 (OAuth2)
- `ai/static/index.html` — iPad PWA 채팅 UI

### 배포 방법 (현재: Render.com 무료 + Turso 클라우드 DB)
운영 URL: https://ai-assist-aosk.onrender.com
1. `ai/` 폴더를 Render Web Service로 배포 (`ai/render.yaml` 참고)
2. 환경변수 `GROQ_API_KEY` 설정 (https://console.groq.com 무료 발급)
3. [turso.tech](https://turso.tech)에서 무료 DB 생성 후 `TURSO_DATABASE_URL`, `TURSO_AUTH_TOKEN` 환경변수 설정
   → 별도 디스크 마운트 불필요(Turso가 대화/학습 데이터를 클라우드에 영속 저장). 두 값을 설정하지 않으면
   로컬 SQLite로 자동 전환되지만, Render 컨테이너는 재배포 시 초기화되므로 데이터가 유실됨.
4. iPad Safari에서 배포 URL 접속 → "홈 화면에 추가" → PWA로 사용

### 배포 이력 (참고용, 더 이상 사용하지 않음)
- **Railway**: 2026-07-07 $5 플랜 해지, `ai/railway.json` 삭제됨. 로컬 SQLite + Volume 마운트 방식이었으나 위 Turso 방식으로 완전히 대체됨.
- **Oracle Cloud Always Free**: 신용카드 3D Secure 인증 문제로 가입 실패, `ai/oracle-*.sh` 스크립트 삭제됨.

### 백업 (직접 다운로드 + Google Drive)
- `GET /backup/download` — DB 전체를 ZIP(SQL 덤프 + 카테고리별 JSON + manifest)으로 즉시 다운로드
- Google Drive 연동(선택): Google Cloud Console에서 OAuth2 Client ID/Secret 발급 후 `GDRIVE_CLIENT_ID`, `GDRIVE_CLIENT_SECRET`, `GDRIVE_REDIRECT_URI` 환경변수 설정 → UI에서 Google 로그인 1회 후 버튼으로 백업

### 검색 엔진 (TF-IDF, ChromaDB 미사용)
- `ai/engine.py`의 자체 TF-IDF 엔진이 정적 KB(`ai/knowledge_*.py`)와 학습된 지식(`learned_knowledge` 테이블)을 통합 검색
- 학습/추가 시 `_dirty` 플래그로 표시되고 다음 검색 시 전체 corpus를 재구축하는 구조 (대규모 데이터에선 재구축 비용 고려 필요)

### 파인튜닝 (추후)
- 파인튜닝은 GPU 필요 → Google Colab 또는 Hugging Face 무료 컴퓨트 활용
- 현재는 RAG(검색 증강 생성)로 동일한 효과 구현

---

## 주식 분석 에이전트 팀 (11명)

`ai/stock_analysis/` 디렉토리의 주식 분석 전용 에이전트 팀.

| 역할 | 담당 | 설명 |
|---|---|---|
| 👔 총괄 관리자 | 1명 | 전체 파이프라인 조율, 분석 대상 선정, 최종 승인 |
| 💰 재무 자료 수집 | 1명 | DART 공시·재무제표, KRX 수급(외국인/기관/개인) |
| 📊 경제 자료 수집 | 1명 | 금리·환율·물가·지수·원자재 등 거시경제 지표 |
| 🌍 세계 동향 수집 1 | 1명 | 지정학 리스크, 무역 분쟁, 주요국 정치 |
| 🌐 세계 동향 수집 2 | 1명 | 산업 트렌드, AI·반도체·배터리·바이오 동향 |
| 📋 자료 취합 | 1명 | 4개 수집 에이전트 데이터 통합·정규화 |
| 🔍 상세 분석 | 1명 | 저평가 스크리닝, DCF/PER/PBR, 매수·매도 시점 |
| ✅ 교차 검증 1 | 1명 | 재무 데이터 이상치·회계 처리 검증 |
| 🔬 교차 검증 2 | 1명 | 분석 논리 일관성·편향 검증 |
| ⚠️ 교차 검증 3 | 1명 | 리스크 독립 검증, 스트레스 테스트 |
| 📝 레포트 작성 | 1명 | 오전 7시 / 저녁 10시 정기 보고서 생성 |

### 환경 변수
- `DART_API_KEY` — DART OpenAPI (https://opendart.fss.or.kr)
- `BOK_API_KEY` — 한국은행 ECOS API (https://ecos.bok.or.kr)
- `ANTHROPIC_API_KEY` — Claude API (https://console.anthropic.com) — 미설정 시 규칙 기반 분석으로 fallback

### Claude API 통합
- 모델: `claude-opus-4-8` (적응형 사고 `thinking: adaptive`)
- 적용 위치: `ai/stock_analysis/utils/claude_client.py`
  - `analyze_stock_opinion()` — 종목별 AI 투자의견 (StockAnalyzer)
  - `generate_executive_summary()` — 시황 종합 요약 (ReportWriter)
  - `generate_action_plan()` — 실행 가능한 투자 액션 플랜 (ReportWriter)
- Prompt caching 적용 (시스템 프롬프트 고정 텍스트)
- API 키 미설정 시 자동 fallback → 규칙 기반 분석 유지

### API
- `GET  /stock/team` — 팀 구성 조회
- `POST /stock/analyze` — 분석 실행 (백그라운드)
- `GET  /stock/analyze/sync` — 분석 동기 실행
- `GET  /stock/report/text` — 최근 보고서 텍스트
- `GET  /stock/status` — 실행 상태

---

## 프로젝트 기록

<!-- 중요한 결정 사항, 요구사항, 변경 이력을 여기에 추가 -->

- 2026-05-26: 8인 팀 에이전트 구성 확정 (기획자 1, FE 1, BE 1, 네트워크 1, 보안 1, QA 2, 문서 1)
- 2026-05-26: 에이전트 미설정 계정에 기본팀 자동 적용 기능 구현
- 2026-05-26: 프로젝트 맥락 영속 저장 기능(메모리 API) 구현
- 2026-05-26: 자체 AI 시스템 구현 (Groq+RAG+OneDrive 백업, iPad PWA)
- 2026-06-02: 주식 분석 11인 에이전트 팀 구현 (DART+KRX+매크로+지정학+검증3회+정기보고서)
- 2026-06-02: Claude API (claude-opus-4-8) 통합 — 종목별 AI 투자의견·시황요약·액션플랜 자동 생성, prompt caching 적용
- 2026-06-17: 백업 구조 개선 — `ai/backup.py`에 카테고리별 JSON 추출(by_category/*.json) + manifest.json 추가, ZIP 다운로드/Google Drive 백업 모두 적용
- 2026-06-17: 법률 KB 검색 정확도 개선 — `knowledge_legal.py`/`knowledge_legal2.py` 65개 항목 중 정확도 80% 미만 44개 항목의 `q` 필드에 자연어 변형 키워드 보강 (전체 정확도 67.1% → 92.8%, `test_legal_kb_accuracy.py`로 검증)
- 2026-06-17: 문서/코드 불일치 수정 — CLAUDE.md의 ChromaDB 설명을 실제 구현(TF-IDF `ai/engine.py`)에 맞게 수정, OneDrive→Google Drive 백업 설명 수정
- 2026-06-17: 법률 KB 확장 — `ai/knowledge_legal3.py` 신설, 모자보건법·일가정양립법(남녀고용평등법)·영유아보육법·아동수당법 등 가족·돌봄 관련 법령 추가
- 2026-06-17: 주식 리포트 채팅 요약 개선 — `_extract_report_section()` 헬퍼로 TOP 추천 종목·저평가 종목 섹션을 항상 채팅 응답에 포함 (특정 종목 미언급 시에도 동일), `report_writer.py`에 `_undervalued_picks()` 섹션 신설(분석 대상 종목 중 저평가/강한저평가 등급만 별도 정리)
- 2026-06-17: 검색 엔진 매칭 정교화 — `python-mecab-ko` 형태소 분석기를 옵션으로 도입(미설치 시 규칙 기반 토크나이저로 자동 폴백)해 붙여쓰기 복합어 분해 보강, 도메인별(개발/여행/채용) 동의어 그룹 추가(법률/HR KB와 겹치는 단어는 회귀 방지를 위해 제외). 법률 KB 정확도 92.8% → 93.0%로 유지·개선 확인
- 2026-06-17: 검색·답변 내용 영구 RAG 누적 + 피드백 학습 구현 — `/chat` 응답을 (stock 페르소나 제외) 항상 `auto_learn`으로 영구 저장하도록 확장(기존엔 `no_local` 케이스만 학습), `feedback_boost` SQLite 테이블 신설로 페르소나+질문 단위 좋아요/싫어요 누적 가중치 저장(좋아요 → 1.3배 부스트 최대 3.0, 싫어요 → 0.4배 감산 최저 0.05), `engine.py` 검색 점수에 가중치 곱 적용해 싫어요 받은 답변은 다음 동일 질문 시 새로 생성되고 좋아요 받은 답변은 유사 질문에서도 우선 노출되도록 함, `/feedback` 엔드포인트에서 즉시 라이브 반영 + 엔진 로드 시 SQLite에서 복원하여 재시작 후에도 유지
- 2026-06-22: 배포 후 RAG 데이터 초기화 문제 진단 — 사용자가 실사용 중인 플랫폼은 Railway였고, `ai/railway.json`에는 영속 볼륨이 코드로 선언되어 있지 않아 재배포마다 컨테이너 임시 파일시스템의 SQLite DB가 사라지는 것이 원인으로 확인됨(코드 자체의 학습/누적 로직은 이미 정상 동작, 인프라 설정 누락이 근본 원인). `main.py`에 `DB_PATH` 미설정 시 배포 로그에 경고를 출력하는 점검 코드 추가, `CLAUDE.md`에 Railway Volume 생성 + `DB_PATH=/app/data/memory.db` 환경변수 설정 가이드 추가
- 2026-06-22: KRX 수급/공매도 데이터 조회 실패 시 fallback 누락 수정 — `krx_client.py`의 `get_investor_trading()`/`get_short_selling()`이 pykrx 실패 시 에러 dict를 반환해 주식 분석 파이프라인이 조용히 데이터 누락되던 문제를, 같은 파일 내 다른 함수들이 이미 쓰던 `_mock_investor_data()` 등 fallback 패턴과 일치시켜 수정
- 2026-06-22: 페르소나별 검색 분리 — `main.py`의 `/chat` 인터넷 검색 단계에서 `srch.search_and_learn(search_msg)` 호출이 `persona_id`를 누락해 모든 페르소나의 웹검색 학습 결과가 `search_and_learn()` 기본값인 `persona="hr"`로 고정 저장되던 버그를 발견. `search_and_learn(search_msg, persona_id=req.persona)`로 수정해 이후부터는 페르소나별로 검색/학습 데이터가 분리됨. 이 수정 이전에 이미 저장된 웹검색 학습 데이터는 원래 페르소나 정보가 남아있지 않아 자동 재분류는 불가(필요 시 수동 정리 별도 논의)
- 2026-06-22: 증권사 리포트 컨센서스 섹션에 빈 placeholder 데이터가 "수집 리포트 N건"으로 표시되던 문제 수정 — `securities_report.py`의 `_parse_naver_research()`(네이버 리서치 HTML 파싱)와 `search_reports_ddg()`(DuckDuckGo 보조 수집)가 증권사명·목표주가·투자의견·날짜 추출에 실패해도 빈 문자열을 그대로 담은 report dict를 리스트에 추가하던 구조라, `_build_consensus()`의 `len(reports)`가 빈 항목까지 카운트해 "수집 리포트 2건"처럼 보이지만 실제 내용은 전부 "미상"/"중립"/"TP:-"인 신뢰할 수 없는 결과가 나왔음. `get_all_reports()`에 `_is_meaningful()` 필터(목표주가·투자의견 중 하나라도 실제 값이 있어야 통과)를 추가해 빈 항목을 컨센서스 집계 전에 제거 — 실데이터가 없으면 "수집된 리포트 없음"으로 정직하게 표시됨
- 2026-06-24: 주식 분석 채팅 응답 상세화 + 긴 자료 다운로드 분리 + 전 페르소나 참고자료 링크 추가. (1) `_summarize_stock_report()`(main.py)가 종목 미지정 "현황" 질문에 기존 Executive Summary/TOP 추천 종목/저평가 종목 3개 섹션만 보여주던 것을, 시장 환경 분석·증권사 애널리스트 리포트 컨센서스 2개 섹션을 추가해 5개 섹션으로 확장 — report_writer.py 자체는 14개 섹션을 이미 생성하고 있었으나 채팅 요약 레이어가 그중 일부만 노출해 "모호하다"는 문제가 발생했던 것이 원인. (2) `_summarize_long_text()` 신설 — 답변 본문이 3500자를 넘으면 앞부분만 채팅에 표시하고 전체는 `stock_analysis/reports/answers/`에 파일로 저장 후 다운로드 링크(`/answer/download/{filename}` 신규 엔드포인트, `answer_` 접두사·`..` 차단)로 안내. `run_broker_report`(단독 증권사 리포트 조회)·`run_lowprice_screen`(저평가 저가주 스크리닝) 두 원문 그대로 출력하던 경로에 적용. (3) `_format_reference_links()` 신설 — `[{title,url}]` 목록을 마크다운 링크 푸터로 변환(http만 허용, 중복 제거, 최대 5개). 경로 B(LLM 보강) 양쪽 분기에 `reference_items` 수집을 연결: stock_mode는 증권사 리포트 "링크" 필드 + DDG 자동검색 결과, 비주식 페르소나는 law_search 결과 + 일반 웹검색 결과. 프론트엔드(`index.html`의 `renderMd()`)가 이미 마크다운 링크와 `/stock/download/` `/answer/download/` 다운로드 버튼 스타일을 지원하고 있어 프론트엔드 변경은 불필요했음. 적용 범위 제한: KB-직접답변(경로 A)·기업 KB 무응답(경로 C)에는 참고자료가 없어 미적용, 뉴스 기사(`news_collector.py`의 "출처" 필드는 출처명/URL이 혼재해 신뢰 불가)는 참고링크에서 제외
- 2026-06-24: 증권사 리포트·뉴스 수집기가 실데이터를 못 가져오던 근본 원인 다수 발견·수정(이 환경의 외부망 차단이 풀린 뒤 실제 네이버 금융 응답으로 검증). `securities_report.py`의 `fetch_naver_research()`가 `searchType=priceTo&code={ticker}` 파라미터를 쓰고 있었는데 이는 종목과 무관한 전체 "목표주가 변경" 리스트를 반환하는 잘못된 조회였음(올바른 파라미터는 `searchType=itemCode&itemCode={ticker}`) — 이 때문에 `_parse_naver_research()`의 정규식(`/research/company_read.naver?nid=` 절대경로 기대)도 실제 마크업(`company_read.naver?nid=` 상대경로)과 맞지 않아 항상 0건이 파싱됐음. 목표주가·투자의견은 목록 페이지에 컬럼 자체가 없고 개별 리포트 본문(`목표가 480,000 | 투자의견 Buy` 형태)에만 있다는 것도 확인 — `_enrich_with_target_opinion()` 신설로 상위 5건의 본문을 추가 조회해 채움, `_extract_report_meta()`로 본문에서 목표가·투자의견 파싱, 영문 등급(Buy/Hold/Sell)을 한글 버킷으로 정규화하는 `_normalize_opinion()` 추가(기존 `_build_consensus()`가 한글 키워드만 매칭해 영문 등급은 의견분포에서 누락되던 문제도 같이 해결). 또한 `search_reports_ddg()`가 `async def`이면서 내부적으로 완전히 동기 블로킹인 DDGS 호출을 실행하고 있어, `get_all_reports()`의 `asyncio.gather()`로 `fetch_naver_research()`와 함께 묶이면 이벤트 루프를 점유해 네이버 쪽 요청이 거의 항상 타임아웃되던 동시성 버그를 발견·수정(`run_in_executor`로 별도 스레드 실행, `news_collector.py`가 이미 쓰던 패턴과 통일) — 이게 "정확한 내용이 거의 안 들어간다"는 원래 사용자 불만의 진짜 근본 원인이었음(이전 수정은 빈 placeholder를 걸러내는 대증 처방이었을 뿐, 데이터가 애초에 안 들어오는 원인은 그대로였음). `news_collector.py`의 `_fetch_naver_finance_news()`도 같은 종류의 파싱 버그 수정(제목이 `title=` 속성이 아니라 `<a>` 태그 내부 텍스트로 들어있었음, 날짜 정규식이 공백·시간 포함 형식과 안 맞았음). 두 파일의 HTML 클린업 헬퍼(`_clean`/`_clean_html`)에 `html.unescape()` 추가해 `&hellip;` 등 엔티티 미디코딩 문제도 해결. 부가적으로 `aiohttp.ClientSession()` 호출들에 `trust_env=True`를 추가(이 개발 샌드박스의 로컬 프록시를 타려면 필요했던 설정, 배포 환경에는 영향 없음). 전체 파이프라인 실측 결과: 삼성전자 기준 증권사 리포트 5건(평균 목표주가 522,000원, 의견분포 매수 5건)·뉴스 8건이 정상 수집·표시됨을 확인
- 2026-07-07: DB를 Railway 로컬 SQLite → Turso 클라우드 DB(무료, SQLite 호환)로 이전 — `memory.py`가 `TURSO_DATABASE_URL`+`TURSO_AUTH_TOKEN` 설정 시 Turso HTTP API(/v2/pipeline)를 순수 Python urllib로 호출(Rust 빌드가 필요한 libsql-experimental은 Render 빌드 실패로 제거), 미설정 시 기존 로컬 SQLite로 자동 전환. 배포도 Render.com 무료 웹서비스(https://ai-assist-aosk.onrender.com)로 이전(Railway $5 플랜 해지). Railway 기존 데이터 934건(학습지식 921·대화 8·피드백 4·설정 1)을 `migrate_to_turso.py` + `/admin/import-db` 엔드포인트로 무손실 이관 완료. Render 무료 티어는 15분 비활성 시 절전(첫 접속 30~60초 웨이크업) — 필요 시 UptimeRobot 등 무료 핑으로 상시 유지 가능. 같은 날 `UnboundLocalError`(law_results/results 미초기화 참조) 수정
- 2026-07-07: 의도 분석 에이전트(`intent_agent.py`) 신설 — `/chat` 0.5단계에서 질문 의도를 LLM으로 분석해 (1) 검색 최적화 정제 질의 생성: KB 매칭 점수가 원본 질의보다 높거나 같을 때만 채택하는 게이트로 회귀 방지, 채택 시 법령/웹 검색에도 동일 질의 적용, (2) 의도·답변요건(`format_intent_context`)을 LLM 컨텍스트 지시문 앞에 주입(주식/비주식 경로 모두)해 질문 의도에 맞춘 답변 유도. 분석 실패·타임아웃(8초, `INTENT_TIMEOUT`) 시 원본 질의 그대로 사용해 기존 동작 완전 유지, `INTENT_AGENT=off`로 비활성화 가능. 직접 계산(CALC) 경로(지연 불필요)와 company 페르소나(사내 문서 전용·외부 LLM 미사용 정책)는 분석 제외
- 2026-07-07: 연차 계산기 기준일 파싱 + 취업규칙 KB 연동 + 계산기 전체 QA. (1) `try_annual_leave_calc()`가 질문 속 기준일("24년 1월 1일 입사자는 26년 1월 1일에 몇 개?")을 무시하고 항상 오늘 기준으로 계산하던 버그 수정 — `_parse_all_dates()` 신설로 다중 날짜 추출, '입사' 키워드 인접성으로 입사일 판별, 나머지 중 가장 늦은 날짜를 기준일로 사용(기준일<입사일이면 경고, 미래 가정 질문도 계산 가능). (2) 계산 결과의 "회사 취업규칙 확인 권장" 고정 문구를 실제 KB 연동으로 대체 — `main.py`의 `_company_rule_supplement()`가 company 페르소나 KB에서 관련 규정 검색(유사도≥0.15), 있으면 규정 본문+출처 표시, 없으면 연차 계산에 한해 일반 안내 문구. (3) QA로 같은 부류 오류 2건 추가 발견·수정: `try_min_wage_check()`도 질문 속 연도("2024년 시급 9900원 위반?")를 무시하고 현재 연도로 판정하던 버그(`_question_year_min_wage()` 신설, 데이터 밖 연도는 None 반환해 KB/LLM 경로로), `_parse_amount()`의 순수 숫자 fallback이 "2024년"의 2024를 금액으로 오인하던 버그(년/월/일 뒤따르는 숫자 제외). 실수령액·4대보험 계산 제목의 "{현재연도}년 기준" 표기도 실제 요율 연도인 "2024년 요율 기준"으로 정직하게 수정. QA 테스트 28건 전체 통과 확인
- 2026-07-07: 복수 계산 의도 종합 응답 — `try_any_calc()` 디스패처를 "첫 매칭 반환"에서 "전체 의도 수집 후 종합"으로 변경. 계산기를 7개 그룹으로 분류(`_CALC_GROUPS`, 같은 그룹 내에서는 첫 매칭만 — 기간연차/단일연차처럼 동일 주제 대체 계산기 중복 방지), 서로 다른 그룹이 2개 이상 매칭되면 "📊 종합 계산 결과" 헤더에 인식된 의도 목록을 표시하고 각 계산 결과를 구분선으로 결합(예: "퇴직금이랑 연차 알려줘" → 연차+퇴직금 두 섹션, 연차 기준일은 퇴직일로 자동 적용). 단일 매칭은 기존 그대로 반환해 회귀 없음. QA 38건 전체 통과
- 2026-07-08: OpenCode 연계 — (1) `llm.py` 폴백 체인에 OpenCode Zen(무료 모델 게이트웨이, OpenAI 호환 https://opencode.ai/zen/v1) 추가: `OPENCODE_ZEN_API_KEY` 설정 시 Claude 다음 2순위로 동작, 기본 모델 `deepseek-v4-flash-free`(환경변수 `ZEN_MODEL`), 모델 종료(404) 시 나머지 무료 모델 4종(`nemotron-3-ultra-free` 등)으로 자동 재시도. 무료 모델 ID 5종은 Zen API 실측으로 확인. (2) 레포 루트에 `opencode.json`(무료 모델 기본 + CLAUDE.md instructions 자동 로드)·`AGENTS.md`(에이전트 작업 규칙 요약) 추가로 OpenCode 코딩 에이전트로 유료 API 없이 이 레포 개발 가능. 상세 가이드 `ai/OPENCODE_GUIDE.md`
- 2026-07-08: KB 오답 학습(오염) 문제 해결 — "출장 규정 알려줘"에 "직장 내 괴롭힘" 답변이 나오던 원인은 과거 오답이 자동학습(source='자동학습')으로 KB에 저장되어 동일 질문 재질의 시 높은 유사도로 직접 서빙되는 구조(정적 KB·토크나이저는 정상임을 로컬 재현으로 확인, '출장'≠'직장' 토큰 분리 정상). 3중 수정: (1) `memory.topic_overlap()` 모듈 승격(retrieve_best 내부 중복 제거) 후 `auto_learn()`에 품질 게이트 4 추가 — 질문 핵심 단어가 답변에 전혀 없으면 주제 불일치로 저장 차단(오염 원천 방지), (2) `retrieve_best()`가 `top_source` 반환, main.py에서 자동학습/대화 출처 항목이 주제 불일치면 KB 직접 서빙을 LLM 재생성으로 강등(기존 오염 항목 노출 방지), (3) `GET /admin/learned?q=키워드`(검색)·`DELETE /admin/learned/{id}`(삭제+엔진 즉시 반영) 관리 API 신설(오염 항목 수동 정리). 추가 발견: `backup.py`가 로컬 SQLite 파일만 덤프해 Turso 이전 후 백업이 빈 ZIP이 되던 버그 — `mem._conn()` 경유로 재작성해 Turso/로컬 모두 실데이터 백업(sqlite_master DDL + INSERT 덤프 생성)
- 2026-07-08: KRX 계정 연동 완료(data.krx.co.kr 무료 회원가입 + Render `KRX_ID`/`KRX_PW` 환경변수) — pykrx 1.2.8부터 일부 API가 로그인을 요구하게 된 것이 "KRX 로그인 실패" 경고의 원인이었음(기능 자체는 `krx_client.py`/`popular_stocks.py`의 네이버 폴백으로 계속 정상 동작 중이었음). 로그인 성공 확인. 같은 날 별도로 `[claude] HTTP 400 오류` 로그의 실제 원인 파악 시도 중 로깅 버그 발견·수정 — `llm.py`의 `_claude_stream`/`_openai_compat_stream`/`_gemini_once`가 `httpx` 스트리밍 응답에 대해 본문을 읽지 않은 채 `raise_for_status()`를 호출해, 예외 처리부의 `e.response.text` 접근이 `ResponseNotRead`로 실패하고 `str(예외)`("Client error '400 Bad Request'...")만 로그에 찍혀 실제 API 오류 사유(예: 크레딧 소진, 잘못된 요청 필드 등)를 알 수 없었음. 세 함수 모두 `raise_for_status()` 전에 `await resp.aread()`로 본문을 먼저 읽도록 수정해 다음 발생 시 실제 오류 메시지가 로그에 남도록 함
- 2026-07-08: 예산관리(`budget_store.py`) 데이터 유실 문제 발견·수정 — Turso 이전 시 `budget_store.py`는 마이그레이션 대상에서 누락되어 있었음. `BUDGET_FILE = os.path.dirname(mem.DB_PATH)/budget-data.json` 형태로 로컬 파일에 저장하는 구조였는데, Railway에서는 `DB_PATH=/app/data/memory.db`가 영속 볼륨 안에 있어 우연히 함께 보존됐지만, Render+Turso 전환 후에는 `DB_PATH` 환경변수를 없애 로컬 파일 경로가 컨테이너 임시 파일시스템(`ai/data/`)으로 떨어지면서 재배포마다 초기화되고 있었음(실제로 재배포 후 `/budget/data`가 빈 상태로 확인됨). Railway 앱이 아직 살아있어 `/budget/sheets`로 원본 데이터(급여RAW 240행, 급여 자료 정리 30행) 백업 후 Render로 이관 완료. 재발 방지: `read_budget()`/`write_budget()`을 로컬 파일 I/O에서 `mem.get_setting()`/`mem.save_setting()`(app_settings 테이블, Turso/SQLite 공용 경유)으로 변경해 대화·학습 데이터와 동일하게 DB에 영속 저장되도록 수정. 코드 변경으로 저장 위치가 바뀌므로 배포 후 Railway 데이터 1회 재이관 필요
- 2026-07-08: "출장 규정 확인해줘" 등 질의 시 화면에 `<!DOCTYPE html>...`로 시작하는 원본 HTML이 그대로 노출되던 문제 진단·수정 — KB 오염이 아니라 프론트엔드 오류 처리 버그였음(운영 KB `/admin/learned` 검색으로 해당 내용이 DB에 없음을 확인해 KB 원인 배제). `index.html`의 `sendMessage()`가 `if (!resp.ok) throw new Error(await resp.text())` 구조라, Render 재배포·콜드스타트 중 프록시가 반환하는 502/503 HTML 오류 페이지 본문을 그대로 에러 메시지로 사용해 채팅창에 원문이 그대로 찍히고 있었음. `friendlyErrorMessage()` 헬퍼 신설 — 502/503/504는 "서버 일시 응답 없음, 잠시 후 재시도" 안내로, JSON 오류는 `detail` 필드만 추출, 그 외 HTML 시작 응답은 원문 노출 없이 상태 코드만 표시하도록 수정. 이력서 분석(`/analyze/resume`) 등 다른 fetch 경로는 이미 `.json().catch()` 패턴으로 안전하게 처리되어 있어 추가 수정 불필요했음
- 2026-07-08: "출장 규정 확인해줘"가 회사 실제 규정과 다르게 나오는 문제 진단·수정. 원인 3가지: (1) Railway→Turso 마이그레이션 시 `kb_static_index`(정적 KB 중복방지 해시 테이블)가 이관 대상에서 제외되어 있어, Render 최초 기동 시 `_seed_static_kb_to_db()`가 기존 해시를 못 찾고 company 페르소나 정적 KB 73개 항목 전체를 중복 저장(총 146개, id 760~832가 750~1698의 중복) — `/admin/learned`로 전수 조사 후 중복 73건 삭제. (2) 오늘 QA 테스트 중 "출장 규정 알려줘"를 hr 페르소나로 여러 차례 호출한 것이 자동학습되어(LLM이 지어낸 가상의 수치가 포함된 답변, source='자동학습') 이후 같은 질문에 오답이 직접 서빙되던 문제 — 해당 2건(id 1796, 1797) 삭제. (3) 근본 구조적 원인: `personas.py`의 `_AUTO_CLASSIFY_KEYWORDS["hr"]` 목록에 "출장" 키워드가 없어 매칭되는 페르소나가 없으면 기본값 `hr`로 라우팅되고, `company`는 "명시적 선택 필요"라는 이유로 자동분류 후보에서 원천 제외되어 있어 사내 실제 규정(company KB에 정확히 존재: 출장일비 국내 1급지 30,000원 등)에 닿지 못하고 hr 페르소나의 낮은 KB 점수로 인한 자동 웹검색이 "공무원 여비규정"(정부기관용, 전혀 다른 규정)을 가져와 답변하고 있었음. 사용자에게 자동 라우팅 개선 여부를 확인 후(권장안 선택) `main.py`에 company KB 우선 라우팅 로직 추가 — `req.persona=="auto"`이고 라우팅된 페르소나가 company가 아닐 때, company KB 매칭 점수가 현재 페르소나 점수보다 높고 0.15 이상이면 company로 전환(페르소나·시스템프롬프트·KB 컨텍스트 전부 재계산). 회사 KB에 명백히 더 정확한 답이 있을 때만 전환되도록 임계값을 두어 오탐 최소화
- 2026-07-08: "출장비 신청 어떻게 해?" 재현 조사 중 정적 KB 자체에 근거 없는 구체적 수치(임원 숙박비 15만원 한도, 일비 3만원 등)가 하드코딩되어 있는 것을 발견 — `knowledge_hr_gaps.py`의 "원거리 출장비" 항목이 실제 출처 없이 "공무원 여비 기준이 민간 기업 벤치마크로 활용됨"이라 서술하며 구체적 금액을 사실처럼 제시하고 있었음. 항목 구성(교통비/숙박비/일비/식비 카테고리)만 남기고 특정 금액·직급별 표는 제거, "회사마다 다르며 반드시 소속 회사 취업규칙을 확인하라"는 명시적 경고문 추가. 근본 원인 추가 발견: Turso 이관 시 `kb_static_index`(중복방지 해시 테이블)가 이관 대상에서 제외되어 있던 문제가 company 페르소나(73건)뿐 아니라 hr/dev/travel 등 전 페르소나 정적 KB에도 동일하게 영향을 미쳐 대규모 중복이 남아있었음 — 관리 API `GET /admin/learned?offset=`(페이지네이션), `GET/POST /admin/learned/duplicates?apply=true`(전체 content 완전일치 중복 탐지·일괄삭제, 가장 오래된 항목만 보존) 신설로 전 페르소나 전수 정리 가능하도록 함. `migrate_to_turso.py`의 `_TABLES`에도 `kb_static_index`를 추가해 향후 재이관 시 동일 문제 재발 방지
- 2026-07-08: 공유 링크(URL 기반 부분 공개) 기능 신설 — 소유자가 특정 페르소나만 선택해 공유 링크를 발급하면, 링크를 받은 사람은 로그인 없이 해당 페르소나로만 채팅 가능(예산관리·백업·학습관리 등 관리 기능은 접근 불가). `memory.py`에 `share_links` 테이블(token/personas(JSON)/enabled/expires_at) 및 `create_share_link`/`get_share_link`/`list_share_links`/`revoke_share_link` 추가. API: `POST/GET /admin/share`(생성/목록, 소유자용), `DELETE /admin/share/{token}`(폐기), `GET /share/{token}`(공개, 링크 유효성+허용 페르소나 확인용). `/chat`에 `share_token` 필드 추가 — 유효하지 않거나 허용되지 않은 페르소나 요청은 403, `auto` 모드는 허용된 페르소나 교집합으로 자동 제한. 기존 "company KB 우선 라우팅" 로직도 공유 링크가 company를 명시적으로 허용한 경우에만 동작하도록 가드 추가(권한 범위 밖 페르소나로 우회 전환되는 것 방지). 프론트엔드(`index.html`): `?share=TOKEN` URL 파라미터 감지 시 `/share/{token}`으로 유효성 확인 후 허용된 페르소나만 노출하고 소유자 전용 UI(예산관리 탭, 공유 링크 관리 버튼) 숨김, 무효/만료 링크는 안내 메시지 표시. 소유자는 페르소나 상단바의 "🔗 공유 링크" 버튼으로 링크 생성·목록·복사·삭제 가능한 모달 사용
- 2026-07-09: 법률 KB 검색 정확도 추가 개선 — `engine.py` 한국어 어미 처리에 '이란'·'하려면'·'하려고'·'하려는' 추가, 복합어 정규화 맵에 자동 QA에서 발견된 미분리 케이스 8건(유언장작성, 비정규직차별 등) 추가. `knowledge_legal.py`/`knowledge_legal2.py`/`knowledge_base.py`의 `q` 필드에 자연어 변형 키워드 보강(예: "사직서 내라고 압박", "채권추심법 위반" 등 실제 사용자가 쓸 법한 표현 추가), 일부 항목의 중복 반복 구문 정리. `test_legal_kb_accuracy.py`의 테스트 케이스 1건 교체(더 어려운 자연어 표현으로). 전체 정확도 456개 질의 중 445개 통과(97.6%), 80% 미만 항목 0개로 전부 해소(이전 92.8~93.0% 대비 개선)
- 2026-07-09: Cowork 병렬 세션(PR #6 `claude/fix-async-blocking-and-backup-auth`, "Weekly maintenance pass")의 QA/보안 수정을 검토 후 수동 반영 — main.py/memory.py 등 핵심 파일이 이 세션의 작업과 크게 갈라져 있어 자동 병합 대신 실제 변경 커밋(e6c3a13, f6cb221) 2개만 골라 파일별로 직접 적용. (1) 이벤트루프 블로킹 동기 호출 8곳을 `run_in_executor`로 래핑: `main.py`의 `chat()` DDG 검색·`/stock/popular/sync`, `law_search.py`의 `search_law_ddg`, `krx_client.py`의 pykrx 호출 5곳(`_run_sync` 헬퍼 신설), `financial_collector.py`의 네이버 ticker 조회, `stock_api.py`의 `/screen/lowprice` — 방치 시 해당 호출 동안 다른 모든 요청(채팅 포함)이 멈추는 구조적 문제였음. (2) `/backup/download`·`/backup/google-auth`·`/backup/google-drive`·`/debug/law`에 인증 없이 전체 대화·학습 DB가 노출되던 문제 수정 — 선택적 `BACKUP_TOKEN` 환경변수 게이팅 추가(미설정 시 기존처럼 열려있되 시작 로그에 경고), Google OAuth 콜백에 CSRF `state` 파라미터 검증 추가, 토큰 저장을 `pickle`(역직렬화 위험)에서 JSON으로 전환(레거시 pickle 파일 1회 자동 마이그레이션). (3) 부가 수정: `stock_api.py`의 `_analysis_running` 플래그를 백그라운드 태스크 시작 전에 즉시 설정해 동시 요청 경쟁 조건 차단, `report_writer.py`의 다음 보고서 시각 안내 로직 버그(`hour<12`→`hour<22`) 수정, `dart_client.py`에 신규 상장 종목 6개 추가 + `None.replace()` 크래시 방어 + 공시 검색 실패 시 조용히 무시하지 않고 로그 남기도록 수정, `email_sender.py` SMTP 연결에 `timeout=30` 추가(무한 대기 방지), `claude_client.py`의 세 LLM 호출에 CLAUDE.md에 이미 명시돼 있던 `thinking:adaptive`/`effort:medium` 설정이 실제로는 누락되어 있던 것을 추가, `calculator.py`에 구어체 금액 표현("1억5천") 파싱 추가 + 4대보험 요율을 2024년 값에서 2026년 값으로 갱신(라벨도 "2026년 요율 기준"으로 동반 수정), `personas.py`의 미사용 `classify_persona()`(단수) wrapper 제거. (4) 같은 PR 브랜치의 법률 KB 정확도 보강 diff는 이미 별도 세션에서 검토·적용됨(commit 1bec482) — 이번엔 그 상위집합에 있던 추가분만 반영: `engine.py` 복합어 정규화 맵에 우선순위 있는 특수 케이스 3건(출산휴가배우자 등, 반드시 맨 앞 배치) 추가, company/dev/travel/resume 페르소나용 신규 자동 QA 하네스 `test_persona_kb_qa.py` 추가(현재 베이스라인 73.8% 통과 — legal KB의 97.6%와 달리 아직 개선 여지 많음, 후속 작업 후보). 병합하지 않은 것: 같은 브랜치 히스토리에 있던 대규모 엑셀형 스프레드시트 재구현(`budget.html` 4900줄+) — 현재 이 세션의 더 단순한 예산관리 구현(Turso 저장, 실데이터 복원 완료)과 완전히 다른 별개 구현이라 무단 교체 시 사용자의 실제 예산 데이터가 깨질 위험이 있어 사용자 확인 없이는 보류
- 2026-07-09: 코드베이스 전수 감사(4개 영역 병렬 조사: main.py 라우팅, memory/engine/personas KB 레이어, calculator/stock_analysis 파이프라인, 프론트엔드/배포설정) 후 발견된 실제 버그·죽은 코드 정리. **버그 수정 2건**: (1) `engine.py`의 "동적 학습 데이터 복원" 로직이 `sqlite3.connect(DB_PATH)`로 로컬 파일을 직접 열어 Turso 사용 시 그 파일이 없어 재배포마다 피드백학습·문서업로드 등 동적 KB가 검색엔진에 복원되지 못하던 데이터 소실 버그를 `memory._conn()` 경유로 수정(로컬 테스트로 복원 정상 동작 확인). (2) `memory.store_memory()`가 다른 KB 쓰기 경로(upsert_knowledge/store_document/auto_learn)와 달리 유일하게 중복 검사가 없어, `search.search_and_learn()`이 같은/유사 질의로 반복 호출될 때마다 동일 웹검색 결과가 무제한 중복 저장되던 문제를 (content, persona) 완전일치 검사로 해결. **부수 수정**: `/history/stock/reset`이 Turso 우회 + 존재한 적 없는 source 문자열('auto_learn'/'learned')을 필터링해 항상 0건 매칭이던 버그를 실제 source 값('자동학습') 기준으로 수정, `/knowledge/stats`도 동일하게 Turso 우회 버그 수정 + db_backend 필드 추가, `/health`에 Turso 인지 로직 추가(로컬 디스크 지표는 SQLite 모드에서만 표시). **죽은 코드 제거**: main.py의 완전히 도달 불가능한 중복 라우트(`/stock/reports/list` — stock_api.py의 router 버전에 항상 가려짐), `memory.store_conversation_memory()`(호출부 전무), `index.html`의 `downloadStockReport()`(호출부 전무), stock_analysis 전반의 미사용 메서드 8개(get_macro_signal/get_investor_summary/get_undervalued_candidates/get_industry_opportunities/get_clean_stocks/_fetch_naver_research_news/fetch_major_shareholders+mock/fetch_company_info+mock 및 dart_client mock 2종), Oracle Cloud 배포 스크립트 3개+`railway.json`(완전 미사용 — Oracle 가입 포기, Railway 해지로 둘 다 무의미해짐), stock_analysis 전역 미사용 import 13개(pyflakes로 검증) 및 미사용 지역변수 3개. 전 작업 후 `test_legal_kb_accuracy.py`(97.6%)·`test_persona_kb_qa.py`(73.8%) 모두 회귀 없음 확인. **후속 검토 대상으로 남긴 것**(더 큰 판단이 필요해 이번엔 보류): `main.py`/`engine.py`에 서로 반대 방향으로 동작하는 중복 `_COMPOUND_MAP` 2벌, `engine.py`의 로컬 폴백 persona 인식이 hr/dev/travel 3개만 커버(company/stock/resume 누락), main.py 내 6곳 청크스트리밍·4곳 reference_items 구성 등 중복 패턴, 문서화된 "레포트 작성" 팀 역할(오전7시/저녁10시 정기보고서)의 스케줄러 서브시스템(`run_stock.py`/`schedule_reports`/`start_scheduler`)이 현재 Render 단일 프로세스 배포에는 전혀 연결되어 있지 않아 사실상 동작하지 않는 상태
- 2026-07-09: main.py 내부 중복 로직 4종을 공용 헬퍼로 통합(동작 변경 없음, 코드량 축소 목적) — (1) 청크 스트리밍 반복 패턴 6곳을 `_stream_chunks()` 비동기 제너레이터로 통합. (2) 종목명→ticker 역방향 조회 3곳을 `_ticker_for_name()`으로 통합. (3) KB 답변에서 제목/출처/본문을 분리하는 로직 3곳을 `_split_kb_answer()`+`_format_kb_header()`로 통합(company 복수결과·단일결과·계산결과 보충 3개 경로에서 각각 살짝 다르게 재구현되어 있던 것). (4) persona/시스템프롬프트/오늘날짜 조합 로직 2곳(초기 라우팅 + company KB 우선 라우팅 전환)을 `_build_persona_context()` 클로저로 통합. reference_items 구성(4곳)과 스트림 에러 처리(3곳)는 각각 1~3줄의 단순 반복이라 추가 추상화 실익이 낮아 보존. 통합 후 test_legal_kb_accuracy.py 97.6% 회귀 없음 확인
- 2026-07-09: 감사에서 제기된 두 항목을 실측으로 마무리. (1) `main.py`/`engine.py`의 이름이 겹치는 두 `_COMPOUND_MAP`(반대 방향 변환)을 실제 KB(861개)로 21개 항목 전수 A/B 테스트한 결과, `main.py`의 사용자 질의 사전조인 단계를 제거하면 검색 점수가 4건은 동일, 17건은 최대 19배까지 하락(예: "권고 사직" 0.76→0.04)하는 것으로 확인됨 — 이론적 우려와 달리 실제로는 유해한 중복이 아니라 서로 다른 방향으로 보완하는 의도된 설계였음. 로직은 그대로 유지하고 혼동 방지를 위해 `main.py`의 맵을 `_QUERY_JOIN_MAP`으로 개명 + 두 맵의 관계를 설명하는 주석 추가(통합하지 않음을 명시). (2) `engine.py`의 로컬 폴백(API 키 전부 소진 시 최후 수단) persona 인식이 `detect_persona()`/`_PERSONA_INTRO`/`_THANKS_REPLY`/`_NO_ANSWER` 전부 hr/dev/travel 3개만 지원해 company/stock/resume 대화 시 로컬 폴백에서 일반 안내만 나오던 문제를 페르소나별 맞춤 문구로 확장(7개 페르소나 전부 커버, personas.py의 실제 페르소나 목록과 동기화)
- 2026-07-10: 주간 정기 점검 — 5개 영역 병렬 감사(main/personas/llm/memory, engine/backup/search, stock_analysis 파이프라인, server.js/budget/프론트엔드, 외부 라이브러리 업데이트) 후 발견된 버그·보안 이슈 수정, 브랜치 진단 결과 세션 기본 배정 브랜치(`claude/practical-galileo-kvzcy0`)가 실제로는 main과 동일한 옛 스냅샷이었고 CLAUDE.md가 가리키는 `claude/account-pinned-agents-v6W7p`가 23커밋 앞선 실제 작업 브랜치임을 확인해 후자를 기준으로 작업. **보안 수정**: (1) `/admin/import-db`(임의 SQLite 업로드→운영 DB 병합)·`/admin/learned*`(학습데이터 조회/전체삭제)·`/admin/share*`(company 페르소나 포함 임의 공유링크 발급/폐기) 전 엔드포인트가 `BACKUP_TOKEN` 게이팅이 전혀 없어 `/backup/*`와 달리 인증 없이 데이터 유출·변조가 가능했던 문제를 동일한 `_require_backup_token` 패턴으로 통일(미설정 시 기존처럼 열림+시작 경고 문구에 `/admin/*` 추가). 부수로 `/admin/share/{token}` DELETE의 경로 파라미터명이 인증용 `token` 쿼리파라미터와 겹치던 것을 `share_token`으로 개명. (2) `/health`가 인증 없이 `TURSO_DATABASE_URL`을 그대로 노출하던 것을 `turso_configured: true/false`로 대체. (3) `_require_backup_token`의 토큰 비교를 `hmac.compare_digest`로 상수시간 비교화. (4) `public/index.html`의 프로젝트 메모 목록이 `e.content`/타입 라벨을 이스케이프 없이 `innerHTML`에 삽입해 저장형 XSS가 가능했던 것을 `escapeHtml()` 헬퍼로 수정. **버그 수정**: (5) `main.py`의 stock 페르소나 채팅 경로에서 DDG 검색 학습 호출(`srch.search_and_learn`)에 `persona_id`가 누락되어 2026-06-22에 고친 것과 동일한 버그(모든 웹검색 학습이 `hr`로 고정 저장)가 다른 호출부에 남아있던 것을 수정. (6) 주식 분석 검증기(`validators.py`)가 DART_API_KEY 미설정 시 붙는 샘플데이터 마커(`_note`, "재무제표" 딕셔너리)를 확인하지 않고 "밸류에이션" 딕셔너리만 확인해, 완전히 조작된(고정값) 재무제표로 계산된 목표주가·리스크 점수가 아무 경고 없이 보고서에 노출되던 문제를 발견 — 재무제표 쪽 마커도 확인하도록 추가하고, 그 결과(`주의` 리스트)가 지금까지 리포트에 전혀 노출되지 않던 것(경고/`flags`만 표시)도 `report_writer.py`에 "샘플(모의) 데이터로 계산된 종목" 문구로 노출되게 수정. (7) 같은 파일의 `derived.get(metric) or val.get(metric)` 폴백이 실제 `0`값(부채비율=0 등)을 없는 값으로 취급해 정상범위 검증을 건너뛰던 것을 명시적 `is None` 체크로 수정. (8) `economic_collector.py`가 BOK_API_KEY로 실시간 금리/환율을 받아와도 방향("동결"/"원화강세")·코멘트·주요요인 등 정적 서술 텍스트는 그대로 남아있어 실제 수치와 모순될 수 있던 문제를, ECOS 응답에서 직전 값도 함께 받아와 방향을 실측 비교로 재계산하고(환율은 전일대비도 함께 복원) 정적 코멘트/주요요인은 제거하도록 수정. 글로벌지수·원자재·경제캘린더(원래부터 100% 정적)에도 `_note`를 추가하고 `report_writer.py`의 "시장 환경 분석" 섹션 말미에 데이터 출처 요약 줄을 신설해 실시간/정적 여부가 사용자에게 보이도록 함. (9) `main.py`의 `/learn/document`·`/analyze/resume`가 PDF 파싱(pdfminer/pdftotext, 최대 30초 블로킹)을 `run_in_executor` 없이 직접 호출해 그동안 이벤트루프 블로킹 반복 수정 대상에서 누락돼 있던 것을 동일 패턴으로 수정. **문서/죽은코드 정리**: `ai/backup.py`의 미사용 `import sqlite3` 제거, `securities_report.py`의 정규화 후 절대 매칭될 수 없는 데드 브랜치(`key.replace("비중확대","BUY")`) 제거, `knowledge_dev_coding.py`의 "이번 AI 프로젝트 구조" KB 항목이 실제로는 Google Drive인 백업을 "OneDrive(Microsoft Graph API)"로, 페르소나를 "HR/Dev/Travel" 3개만으로 서술하던 오래된 내용을 현재 코드와 일치하도록 수정. Google Drive 토큰 저장 경로(`GDRIVE_TOKEN_PATH` 기본값 `/tmp`)가 `DB_PATH`와 같은 재배포 초기화 문제를 겪는데도 경고가 없던 것을 동일한 시작 경고 패턴으로 추가. **외부 업데이트 반영**: Groq가 2026-06-17 공지로 `llama-3.3-70b-versatile`(현재 `GROQ_MODEL` 기본값)를 2026-08부터 무료/개발자 티어에서 중단 예정이라 확인, 권장 대체 모델 `openai/gpt-oss-120b`로 기본값 변경(Claude/Zen/OpenRouter가 우선순위상 위에 있어 평소엔 영향 적음). 그 외 Anthropic Claude(`claude-opus-4-8` 최신 유지)·`ddgs`(이미 마이그레이션 완료)·`pykrx`(3.10+ 요구 외 이상 없음)·FastAPI/uvicorn·DART/ECOS API는 특이 변경사항 없음. 전 수정 후 `test_legal_kb_accuracy.py`(97.6%, 회귀 없음)·`test_persona_kb_qa.py`(수정 전후 동일 171건, 회귀 없음) 확인, `/admin/*` 토큰 게이팅·경제수집기 실시간 방향 계산은 서버 기동 후 실제 호출로 동작 검증 완료. **보류(구조적 판단 필요, PR 대상으로 보고만 함)**: server.js(별개의 인사평가 HR-KPI 앱, ai/ 어시스턴트와 무관)의 모든 데이터 변경 엔드포인트(에이전트 고정/메모리 추가삭제)에 인증이 전혀 없는 문제 — 실제 배포/위협모델을 알 수 없어 임의로 인증 스킴을 설계하지 않음. `budget.js`(Node/Express 쪽 예산관리 API)가 `server.js`에 라우터로 마운트되지도 않고 필요 의존성(multer, xlsx)도 package.json에 없어 완전히 죽어있는 상태(직전 커밋이 "삭제된 예산 기능 복원"이라 의도적으로 남겨둔 것일 가능성 있어 임의 삭제하지 않음) — `ai/budget_store.py`+`ai/static/budget.html`(FastAPI 쪽, 정상 동작)과는 별개 구현이므로 계속 쓸지 정리할지 사용자 결정 필요
- 2026-07-15: 실사용 QA(4개 에이전트 병렬, 운영 서버 https://ai-assist-aosk.onrender.com 실제 호출)로 발견한 KB 오염 재발 근본 원인 수정 — `memory.py`의 `_seed_static_kb_to_db()`가 content_hash 기반으로 새 항목만 추가할 뿐, 같은 질문(q)의 답변이 소스 `.py` 파일에서 수정된 경우 예전 버전 DB 행을 전혀 삭제하지 않는 구조였음. 이 때문에 2026-07-08 PII 제거 커밋과 동일자 "출장비" 가상 데이터 제거 커밋이 코드상으로는 이미 고쳐졌음에도, 운영 DB에는 수정 전 예전 행이 그대로 남아있어 검색엔진이 여전히 그 옛 행을 서빙할 수 있는 상태였음(실제로 `/admin/learned` 조회로 PII 5건 id 1632/1648/1661/1666/1667, 출장비 가상데이터 2건 id 661/1805가 최신 수정본과 나란히 존재함을 확인, 1805는 발견 수 분 전에 auto_learn으로 막 재생성된 것). `_seed_static_kb_to_db()`를 재작성 — 현재 `KNOWLEDGE` 리스트로 `(persona, q.lower())→content_hash` 맵을 만들고, DB의 기존 `source='정적KB'` 행 전체를 같은 방식으로 파싱해 실제 해시가 기대 해시와 다르면(=소스에서 내용이 바뀐 경우) 새 버전 삽입과 함께 그 행을 삭제하도록 함(현재 KNOWLEDGE에 없는 질문은 안전하게 건드리지 않음). 로컬 시뮬레이션 테스트(임시 SQLite: 가짜 KNOWLEDGE로 1차 시드 → 내용 변경 → 재시드 → 예전 내용 미존재+새 내용 존재 확인 → 3차 재시드로 멱등성 확인) 전부 통과, `test_legal_kb_accuracy.py`(97.6%)·`test_persona_kb_qa.py`(73.8%) 회귀 없음 확인. 운영 DB의 확인된 stale 행 7건(위 PII 5건+출장비 2건)은 `/admin/learned/{id}` DELETE로 수동 정리 완료(재검색으로 0건 확인). 같은 QA에서 추가로 발견했으나 아직 미수정: `POST /feedback`이 항상 HTTP 500(원인 미확정, `apply_feedback_boost` 또는 `engine.set_feedback_boost` 추정), 일부 `/chat` 질의가 합성되지 않은 원본 내부 지시문+검색 스니펫을 그대로 답변으로 반환(그리고 이게 다시 auto_learn되어 재오염 — id 1805의 실제 발생 경로로 추정), 32,000자 메시지 1건이 전체 앱을 30~90초 이상 멈추게 하는 메시지 길이 제한 부재. 세 건 모두 후속 작업으로 남김
- 2026-07-15: `POST /feedback` 상시 HTTP 500 원인 확정·수정 — `memory.py`의 `_TursoHttpConn._enc()`(Python 값 → Turso Hrana 프로토콜 값 인코딩)가 `float`를 `{"type":"float","value":str(v)}`처럼 문자열로 인코딩하고 있었음. Hrana/Turso v2/pipeline 프로토콜은 `integer`는 64비트 정밀도 보존을 위해 문자열로, `float`는 반대로 순수 JSON 숫자로 인코딩해야 함 — 타입 불일치로 Turso가 해당 바인드 값을 오류 처리하면 `execute()`가 예외를 던지는데, `/feedback` 핸들러에 별도 예외처리가 없어 FastAPI 기본 500으로 노출됨. 전체 스키마에서 `REAL` 컬럼은 `feedback_boost.boost` 하나뿐이고, 이 값을 파라미터로 바인딩하는 곳도 `apply_feedback_boost()`가 유일해 다른 모든 Turso 쓰기(TEXT/INTEGER만 사용)는 영향받지 않고 이 경로만 항상 실패했던 것과 정확히 일치. `_enc()`의 float 분기를 `{"type":"float","value":v}`(숫자 그대로)로 수정. 이 샌드박스에는 Turso 접속 정보가 없어 운영 DB로 직접 재현은 못했으나, `_enc()` 단위 테스트(각 타입별 인코딩 검증)와 로컬 SQLite 경로의 `apply_feedback_boost()` 3연속 호출(좋아요→좋아요→싫어요, boost 값 1.3→1.69→0.676 정상 계산) 테스트, 기존 KB QA 회귀(97.6%/73.8% 동일) 확인 완료. 배포 후 실제 `/feedback` 호출로 최종 검증 필요
- 2026-07-15: `/chat` 프롬프트/컨텍스트 원문 유출 + 재오염(id 1805 등) 근본 원인 확정·수정 — `engine.py`의 `_compose_with_context()`(모든 LLM 제공자(Claude/Zen/OpenRouter/Groq/Gemini)가 동시에 실패·소진되었을 때만 쓰이는 최후의 로컬 폴백)가 `context` 문자열을 자르기만 하고 그대로 반환하고 있었음. `context`는 main.py가 "실제 LLM에게만" 전달하려고 만든 지시문(`[의도 분석] ...답변은 이 의도에 정확히 맞춰 작성하세요.`, `[주의: 아래 참고 자료 중...]`, `[아래 자료를 참고해...]`)과 검색/법령/RAG 원문을 하나로 합친 것이라, 로컬 폴백이 트리거되면 이 내부 지시문 전체 + 미가공 원문 스니펫이 그대로 "AI 답변"으로 사용자에게 노출되고 있었음(QA 2개 에이전트가 독립적으로 재현). 이 미합성 텍스트가 이후 `ai_reply_clean.strip() and not stock_mode` 조건만으로 `mem.auto_learn()`에 그대로 들어가 KB에 저장되는 것도 확인 — 2026-07-15 앞선 항목에서 발견한 "출장비" 재오염 행(id 1805, 발견 수 분 전 생성)이 바로 이 경로로 재생성된 것으로 추정됨. 수정: `_compose_with_context()`에 `_INTENT_LINE_RE`(`[의도 분석]` 태그로 시작하는 한 줄, 태그만 괄호이고 뒤 안내문은 괄호 밖이라 별도 패턴 필요)와 `_INSTRUCTION_BLOCK_RE`(`[주의:...]`/`[아래 자료를 참고해...]`로 시작해 `]`로 끝나는 블록)를 추가해 두 지시문을 원문에서 제거하고, `LOCAL_FALLBACK_MARKER`로 시작하는 명시적 경고("AI 응답 생성 서비스에 일시적으로 연결할 수 없어, 검색된 원본 자료를 그대로 보여드립니다 — AI가 요약·검증하지 않은 원본이므로 참고용으로만 활용하세요")를 답변 앞에 붙이도록 재작성. `main.py`의 auto_learn 호출 조건에 `LOCAL_FALLBACK_MARKER not in ai_reply_clean`을 추가해 이 미합성 폴백 응답이 정상 답변처럼 KB에 재학습되는 것을 원천 차단(실제 정상 라벨이 붙은 `[최신 뉴스·기사]` 같은 표시용 대괄호는 오탐 없이 보존되는 것도 검증). 세 가지 케이스(HR 주의 블록+의도분석, stock 자료참고 블록, 지시문 없는 순수 컨텍스트) 및 `engine.local_stream()` 전체 파이프라인 end-to-end 테스트로 지시문 완전 제거+마커 부착 확인, `test_legal_kb_accuracy.py`(97.6%)·`test_persona_kb_qa.py`(73.8%) 회귀 없음 확인. 이 폴백은 5개 API 제공자가 모두 동시에 실패해야 트리거되는 드문 경로라 이 샌드박스에서 실제 운영 트래픽으로 재현하지 못했으나, 문제의 코드 경로(`_compose_with_context`가 `context`를 가공 없이 그대로 반환)와 QA가 관찰한 증상(내부 지시문 원문 노출 + 재오염)이 정확히 일치함
- 2026-07-15: `/chat` 메시지 길이 무제한으로 인한 DoS 수정 — QA에서 32,000자 메시지 1건이 전체 앱(다른 요청 포함)을 30~90초 이상 멈추게 하는 것을 확인(검색·법령 조회·LLM 호출 파이프라인이 메시지 크기에 비례해 블로킹). `main.py`에 `_MAX_CHAT_MESSAGE_LEN = 4000` 상수 추가, `/chat` 핸들러 최상단(공백 메시지 체크 바로 다음, 다른 처리 이전)에서 길이 초과 시 `HTTPException(400, ...)`으로 즉시 거부하도록 수정 — 무거운 파이프라인에 진입하기 전에 차단되므로 이벤트루프 점유 자체가 발생하지 않음. 4000자는 일반 대화 질문 대비 충분히 넉넉하면서(기존 코드 곳곳의 질문 길이 관례인 500~1000자보다 훨씬 큼) 32,000자급 페이로드는 확실히 차단하는 값으로 선택, 긴 문서는 이미 존재하는 `/learn/document` 업로드 경로를 안내. 이 샌드박스에서 실제 프로덕션에 DoS 페이로드를 재전송해 재확인하지는 않음(운영 서비스에 불필요한 부하를 줄 수 있어 배제) — 코드 검토로 원인·수정 모두 확정
- 2026-07-21: 배포 후 `/feedback`·`/chat` 로컬 폴백 두 수정을 실제 운영 서버(https://ai-assist-aosk.onrender.com)에서 재검증. (1) `/feedback`: 4개 시나리오(신규/upsert 재호출/싫어요/다른 페르소나)를 실제 curl로 호출해 전부 `200 {"ok":true}` 확인, `/feedback/stats`에도 정상 반영됨을 확인 — Turso float 인코딩 수정이 운영에서도 정상 동작. (2) `/chat` 로컬 폴백: 검증을 위해 Render 환경변수 `LLM_PROVIDER=local`을 사용자와 함께 일시적으로 설정해 강제로 폴백 경로를 트리거(`/model-info`로 `fallback_chain: "local"` 전환 확인) → 웹검색 컨텍스트 질의와 law.go.kr 컨텍스트 질의 둘 다에서 `LOCAL_FALLBACK_MARKER` 경고가 정상 표시되고 `[주의: ...]`/`[의도 분석]` 등 내부 지시문 유출이 전혀 없음을 확인, `/admin/learned` 재검색으로 두 응답 모두 KB에 학습되지 않았음(count:0)도 확인. 검증 후 환경변수 삭제해 정상 Claude 체인(`claude → openrouter → groq → local`)으로 원복 완료, 이후 일반 채팅 응답도 정상(Claude 품질) 동작 재확인
- 2026-07-21: 위 재검증 과정에서 사용자가 (LLM_PROVIDER=local 폴백 모드로 강제 전환된 상태에서) 주식 분석 파이프라인 실행 결과를 공유하며 "내용을 더 구체적으로 만들 수 없냐"고 문의 — 조사 결과 `report_writer.py`가 생성하는 전체 보고서에는 이미 종목별 AI 투자의견(Claude 서술형 근거)·매수/매도구간·손절기준·핵심근거가 담긴 「종목별 상세 분석」섹션과, AI 액션 플랜·매수 검토 종목·손절 원칙·모니터링 포인트가 담긴 「오늘의 행동 계획(Action Plan)」섹션이 모두 존재하는데, `main.py`의 `_summarize_stock_report()`(채팅 응답용 요약기)가 특정 종목을 지목하지 않은 일반 "현황" 질문에서는 이 두 섹션을 전혀 추출하지 않고 종합요약·시장환경·TOP추천표·저평가표·증권사컨센서스 5개 섹션만 보여주고 있었던 것이 원인(표에 있는 숫자 나열 이상의 서술적 근거·실행 지침이 빠져 막연하게 느껴짐). `_top_n_stock_detail_lines()` 신설 — 「종목별 상세 분석」섹션이 매매시점 종합점수 내림차순으로 이미 정렬되어 있음을 이용해 상위 3개 종목(▶로 시작하는 블록)만 추출, 일반 질문 경로에 「핵심 종목 상세 (TOP 3)」로 추가. 「오늘의 행동 계획」섹션도 동일하게 일반 질문 경로에 추가. 특정 종목을 지목한 질문(`targets` 있음)은 기존처럼 해당 종목 상세만 보여주므로 변경 없음. 가짜 보고서 텍스트로 두 추출 함수·전체 조합 로직을 end-to-end 시뮬레이션해 상위 3개만 정확히 잘리고 4번째 종목은 제외됨, 액션 플랜 섹션이 정상 포함됨을 확인, `test_legal_kb_accuracy.py`(97.6%)·`test_persona_kb_qa.py`(73.8%) 회귀 없음 확인. 실제 운영 트래픽 재현은 하지 않음(전체 보고서 생성이 종목당 Claude 호출 1회 + 시황요약/액션플랜 각 1회로 유료 API 비용이 발생하는 무거운 파이프라인이라 불필요한 실행을 피함) — 배포 후 일반 "오늘 주식 현황" 류 질문으로 확인 필요
- 2026-08-10 이후(병렬 세션 반영): 예산관리·백업·공유링크·이력삭제 등 관리자 API가 `BACKUP_TOKEN` fail-open(미설정 시 무조건 통과)에서 fail-closed(미설정이어도 토큰 불일치 시 차단)로 강화됨에 따라, 사용자가 `budget.html`에서 매번 관리자 토큰 프롬프트를 마주하는 불편을 겪음("브라우저/주소가 바뀌면 다시 요청되는데 Render에서 매번 값을 확인하기 번거롭다"). `index.html`의 기존 `?share=TOKEN` 공유링크 자동 인식 패턴을 참고해, `budget.html`·`index.html` 양쪽의 `getAdminToken()` 진입부에 `?admin_token=<BACKUP_TOKEN 값>` URL 파라미터를 감지하면 자동으로 `localStorage`(`ai_admin_token`, 두 페이지가 공유하는 키)에 저장하고 `history.replaceState`로 주소창에서는 즉시 제거(북마크 자체는 그대로 유지됨)하는 IIFE 추가 — 이 URL을 북마크해두면 브라우저·기기가 바뀌거나 저장공간이 초기화돼도 다시 Render를 확인할 필요 없이 북마크 클릭만으로 인증됨. Node로 `<script>` 블록 문법 검증 + IIFE 로직 자체를 모킹한 유닛 테스트(파라미터 있을 때 저장+URL 정리, 없을 때 완전 무변화)로 두 케이스 모두 확인. 토큰이 북마크 URL에 평문으로 남는 트레이드오프(브라우저 북마크 동기화 등에 노출될 수 있음)를 사용자에게 고지함 — 개인 단독 사용 관리 도구라는 전제하에 편의성을 우선한 선택
- 2026-08-11: KB 자기개선(자동 정제) 루프 신설 — 사용자가 "주기적으로 스스로 내용을 재정제/통합하는 자기개선 루프가 있었으면 좋겠다"고 요청. 기존 학습 구조는 매 대화마다 실시간으로 저장만 할 뿐 나중에 다시 검토·정리하는 과정이 전혀 없어(코드 확인 결과 스케줄된 재학습·재작성 프로세스 자체가 존재하지 않았음), 유사한 질문이 자동학습/웹검색으로 반복 저장될 때마다 거의 동일한 내용이 여러 행으로 계속 쌓이는 문제가 있었음. 범위는 사용자 선택에 따라 "안전한 정리만"(LLM 미호출, 순수 기계적 판단)으로 제한 — 이 프로젝트에서 실제 반복된 KB 오염 사고(가상 데이터 생성, 주제 불일치 학습 등)가 전부 "AI가 스스로 내용을 지어내는" 경로에서 나왔었기 때문에, 자기개선 루프는 의도적으로 "지우기"만 하고 "새로 쓰기"는 하지 않도록 설계함. `ai/refine.py` 신설: (1) `find_duplicate_clusters()` — 동일 페르소나 내 동적 학습 데이터(정적KB 제외)를 대상으로, 답변(A) 텍스트만으로 TF-IDF 코사인 유사도를 계산해(질문 텍스트까지 섞으면 답변이 짧을수록 질문 어휘가 유사도를 희석시켜 진짜 중복을 놓치는 문제가 실측으로 확인되어 답변만 비교하도록 조정) 임계값(기본 0.85) 이상인 항목들을 Union-Find로 클러스터링, 가장 최신(id 최대) 항목만 남기고 나머지를 제거 대상으로 표시. (2) `find_disliked_questions()` — `feedback_boost.boost`가 임계값(기본 0.1, 연속 싫어요 3회 이상 하향에 해당) 이하로 떨어진 질문에 해당하는 Q&A 항목을 제거 대상으로 표시. `main.py`에 `POST /admin/refine`(apply=false 기본은 미리보기만, true면 실제 삭제 + 엔진 소프트 삭제 동기화 + 해당 feedback_boost 행도 함께 삭제 — 그렇지 않으면 같은 질문이 나중에 더 나은 내용으로 재학습되어도 옛 하향 가중치 때문에 부당하게 계속 억눌리는 문제가 있어서 같이 초기화) 엔드포인트 신설, 기존 `/admin/*`와 동일하게 `BACKUP_TOKEN` 게이팅 적용. 실행 방식은 사용자 선택에 따라 앱 내부 스케줄러가 아닌 외부 트리거 방식 채택(Render 무료 티어가 유휴 시 잠들어 있으면 내부 스케줄러는 예정 시각에 아예 실행되지 않을 수 있음 — 실제로 CLAUDE.md에 이미 기록된 주식 리포트 스케줄러가 이 문제로 사실상 미작동 상태인 전례가 있었음) — `.github/workflows/kb_refine.yml` 신설, `update_laws.yml`(법령 데이터 자동 업데이트)과 동일한 `schedule`+`workflow_dispatch` 패턴으로 매주 일요일 UTC 3시(한국시간 낮 12시)에 curl로 엔드포인트를 호출(Render 콜드스타트 대비 `--retry 2 --max-time 90`), `secrets.BACKUP_TOKEN` 사용. 검증: (a) 근사 중복 클러스터링 함수를 실제 시나리오 3종(같은 답변·다른 질문 표현의 짧은 텍스트 쌍은 클러스터링됨, 완전 무관한 내용은 안 묶임, 질문은 비슷해도 답변이 실질적으로 다른 위험한 케이스는 절대 안 묶임 — 이 세 번째 케이스가 가장 중요한 안전장치)로 개별 검증, (b) 로컬 SQLite로 전체 파이프라인 end-to-end 시뮬레이션(정적KB 861개 + 동적 데이터 6건 시드 → 근사중복 통합·비추천 정리 실행 → 정적KB 861개 완전 보존, 최신 중복 항목만 잔존, 무관 항목 보존, 반복 비추천 항목 삭제, feedback_boost 초기화까지 전부 확인), (c) `.github/workflows/kb_refine.yml`의 YAML 파싱 + 내장 bash 스크립트 문법 검사, (d) `test_legal_kb_accuracy.py`(97.6%)·`test_persona_kb_qa.py`(73.8%) 회귀 없음 확인. 실제 운영 DB 대상 실행은 하지 않음(현재 운영 데이터에 어떤 근사중복이 실제로 있는지 모른 채 대량 삭제를 트리거하는 것은 위험 — 최초 1회는 `apply=false`(미리보기)로 먼저 실행해 무엇이 지워질지 확인 후 사용자 승인 하에 apply=true로 전환할 것을 권장)
- 2026-08-11: `POST /admin/refine` 실제 운영 DB 대상 첫 실행 — 사용자가 직접 `BACKUP_TOKEN`을 전달해 미리보기(apply=false)로 확인 후 승인, apply=true로 적용. 동적 학습 데이터 105건 중 근사 중복 클러스터 5개(모두 내용 직접 대조로 타당성 확인 — 그중 1건은 이전에 발견·삭제했던 "출장비 숙박비 15만원" 가상 데이터의 또 다른 사본, 1건은 같은 날 고친 `/chat` 프롬프트 유출 버그의 흔적)를 삭제, 반복 비추천 0건. 부가로 근사중복 정리가 놓친 프롬프트 유출 잔재 2건(id 1838/1861 — 겉보기엔 비슷해 보이지만 실제 주제가 달라 정제 로직이 정확히 별개로 판단해 하나만 통합하고 남긴 것)을 `/admin/learned/{id}`로 수동 삭제. 105→98건, 검색엔진 인덱스(966→959)도 정상 동기화됨을 재확인. 사용된 `BACKUP_TOKEN`이 대화 로그에 노출되어 사용자에게 교체를 권고함
- 2026-08-11: 자기개선 루프 확장 검토 요청에 따라 3가지 진행. (1) **주식 정기 리포트 스케줄러 복구** — `stock_api.py`의 `start_scheduler`가 `main.py` 어디서도 호출되지 않아 문서화된 "오전7시/저녁10시 정기보고서"가 실제로는 한 번도 실행된 적 없었던 것을(2026-07-09 감사에서 이미 발견했으나 미해결 상태로 남아있던 항목) `kb_refine.yml`과 동일한 외부 스케줄러 패턴으로 해결 — `.github/workflows/stock_report.yml` 신설, 한국시간 07:00/22:00(UTC로 각각 전날 22:00·같은 날 13:00, 주말 제외를 위해 cron day-of-week를 UTC 기준으로 환산: `0 22 * * 0,1,2,3,4`+`0 13 * * 1-5`)에 `POST /stock/analyze` 호출. 이 작업 중 `POST /stock/analyze`·`GET /stock/analyze/sync`가 종목당 Claude 호출이 실제로 발생하는 무거운 파이프라인인데도 인증이 전혀 없어 URL만 알면 누구나 반복 호출해 비용을 유발할 수 있었던 것을 발견 — `/admin/*`와 동일한 `_require_backup_token` 게이팅 추가(main.py의 `/chat` 내부 트리거는 이 HTTP 엔드포인트가 아니라 `run_once()`를 직접 호출하므로 영향 없음 확인). (2) **자기개선 루프에 충돌 후보 보고 기능 추가** — `find_conflicting_pairs()` 신설: `find_duplicate_clusters()`와 반대로 질문 유사도는 높은데(≥0.7) 답변 유사도는 낮은(<0.3) 쌍을 찾아 `/admin/refine` 응답에 `conflicting_pairs_found`/`conflicting_pairs_preview`로 포함만 하고 **절대 자동 삭제하지 않음**(어느 쪽이 맞는지는 사람이 판단할 영역이라 자기개선 루프의 "지우기만" 원칙 밖에 둠). 근사중복(답변 동일)과는 반대 조건이라 서로 겹치지 않음을 실측 확인, apply=true로 실제 삭제까지 실행해도 충돌쌍은 삭제 대상에 전혀 포함되지 않음을 end-to-end 테스트로 검증. (3) budget.js 삭제는 별도 커밋으로 진행 예정
- 2026-08-11: budget.js(Node/Express) 및 연관 죽은 코드 삭제 — `server.js`에 라우터로 마운트된 적이 없고 필요 의존성(multer, xlsx)도 `package.json`에 없어 애초에 동작 불가능한 상태였음(2026-07-10 감사에서 발견 후 "다른 용도로 남겨둔 것일 가능성"을 우려해 보류해뒀으나, 이번에 사용자 확인 후 삭제 진행). 삭제 범위를 budget.js뿐 아니라 짝을 이루는 프론트엔드 `public/budget.html`까지 넓힘 — `public/`은 `server.js`가 `express.static`으로 서빙 중이라 URL로 직접 접근은 가능했지만, 그 안의 모든 버튼이 `/api/budget/*`(=budget.js가 제공했어야 할 라우트)를 호출해 마운트 누락으로 전부 404가 나는, 사실상 겉모습만 있는 죽은 페이지였음. budget.js가 읽고 쓰던 루트의 `budget-data.json`도 내용이 빈 기본값(`{"headcount":[],"items":[],"uploads":[]}`)뿐이라 데이터 손실 위험 없이 함께 삭제. 실제 예산관리 기능은 `ai/budget_store.py`+`ai/static/budget.html`(FastAPI, DB 영속 저장)로 완전히 별개로 정상 동작 중이라 영향 없음. 삭제 전 레포 전체에서 세 파일에 대한 참조(require, 링크, fetch 호출 등)가 전무함을 확인, 삭제 후 `server.js` Node 문법 검사 통과 확인
- 2026-08-11: `server.js`(별개의 인사평가 HR-KPI 앱) 데이터 변경 API 인증 추가 — 2026-07-10 감사에서 발견 후 실제 배포 방식을 몰라 인증 스킴을 임의로 설계하지 않고 보류해뒀던 항목을 사용자 확인 후 진행. 에이전트 고정/해제(POST·DELETE `/api/accounts/:id/pinned-agents/:agentId`), 프로젝트 메모리 추가·삭제(POST `/memory/context`·`/memory/decisions`, DELETE `/memory/context/:entryId`) 5개 엔드포인트에 인증이 전혀 없어 누구나 계정 데이터를 마음대로 바꿀 수 있었음. `ai/main.py`의 `BACKUP_TOKEN`과 동일한 방식(쿼리 파라미터 `?token=`, 환경변수 `AGENT_ADMIN_TOKEN` 미설정 시 fail-closed로 403, `crypto.timingSafeEqual`로 상수시간 비교)으로 통일해 `requireToken` 미들웨어 신설, 조회(GET) API는 그대로 토큰 없이 열어둠. 로컬에서 실제로 서버를 띄워 검증(토큰 미설정 시 시작 경고 로그+모든 변경 API 403, 토큰 없음/오답 시 403, 정답 토큰 시 정상 동작, GET은 토큰 무관하게 항상 동작 — 4가지 시나리오 전부 curl로 실측), 테스트 중 `data.json`에 쓰인 테스트 데이터는 사전 백업 후 원상복구해 실데이터 오염 없음을 `git diff` 무변경으로 확인
- 2026-08-11: "개인 활용 AI 답변 정확성·정밀화" 고도화 요청에 따라 `test_persona_kb_qa.py` 실패 171건(73.8% 통과)을 실제로 분석 — 놀랍게도 전체 실패의 100%(163건 edge_compound + 8건 partial)가 붙여쓰기(공백 없이 단어 두 개를 이어붙인 입력, 예: "면접에서연봉을") 미분리 문제였음. 이미 법률 KB에서 92.8%→97.6%로 끌어올린 것과 정확히 같은 근본 원인(`_COMPOUND_MAP`에 해당 페르소나 항목이 없어 토큰화 단계에서 실패)임을 확인, 사람이 163개를 일일이 손으로 추가하는 대신 스크립트로 자동 생성 — 각 KB 항목의 질문 앞 두 어절을 실제 테스트가 생성하는 것과 동일한 방식으로 붙인 뒤(`"".join(toks[:2])`) 현재 엔진으로 검색해 실패하는 것만 골라 `글자수 내림차순`으로 `_COMPOUND_MAP`에 추가(총 159개 자동 생성 + 기존 4개는 이미 커버됨). 추가 전 기존 맵과의 부분 문자열 충돌을 전수 검사해 실제 충돌 3건(`노사협의회`가 `노사협의회에서협의하거나`/`노사협의회회의는`보다 먼저 치환되는 문제, `프로젝트`가 `모노레포프로젝트`보다 먼저 치환되는 문제)을 발견 — `str.replace()`를 사전 순서대로 연쇄 적용하는 구조라 짧은 규칙이 먼저 오면 긴 규칙의 매칭 대상이 사라짐(주석으로 이미 문서화되어 있던 원칙). 이 3건을 충돌 대상보다 앞쪽으로 재배치하고 뒤쪽의 중복 항목 제거. 결과: 653개 쿼리 중 통과 171→634(73.8%→97.1%), 페르소나별로 company 81.0%→96.8%·dev 66.7%→97.7%·travel 67.7%→95.8%·resume 72.0%→98.1%. 잔여 실패 19건은 전부 KB 내 서로 다른 항목이 첫 두 어절만으로는 구분 안 되는 진짜 주제 중복(예: "해외출장"으로 시작하는 항목이 3개라 2어절 붙여쓰기만으론 어느 것인지 특정 불가)으로, 토크나이저 버그가 아니라 자동 QA 쿼리 생성 방식 자체의 근본적 한계라 판단해 추가로 손대지 않음(법률 KB의 97.6%와 동일한 성격의 잔여 노이즈). `test_legal_kb_accuracy.py`(97.6%, 회귀 없음) 확인
- 2026-08-11: 동의어 그룹(`_SYNONYM_MAP`) 신중히 확장 시도 — 근거 없이 후보를 나열하지 않고, 실제 KB 내용에서 이미 혼용 중인 단어를 찾아 검색 실패로 이어지는지 먼저 실측 검증하는 방식으로 진행. "자소서"/"자기소개서"는 KB q필드에 이미 둘 다 병기되어 있어 추가 효과 없음 확인(스킵). "인스톨"(로컬 폴백 없이 순수 검색 테스트)은 "도커 인스톨"·"깃 인스톨 어떻게 해"가 score 0.0(완전 미스)임을 확인해 `["설치", "인스톨"]` 추가 — 회귀 없이 반영. "깃"(Git의 한글 표기)은 별도로 `["깃", "git"]`을 추가해봤으나, `_tok()`의 2글자 미만 토큰 제거 규칙(`len(w) < 2`) 때문에 "깃"이라는 토큰 자체가 애초에 안 만들어져 동의어 매핑이 적용될 기회조차 없음을 실측으로 확인 — 효과 없는 죽은 항목이라 제거(토크나이저의 1글자 단어 제거 규칙 자체를 고치는 건 전체 페르소나에 영향을 주는 더 큰 변경이라 범위 밖으로 남김). "도커"(Docker의 한글 표기)는 `["도커", "docker"]` 추가 시 "도커 인스톨"(score 0.0→0.5대) 문제는 고쳤지만, 전역 동의어 규칙이 그 토큰이 등장하는 모든 문서의 벡터에 영향을 준다는 TF-IDF/코사인 유사도의 특성 때문에 도커/깃 언급이 전혀 없는 "설치 기본 사용법" 질의가 기존엔 정확히 Docker 항목을 찾다가 Git 항목으로 뒤집히는 회귀가 실측으로 확인되어(0.345>0.283 → 0.249<0.283) 제외 — 자체 회귀 검증 기준을 통과하지 못한 변경은 그대로 반영하지 않고 되돌림. 최종적으로 `["설치","인스톨"]` 1건만 반영, `test_persona_kb_qa.py`(97.1%, 회귀 없음)·`test_legal_kb_accuracy.py`(97.6%, 회귀 없음) 확인. "도커" 문제는 전역 동의어 규칙 대신 KB q필드에 직접 "도커"를 병기하는 국소적 방식(레거시 KB 항목들이 자소서/자기소개서처럼 이미 쓰고 있는 패턴)으로 풀 수 있는지 후속 검토 대상으로 남김
- 2026-08-11: "자료 누적"(외부 자료를 주기적으로 더 가져와 KB를 풍부하게 만드는 것) 방향 검토 후 travel 페르소나에 적용 — dev는 정적 KB가 이미 안정적 기초를 잘 다루고 있고 최신 동향은 기존 웹검색(`search_and_learn`)이 반응형으로 더 적절히 처리해 뚜렷한 후보를 못 찾음(도입 보류). travel은 외교부 해외안전여행 여행경보(공공데이터포털, https://www.data.go.kr/data/15076237/openapi.do)가 시의성·권위성·관련성 모두 갖춘 후보로 확인 — `fetch_laws.py`+`update_laws.yml`과 동일한 패턴으로 `ai/fetch_travel_alerts.py`+`.github/workflows/update_travel_alerts.yml`(매주 목요일, `law` 워크플로우와 요일 분리) 신설. 실제 API 키 없이 작업해야 했던 제약 때문에(사용자에게 `TRAVEL_ALERT_API_KEY` 발급을 요청한 상태) 다음 단계로 검증: (1) 웹서치로 정확한 데이터셋(15076237)·엔드포인트(`apis.data.go.kr/1262000/TravelAlarmService2/getTravelAlarmList2`)·파라미터명(ServiceKey/returnType/numOfRows/pageNo) 확인, (2) 실제 키 없이 해당 엔드포인트를 직접 호출해 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`라는 구체적 오류가 돌아오는 것을 실측 확인 — 엔드포인트·파라미터명 자체가 정확함을 방증(존재하지 않는 엔드포인트/파라미터였다면 404나 일반 오류가 났을 것). 다만 실제 키로 받는 성공 응답의 정확한 필드 구조(country_nm/alarm_lvl/written_dt 등, data.go.kr 표준 포맷 response.body.items.item 가정)까지는 이 환경에서 검증 못함 — `_extract_items()`가 예상과 다른 구조를 만나면 원본 응답을 그대로 출력하도록 방어적으로 작성해 실제 키 확보 후 1회 조정으로 대응 가능하게 함, `fetch_laws.py`와 동일하게 실패 시 기존 캐시 유지 + 워크플로우는 경고로 종료(빨간불 아님). `engine.py`의 `_load_knowledge()`에 `travel_alert_cache.json` 로드 블록 추가(law_data_cache.json과 동일 패턴), 가짜 캐시 데이터로 로드→검색 전체 파이프라인 end-to-end 테스트 통과 확인, `test_legal_kb_accuracy.py`(97.6%)·`test_persona_kb_qa.py`(97.1%) 회귀 없음 확인. `.env.example`에 `TRAVEL_ALERT_API_KEY` 안내 추가. **사용자 액션 필요**: data.go.kr에서 무료 활용신청으로 키 발급 후 GitHub Actions 시크릿에 `TRAVEL_ALERT_API_KEY`로 등록해야 실제 동작 시작, 최초 실행 결과로 성공 응답 구조 검증 필요. **(2026-08-28 추가 기록 참고: 이 워크플로우도 아래에 기록된 "기본 브랜치 미존재" 문제로 실제로는 아직 한 번도 실행되지 않았을 가능성이 높음 — 별도 확인 필요)**
- 2026-08-28: 주간 정기 점검(claude/account-pinned-agents-v6W7p) — 코드 전수 점검(Python 전체 py_compile, 3개 HTML의 인라인 JS 문법검사, 회귀 테스트 2종) 전부 통과 확인 후, 웹 검색으로 LLM 제공자 최신 상태를 재확인하는 과정에서 실제 버그 발견·수정. `ai/llm.py`의 Gemini 폴백 기본 모델(`gemini-2.0-flash-lite`/`gemini-2.0-flash`)이 2026-06-01자로 완전히 서비스 종료된 것을 확인(웹 검색으로 재현·검증) — 5단계 폴백 체인(Claude→Zen→OpenRouter→Groq→Gemini→local) 중 5번째 단계가 항상 실패하는 죽은 항목이었음. 후속 GA 모델(둘 다 무료 티어)로 교체: `GEMINI_MODEL`→`gemini-3.5-flash-lite`, `GEMINI_CODING_MODEL`→`gemini-3.6-flash`. 참고로 `ai/stock_analysis/utils/claude_client.py`의 `MODEL = "claude-opus-4-8"`도 오래된/잘못된 이름처럼 보여 의심했으나, 웹 검색으로 2026-05-28 출시된 실제 유효 모델임을 확인해 그대로 둠(검증 없이 고쳤다면 오히려 회귀였을 사례).
- 2026-08-28: 위 점검 중 훨씬 더 큰 문제를 발견 — `.github/workflows/kb_refine.yml`(주간 KB 자기개선)과 `stock_report.yml`(평일 주식 정기 보고서, 2026-08-11에 "복구 완료"로 문서화됨)이 GitHub Actions 워크플로 목록(`list_workflows` API)에 아예 나타나지 않는 것을 발견 — 두 파일 모두 `claude/account-pinned-agents-v6W7p` 브랜치에만 커밋됐을 뿐, 저장소의 실제 기본 브랜치(`main`이었다가 최근 `hr-production`으로 전환됨, 둘 다 확인)에는 단 한 번도 존재한 적이 없었음. GitHub의 `schedule` 트리거는 오직 "그 시점의 기본 브랜치에 있는 워크플로 파일"만 평가하므로, 이 두 스케줄은 2026-08-11 생성 이후 지금까지(2026-08-28) **단 한 번도 실제로 실행된 적이 없음** — 2026-07-09에 이미 한 번 겪었던 "스케줄러가 실제로는 한 번도 안 돌고 있었다"는 것과 정확히 같은 클래스의 사고가, 그 수정(내부 APScheduler→외부 GitHub Actions) 자체에도 그대로 재발한 것. `update_laws.yml`은 처음부터 두 브랜치 모두에 존재해 정상적으로 14회 실행됨을 실제 실행 이력으로 확인. 이 저장소의 기본 브랜치가 HR/ERP 시스템(완전히 무관한 다른 앱) 소유의 보호된 브랜치(PR+CI 필수)라 직접 push는 불가능하고 이 브랜치의 변경만으로는 근본적으로 해결이 안 되는 구조적 문제라 판단, `fix/enable-ai-assistant-scheduled-workflows` 브랜치로 두 워크플로 파일(무수정, 이미 YAML 검증·시뮬레이션 완료된 것)을 `hr-production`에 추가하는 PR을 별도로 생성. 같은 날 이후 추가된 `update_travel_alerts.yml`도 동일하게 `claude/account-pinned-agents-v6W7p`에만 존재해 같은 문제를 겪고 있을 것으로 추정 — 위 PR 대상에 함께 포함 필요
- 2026-08-30: 위에서 추정만 했던 `update_travel_alerts.yml`의 기본 브랜치 미존재 문제를 실제로 확인하고 PR #29(`fix/enable-ai-assistant-scheduled-workflows` → `hr-production`)에 추가 완료 — worktree로 그 브랜치를 체크아웃해 `update_travel_alerts.yml`뿐 아니라 `ai/fetch_travel_alerts.py`도 함께 커밋해야 함을 확인(이 워크플로는 `kb_refine.yml`/`stock_report.yml`처럼 배포된 서버를 curl로 호출하는 게 아니라 `update_laws.yml`과 같이 CI 러너에서 직접 스크립트를 실행하는 방식이라, 워크플로 파일만으로는 실행 시 스크립트를 못 찾아 실패했을 것). 이 과정에서 훨씬 더 근본적인 문제를 추가로 발견: `law_data_cache.json`이 `claude/account-pinned-agents-v6W7p`·`hr-production` 두 브랜치 모두에서 여전히 3바이트(사실상 빈 상태)이고 마지막 수정일도 각각 2026-05-28·2026-06-30으로 멈춰있음 — `update_laws.yml`이 "14회 정상 실행"으로 기록된 것과 모순됨. 원인으로 강하게 의심되는 것: `fetch_laws.py`는 `LAW_API_KEY` 미설정 시 다운로드를 조용히 건너뛰고 `exit(0)`으로 종료하도록 설계되어 있어(선택 기능 미설정을 빨간불로 만들지 않으려는 의도), `LAW_API_KEY` 시크릿이 애초에 등록되지 않았다면 14번의 "성공"이 전부 아무 데이터도 못 가져온 채 조용히 통과한 것일 수 있음 — 아직 시크릿 등록 여부 자체는 확인하지 못함(리포지토리 시크릿 값은 조회 불가, 이름 목록도 이번 세션 권한으로는 확인 못함). 더 구조적인 문제: 설령 키가 있어 fetch가 성공해도, `update_laws.yml`/`update_travel_alerts.yml`은 결과 JSON을 "워크플로가 실행된 브랜치"(스케줄 트리거는 항상 그 시점의 기본 브랜치, 현재 `hr-production`)에 커밋하는데, 실제 Render 배포는 `claude/account-pinned-agents-v6W7p`에서 이루어지는 것으로 이 세션 내내 실측 확인됨(예: `/feedback`·로컬 폴백 마커 수정이 정확히 이 브랜치 배포분에서 라이브로 확인됨) — 즉 두 브랜치가 다르면 CI가 갱신한 캐시가 애초에 실제 서비스에 절대 반영되지 않는 구조적 결함일 가능성이 있음(반대로 `kb_refine.yml`/`stock_report.yml`은 결과를 git에 커밋하지 않고 배포된 HTTPS 엔드포인트를 직접 호출하므로 이 문제에서 자유로움). 이 세션에서는 시간 관계상 원인 확정(시크릿 실제 등록 여부 확인)과 구조 개선(예: JSON 커밋 방식 대신 kb_refine처럼 배포된 서버에 직접 반영하는 관리자 엔드포인트로 전환)까지는 진행하지 않음 — 사용자에게 보고 후 후속 진행 여부 확인 필요
- 2026-08-30: 위 두 문제를 사용자 요청으로 실제 확인 — GitHub Actions 실제 실행 로그(2026-08-24 update_laws.yml run)를 직접 조회해 결론을 냄. **(1) LAW_API_KEY 미설정 가설은 틀림** — 로그의 env 블록에 `LAW_API_KEY: ***`로 마스킹되어 표시되어 시크릿이 실제로 등록되어 있음을 확인(값 자체는 볼 수 없지만, 미설정이면 애초에 이 줄 자체가 안 뜨거나 함수가 즉시 스킵해 실행시간이 1초 미만이었을 것 — 실제로는 65초 소요되며 실제 API 호출들이 있었음). **(2) 대신 완전히 다른 진짜 원인을 로그에서 직접 발견** — `fetch_laws.py`의 `_get_mst()`가 법령 검색 응답에서 MST(법령 고유번호)를 `법령MST`/`법령MST번호`/`MST`라는 필드명으로 찾는데, 실제 law.go.kr API 응답에는 이런 필드가 아예 없고 `법령일련번호`라는 필드에 들어있음(로그에 찍힌 실제 응답 예시로 확인: `법령상세링크`의 쿼리파라미터 `MST=218303`과 `법령일련번호: '218303'`이 동일한 값임을 대조). 이 때문에 8개 법령 중 앞 2개(근로기준법·근로자퇴직급여보장법)는 이번 실행에서 우연히 DNS 조회 실패(`Temporary failure in name resolution`, GitHub Actions 러너의 일시적 네트워크 문제로 추정 — 같은 실행의 나중 호출들은 정상 연결됨)로 걸러졌고, 나머지 6개는 API 연결과 응답 자체는 성공했는데도 `_get_mst()`가 빈 문자열을 반환해 매번 "MST 값 없음"으로 0개 조문만 저장하고 끝났던 것 — **처음 만들어진 이후 지금까지 단 한 번도 실제로 법령 원문을 가져온 적이 없었던 진짜 근본 원인**이 이것이었음(LAW_API_KEY 문제가 아니라 필드명 파싱 버그). `_get_mst()`가 확인하는 키 목록 맨 앞에 `법령일련번호`를 추가해 수정, 로그에서 캡처한 실제 응답 dict로 수정된 함수가 정확히 MST를 추출함을 확인. **(2)번 문제(브랜치 불일치)는 여전히 유효** — 이 커밋만으로는 해결 안 됨: `update_laws.yml`(및 신설한 `update_travel_alerts.yml`)이 결과를 커밋하는 브랜치는 그 시점의 저장소 기본 브랜치(현재 `hr-production`)인데, Render가 실제로 배포하는 브랜치는 `claude/account-pinned-agents-v6W7p`로 이번 세션 내내 실측 확인되어 있어, MST 버그가 고쳐져 데이터가 성공적으로 저장되어도 그 갱신분이 실제 서비스에 자동으로 반영되지는 않음 — 두 브랜치 사이에 캐시 파일을 동기화하는 별도 절차가 없다면 여전히 구조적으로 막혀있음. 이 MST 수정은 `claude/account-pinned-agents-v6W7p`에 우선 반영하고, PR #29가 다루는 `hr-production`행 `fetch_laws.py`에도 동일 수정을 반영해야 함(다음 커밋에서 진행)
- 2026-08-30: 브랜치 불일치 문제(문제 2)의 근본 해결 — 사용자가 "두 브랜치를 합치는 게 나을지" 문의해 위험성(hr-production은 별도 도구가 개발 중인 실제 HR/ERP 앱을 함께 호스팅하고, `ai/` 쪽도 이번 세션 동안 독립적으로 계속 발전해 병합 시 실충돌 가능성이 높음)을 설명 후, 대신 "git 커밋 방식 자체를 없애고 kb_refine.yml/stock_report.yml처럼 배포된 서버를 직접 호출하는 방식으로 전환"하는 더 작은 범위의 해법으로 합의. `main.py`에 `POST /admin/refresh-law-cache`·`POST /admin/refresh-travel-alerts` 신설 — `fetch_laws.py`/`fetch_travel_alerts.py`의 기존 fetch 함수(`fetch_one_law`/`fetch_all_alerts`)를 그대로 재사용해 배포된 서버 프로세스 안에서 직접 API를 호출하고, 결과를 `_replace_source_knowledge()`(신설 헬퍼)로 해당 source의 기존 DB 행을 전부 지운 뒤 새로 저장 + 라이브 엔진에도 즉시 반영(재시작 불필요). 매번 전체를 다시 가져오는 권위 있는 외부 자료 특성상 "완전 교체" 방식이 적합 — 이전 실행엔 있었는데 이번엔 없는 항목(예: 경보 해제된 국가)도 자동으로 사라짐을 로컬 시뮬레이션(1차 3개국→2차 미얀마 유지·예멘 삭제·아이티 내용 갱신·시리아 신규, DB 행 수 중복 누적 없이 3개 유지, 엔진 검색으로도 확인)으로 검증. `engine.py`의 `_load_knowledge()`에서 이제 쓸모없어진 `law_data_cache.json`/`travel_alert_cache.json` 파일 로딩 블록 제거 — 이 두 source는 "동적 학습 데이터 복원" 블록이 재시작 시 DB에서 자동으로 함께 복원하므로 별도 블록 불필요. `update_laws.yml`/`update_travel_alerts.yml`을 git 커밋 방식에서 kb_refine.yml과 동일하게 "새 엔드포인트를 curl로 호출만" 하는 방식으로 재작성(체크아웃·Python 설치·git 커밋 단계 전부 제거, 코드가 훨씬 단순해짐) — 이제 어느 브랜치에서 워크플로가 트리거되는지가 결과에 전혀 영향을 주지 않음. `LAW_API_KEY`/`TRAVEL_ALERT_API_KEY`는 이제 GitHub Actions 시크릿이 아니라 **배포된 서버(Render) 환경변수**로 설정해야 함(수집이 서버 프로세스 안에서 일어나므로) — `.env.example`에 안내 갱신. `test_legal_kb_accuracy.py`(97.6%)·`test_persona_kb_qa.py`(97.1%) 회귀 없음 확인. **사용자 액션 필요**: Render 환경변수에 `LAW_API_KEY`·`TRAVEL_ALERT_API_KEY` 설정(둘 다 각 기관 사이트에서 무료 발급), 배포 후 `POST /admin/refresh-law-cache`·`/admin/refresh-travel-alerts`를 한 번 수동 호출해 실제 성공 여부 확인 권장(이 세션엔 두 키 모두 없어 실제 API 응답으로 최종 검증은 못함 — `_get_mst()` 필드명은 실제 로그로 검증됐고 나머지 로직은 로컬 시뮬레이션으로 검증)
- 2026-08-27: 개인 AI 장기기억 개선의 1순위인 **기억 안전성 기반** 구현. 일반 소유자 채팅도 `BACKUP_TOKEN`을 헤더로 검증하고, 대화에 브라우저 `session_id` 범위를 추가해 소유자·공유 링크·브라우저 간 이력이 섞이지 않도록 분리했다. 공유 링크는 허용 페르소나 채팅만 가능하고 학습·백업·피드백·관리 UI/API 쓰기는 차단했다. 기존 `auto_learn()`은 답변을 즉시 `learned_knowledge`에 영구 저장하던 방식에서 `memory_candidates(status='pending')` 후보 저장으로 전환했으며, 소유자 UI/API에서 승인한 항목만 `source='승인학습'`으로 장기기억에 승격된다. 자기정제는 영구 삭제 대신 `memory_quarantine`으로 이동해 복구 가능하게 하고, 직접입력→승인학습→문서→자동학습 순으로 보존 우선순위를 적용했으며, Union-Find 연쇄 병합을 모든 구성원이 서로 유사해야 하는 보수적 클러스터링으로 교체했다. 정기 GitHub Actions는 `apply=false` 미리보기만 수행하고 실제 격리는 수동 승인 시에만 실행한다. 새 테이블은 전체 SQL/JSON 백업과 Turso 이관 대상에 포함했으며 `test_memory_safety.py`로 구버전 DB 열 추가, 세션 격리, 후보 승인, 격리·복구, 출처 우선순위를 검증했다.
- 2026-08-31: 기억 안전성 1순위 배포 전 보강. 기존 `/admin/learned/duplicates?apply=true`가 같은 내용을 페르소나 구분 없이 영구 삭제하던 경로를 `(persona, content)` 단위 판정 + 신뢰 출처 우선 + 격리 방식으로 교체하고, 개별 `/admin/learned/{id}` 정리도 영구 삭제 대신 격리·전체 엔진 재구축으로 통일했다. `migrate_to_turso.migrate()`는 빈 목적지에서도 `init_db(seed_static=False)`로 새 안전 스키마를 먼저 생성한 뒤 구버전 데이터를 이관하고 마지막에 정적 KB를 시딩하도록 해 단독 복원과 중복 방지를 함께 보장했다. `test_api_memory_safety.py`로 무인증 채팅/학습/기억관리 차단, 공유 허용 페르소나, 공유/소유자 이력 격리, 공유 응답 학습 차단, 페르소나별 중복 격리·복구를 실제 FastAPI 요청으로 검증했고, `test_migration_memory_safety.py`로 구버전 DB→빈 DB 복원을 검증했다. 최신 운영 브랜치 재배치 후 법률 KB 446/456(97.8%) 및 범용 페르소나 QA 634/653(97.1%)로 회귀 없음.
- 2026-08-31: 장기기억 개선 2순위인 **문서 의미 청킹·검색 다양화** 구현. 기존 500자 고정 슬라이딩(100자 중첩)이 문장·제목·표를 중간에서 끊던 방식을 `chunk_document()`의 제목/문단/문장 경계 기반 청킹으로 교체하고, 각 청크에 `[문서]`·`[섹션]` 출처 맥락과 제한된 앞 문맥을 포함했다. 동일 문서 재업로드 시 이전 청크를 영구 삭제하지 않고 격리하며, 빈 파일/파싱 실패는 기존 정상 버전을 건드리지 않는다. 검색은 요청 개수보다 넓은 후보군을 받은 뒤 토큰 Jaccard 0.82 이상인 거의 같은 청크를 제거해 겹침 청크가 컨텍스트를 독점하지 않도록 했다. `test_document_chunking.py`로 제목·표 보존, 장문 분할, 겹침 문맥, 검색 다양화, 재업로드 격리, 빈 재업로드 무손실을 검증했고 법률 KB 정확도 97.8%를 유지했다.
- 2026-08-31: 장기기억 개선 3순위인 **학습 후보 품질 대기열** 구현. `/chat`이 이미 KB에서 직접 찾은 답변·Python 계산 결과·company 무응답 안내를 다시 후보로 쌓지 않고 실제로 새로 합성한 답변만 후보화하도록 제한했으며, 생성 경로를 `생성답변`/`KB보강생성`/`웹검색보강`/`법령실시간`으로 기록한다. `score_memory_candidate()`가 길이, 질문-답변 주제 일치, 근거 표시, 구조화, 불확실성 표현, 시점 변동 위험을 설명 가능한 0~1 점수와 경고 목록으로 계산해 승인 UI에 표시한다(점수로 자동 승인하지는 않음). 같은 질문의 정적KB·직접입력·문서 지식이 있으면 후보 승격을 409 충돌로 중단해 높은 신뢰도 지식을 보호한다.
- 2026-08-31: 답변 품질 개선 4순위인 **안전한 2단계 추론** 구현. 기존 deep thinking은 첫 번째 LLM 호출의 상세 작업 분석을 `<think>`로 브라우저에 그대로 노출했으나, 이제 1차 분석은 내부 작업 메모로만 수집하고 사용자에게는 검토 완료 사실만 짧게 표시한다. 2차 최종 답변은 내부 초안을 그대로 신뢰하지 않고 참고 자료와 재대조해 근거가 확인된 내용만 사용하도록 검증 규칙을 추가했고, 내부 초안 길이를 3,000자로 제한하며 로컬 폴백 원문 덤프는 최종 컨텍스트에서 제외한다. 단일 호출 prompt thinking도 상세 사고과정 대신 공개 가능한 요구사항·근거 범위·불확실성 요약만 출력하도록 변경했다. `test_reasoning_safety.py`로 내부 분석 비노출, 최종 검증 컨텍스트 사용, 폴백 마커 제거를 검증했다.
- 2026-08-31: 1~4순위 통합 배포 준비 점검에서 공유 채팅이 `auto_learn` 후보 생성은 막아도 `search_and_learn()`을 통해 웹검색 스니펫을 장기 DB에 직접 쓰는 우회 경로를 발견해 차단했다. 일반/공유 채팅은 모두 `web_search()`로 조회만 하고, 합성 답변은 소유자 세션에서만 승인 후보가 되며, 원문 웹검색을 영구 학습하는 `/learn/search`는 토큰이 필요한 명시적 관리자 기능으로만 남겼다. 공유 링크에서는 주식 전체 분석·저가주 스크리닝·증권사 단독 리포트 같은 고비용 생성 트리거도 실행하지 않는다. 실제 FastAPI 공유 웹검색 모킹 테스트로 `learned_knowledge`·`memory_candidates`가 증가하지 않음을 확인했다. 전체 검증: Python 전체 compileall, 기억/이관/API/문서청킹/추론 안전 테스트 PASS, 법률 KB 446/456(97.8%), 범용 페르소나 634/653(97.1%) 최신 운영 기준 유지, 프런트엔드 JS 문법 및 `git diff --check` 통과. 운영 브랜치 푸시/배포는 수행하지 않음.
- 2026-08-31: 장기기억 추가 개선으로 **민감정보 방어·반복 근거·검색 관측·보존정책**을 구현했다. `privacy.py`가 주민등록번호, Luhn 검증 카드번호, Bearer/대표 API 토큰, 비밀번호·비밀키 할당을 찾아 대화·피드백·문서·직접지식 저장 전에 마스킹하며, 자동 기억 후보는 이메일·전화번호를 포함한 개인정보가 하나라도 있으면 생성하지 않는다. 동일 질문·답변 후보는 원자적 UPSERT로 `seen_count`/`last_seen_at`을 누적하고 반복 관찰 횟수를 품질 점수·승인 UI에 표시하되 자동 승인은 하지 않는다. `memory_retrieval_events`는 질문 원문과 세션 ID 대신 `MEMORY_TELEMETRY_SALT`(미설정 시 `BACKUP_TOKEN`) 기반 HMAC-SHA256, owner/share 구분, 점수·출처·라우팅만 저장하며 `/admin/memory-observability`에서 최근 저신뢰율·컨텍스트 사용률·출처 분포를 조회한다. `/admin/memory-retention`은 기본 미리보기이고 실제 적용은 `apply=true&confirm=PURGE_EXPIRED`가 모두 있어야 하며, 승인된 장기지식과 활성 격리 기억은 삭제하지 않는다. 기본 보존기간은 대화 90일, 미검토 후보 45일, 후보 처리 이력 365일, 검색 관측 30일이고 환경변수로 조정 가능하다. 새 열·관측 테이블은 Turso 자동 마이그레이션, ZIP/JSON 백업, SQLite→Turso 이관에 포함했다. 신규 `test_memory_privacy_observability.py`와 확장된 FastAPI 테스트를 포함한 전체 안전 테스트 통과, 법률 446/456(97.8%)·페르소나 634/653(97.1%) 및 프런트엔드 JS 문법 회귀 없음.
- 2026-08-31: 장기기억 고도화 1번인 **출처·검증·유효기간·모순 감지**를 구현했다. `learned_knowledge`와 승인 후보에 정규화된 근거 JSON, 검증 시각, 유효기한을 저장하고 법령 30일·여행경보 7일·웹검색 14일·생성답변 90일·승인지식 180일의 출처별 기본값을 환경변수로 조정할 수 있게 했다. 만료 기억은 DB에서 지우지 않되 검색 답변 근거에서는 자동 제외하며 `/admin/memory-revalidation` 목록과 `/admin/learned/{id}/verify`로 관리자 재검증·기한 연장을 지원한다. 후보 승인 전에 같은/유사 질문의 기존 기억과 수치·반대 의미·답변 급변을 탐지해 409로 보류하고, UI에서 근거·기한·충돌 사유를 확인한 경우에만 별도 강제 승인이 가능하다. 단, 정적KB·직접입력·문서 지식은 강제 플래그로도 자동 덮어쓸 수 없다. 외부 근거 URL의 민감 쿼리는 제거하며, 격리·복구·Turso 이관·엔진 재시작에도 모든 출처 메타데이터가 보존된다. `test_memory_provenance.py`와 FastAPI/구버전 이관 회귀 테스트로 만료 검색 차단, 충돌 보류/강제 승인, 보호 출처, 재검증, 격리 복구를 검증했다.
- 2026-09-01: 2026-08-11에 후속 검토 대상으로 남겨뒀던 "도커" 문제를 재검토·해결 — 전역 동의어 규칙 대신 KB `q` 필드에 직접 "도커"를 병기하는 국소적 방식(자소서/자기소개서가 이미 쓰던 패턴)이 실제로 통하는지 실측. `knowledge_dev_coding.py`의 Docker 항목 q필드에 "Docker" 바로 뒤에 "도커"를 추가 — 전역 동의어 규칙과 달리 그 문서 하나의 벡터만 바뀌므로, 이전에 전역 동의어 시도에서 봤던 교차 문서 회귀("설치 기본 사용법"이 Docker 대신 Git으로 뒤집히던 문제)가 재현되지 않음을 확인(Docker 0.348 > Git 0.325로 계속 정상). "도커 컨테이너 이미지" 검색이 score 0.0(완전 미스)→0.487(1위)로 개선, "도커 인스톨"류도 순위 밖→僅僅0.016 차이 2위로 크게 개선(완전히 이기지 못하는 잔여 원인은 Python 항목이 "설치"를 두 번 반복해 TF 상 유리한 것으로, 실제 콘텐츠를 왜곡하지 않고는 못 고치는 사소한 잔여 노이즈로 판단해 그대로 둠). 첫 시도에서 "도커"를 "Docker" 바로 뒤가 아니라 "설치" 뒤에 넣었더니, 자동 QA의 edge_compound 생성 로직(`q.split()[:2]`를 이어붙임)이 만드는 문자열이 바뀌어 새로운 실패가 생기는 것과 별개로, "설치_기본" bigram이 깨지면서 위에서 고쳤던 바로 그 교차문서 회귀가 다른 경로로 재현되는 것을 발견 — "Docker" 바로 뒤 배치가 유일하게 두 문제 모두를 피하는 위치임을 확인. 그 대신 "Docker"+"도커"가 나란히 놓이면서 edge_compound 테스트가 "Docker도커"라는 합성 문자열을 새로 생성해 실패하는 부작용이 생겨, `_COMPOUND_MAP`에 `'Docker도커': 'Docker 도커'` 1건을 추가해 해결(159개 항목을 추가했던 것과 동일한 종류의 테스트 아티팩트). `test_legal_kb_accuracy.py`(97.6%)·`test_persona_kb_qa.py`(97.1%, dev 97.7%) 회귀 없음 확인
- 2026-09-01: 2026-07-09에 "실제 예산 데이터 손상 위험"으로 병합을 보류해뒀던 대규모 엑셀형 예산관리 스프레드시트(`budget.html` v2026.07.03-20, 멀티시트·수식·셀참조·행열삽입삭제·서식·undo/redo·스냅샷/버전비교)를 재검토 — 사용자 요청으로 `hr-production`의 사본과 현재 작업 브랜치의 `ai/static/budget.html`을 직접 diff한 결과, 차이가 39줄뿐이고 그마저 전부 이번 세션에서 추가한 "북마크 토큰 자동 인증" 기능이었음. 즉 **이 스프레드시트는 이미 병합되어 현재 실제로 쓰이는 구현**이었고(`ai/main.py`에 필요한 API `/budget/sheets`·`/budget/grid`·`/budget/grid/snapshot`·`/budget/grid/compare` 등이 전부 이미 구현되어 있음을 확인), 2026-07-09의 "보류" 기록은 그 시점 기준으로 이후 병렬 세션들 사이에서 실제로 정착된 것으로 판단됨(중간에 "다른 세션이 삭제한 예산 기능을 복원"하는 커밋도 있었음을 이전에 확인함). 별도 조치 불필요 — 문서만 최신 상태로 정정
- 2026-09-01: 추가 고도화 1단계인 **실질 재검증 흐름** 구현. 기존 `/admin/learned/{id}/verify`가 호출만으로 유효기간을 연장하던 동작을 차단하고 `confirm_current=true`와 3자 이상의 검토 사유를 필수화했으며, 근거가 없던 기억에는 민감정보가 제거된 관리자 수동 검증 근거를 생성한다. `law.go.kr`·`mofa_travel_alert`는 개별 수동 연장을 금지하고 `/admin/memory-revalidation/authoritative` 허용 목록을 통해 기존 공공 API 수집기를 실제 실행해 전체 원문을 교체한다. 임의 evidence URL은 서버에서 방문하지 않아 SSRF를 차단하고, 법령 일부 실패·여행경보 빈 응답 시 기존 데이터를 보존하는 안전장치를 유지한다. 기억 검토 모달에 만료/임박 목록, 근거, 수동 확인, 공공 원문 재수집 UI를 추가하고 `memory_revalidation_events`에 성공·실패·검토 사유를 365일 보존한다.
- 2026-09-01: 추가 고도화 2단계인 **기억 버전·감사·롤백·동시성 보호** 구현. 일반 장기기억에 `version`/`updated_at`을 추가하고 수정·재검증·롤백 직전 상태를 `memory_revisions`에 내용·근거·출처·유효기간·행위·사유·검토자와 함께 보존한다. `/admin/learned/{id}/history`와 `/rollback` 및 관리 UI의 변경 이력 버튼으로 과거 버전을 조회·복원할 수 있으며, 롤백은 버전 번호를 뒤로 돌리지 않고 새 버전으로 기록한다. 재검증·롤백은 화면이 읽은 `expected_version`과 현재 버전이 다르면 409로 중단해 오래된 관리 화면의 덮어쓰기를 방지한다. 기억 후보 승인은 임시 nonce로 한 검토자만 선점하고, 강제 승인은 검토 사유를 필수화했으며 검토자·사유를 후보에 기록한다. 격리·복구도 원래 ID와 버전을 유지해 기존 변경 이력 연결이 끊기지 않는다.
- 2026-09-01: "예산 스프레드시트를 실제 엑셀/구글시트 수준으로 고도화" 요청에 따라 로컬 서버(Render 배포와 동일 코드)를 직접 띄우고 Playwright로 실사용자처럼 조작하며 검증 — 코드만 읽지 않고 실제로 셀 선택→입력→수식→복사/붙여넣기→채우기 핸들 드래그까지 구동. 수식 엔진(SUM/VLOOKUP/INDEX·MATCH/SUMIFS/IFERROR 등), 내부 복사·붙여넣기, 엑셀에서 복사한 것처럼 TSV 형식 외부 붙여넣기, 수식 셀 복사 시 상대참조 자동 조정, 채우기 핸들 드래그 자동 채우기가 전부 정상 동작함을 실측 확인. 그 과정에서 이 도구의 근본 성격(한국어 예산·비용 항목을 다루는 국문 스프레드시트)에 심각한 실사용 버그를 발견 — **한글(및 다른 IME 기반 언어) 입력이 셀에 전혀 반영되지 않고 조용히 사라짐**. 원인: 셀이 선택된 상태에서 실제 키보드 포커스는 항상 숨겨진 `#paste-trap`(OS 클립보드 복사/붙여넣기 이벤트를 안정적으로 받기 위한 textarea)에 있고, "타이핑을 시작하면 편집모드 진입"은 `document` 레벨 `keydown` 핸들러의 `e.key.length===1` 검사로만 판별하고 있었는데, 한글 등 IME 조합 입력 중에는 `keydown.key`가 조합 미확정을 뜻하는 `'Process'`(length 7)로 들어와 이 조건을 절대 통과하지 못함 — 편집모드가 열리지 않은 채 조합 텍스트가 paste-trap에만 쌓이다 Enter를 누르면 아무것도 반영되지 않고 사라짐(실측: 셀 선택 후 "사과" 입력해도 셀 값 그대로임을 확인). 수정: `paste-trap`에 `compositionstart` 리스너를 추가해 IME 조합이 실제로 시작되는 시점에 곧바로 `enterEdit()`로 포커스를 진짜 편집 가능한 `td`(contentEditable)로 옮기도록 함(`ai/static/budget.html`). Playwright의 `.type()`은 실제 IME composition 이벤트를 재현하지 않아(브라우저 자동화 도구의 알려진 한계) `page.evaluate()`로 실제 한글 IME가 발생시키는 것과 동일한 합성 이벤트 시퀀스(`compositionstart`→`compositionupdate`→`input`→`compositionend`, data:'사과')를 직접 dispatch해 검증 — 포커스가 `paste-trap`→`td.editing`으로 정확히 이동하고 `editMode=true`가 됨을 1차 확인, 이어서 전체 시퀀스 재현 후 Enter까지 눌러 A1 셀에 실제로 "사과"가 커밋됨을 2차로 최종 확인. JS 구문 검사(`new Function()`으로 script 블록 파싱) 통과.
- 2026-09-01: 위 IME 수정 이후 이어서 되돌리기/다시하기·틀고정·다중 시트 추가·시트 간 수식 참조를 Playwright로 실사용 흐름대로 검증(한글 입력 후 Ctrl+Z/Ctrl+Y 정상, 틀고정 적용 시 sticky 셀 75개 생성 확인, 시트 추가 후 `Sheet2!A1*2` 형태의 시트 간 참조 수식 정상 계산) — 여기까지는 전부 정상이었으나, 리본 UI의 "수식" 탭에 있는 자동합계(Σ)·평균·개수·최대·최소 버튼(`insertAutoSum`/`insertAvg`/`insertCount`/`insertMax`/`insertMin`)과 데이터 유효성 검사 드롭다운 목록에서 항목 클릭(`setCellFromDropdown`)까지 총 6곳에서 공통으로 호출하는 `setCell(r,c,v)` 헬퍼 함수가 파일 전체에 정의 자체가 없는(호출만 있고 선언이 없는) 상태였음을 발견 — 클릭 시 `ReferenceError: setCell is not defined`가 발생해 아무 반응도 일어나지 않는(에러가 콘솔에만 찍히고 화면엔 아무 피드백이 없어 눈치채기 어려운) 상태로, 이 스프레드시트에서 가장 눈에 띄는 "함수 빠른 삽입" 버튼들과 엑셀의 목록형 데이터 유효성 검사에 해당하는 기능이 전부 동작하지 않고 있었음. `grid()` 함수 바로 아래에 다른 그리드 조작 헬퍼(`clearCell`의 안전 성장 패턴, 붙여넣기 로직의 `put()` 패턴)와 동일한 방식으로 `setCell(r,c,v)`를 신설(행/열이 부족하면 안전하게 늘린 뒤 값을 대입) — 호출부 6곳 모두 이미 `pushUndo()`/`markDirty()`/`render()`를 자체적으로 감싸고 있어 `setCell` 자체는 그리드 배열 대입만 담당하도록 최소하게 구현. Playwright로 SUM/AVERAGE/COUNT/MAX/MIN 5개 버튼 전부 각각 독립된 숫자 열에서 정상 계산됨(100/25/4/40/10)과 데이터 유효성 목록에서 항목을 클릭하면 셀에 정상 반영됨을 재검증, JS 구문 검사 통과
- 2026-09-01: 추가 고도화 3단계인 **요청형 LLM 의미 모순 검증** 구현. 기존 수치·반대어·Jaccard 규칙 검사를 1차 안전 게이트로 유지하고, 관리자가 후보별 `AI 의미 검증`을 요청할 때만 같은 페르소나의 관련 유효 기억 최대 8건을 기존 LLM 폴백 체인으로 비교한다. 검증기는 `conflict/consistent/uncertain/unrelated`와 신뢰도·충돌 기억 ID·짧은 사유만 JSON으로 반환하며 상세 사고과정은 저장·노출하지 않는다. 입력에 없던 기억 ID는 제거하고 결과를 후보에 저장해 UI에 표시한다. 신뢰도 0.7 이상의 conflict/uncertain은 일반 승인을 보류하지만 자동 거절·자동 덮어쓰기는 하지 않으며, 관리자가 근거와 사유를 확인한 강제 승인만 가능하다. 호출 실패는 `unavailable`로 기록하고 기존 규칙 기반 흐름을 유지해 LLM 장애가 관리 기능 전체를 막지 않는다.
- 2026-09-01: 추가 고도화 4단계인 **의존성 없는 하이브리드 검색·확장성 개선** 구현. 기존 TF-IDF 코사인을 주 검색 신호로 유지하면서 문서 길이와 반복 키워드에 강한 BM25 점수를 최대 8%의 제한적 보조 가중치로 결합했고, 토큰 검색 결과가 전혀 없을 때만 문자 3-gram Dice 유사도로 오타·띄어쓰기 폴백을 수행한다. 법률 조항 질의는 유사 문자열 오탐을 막기 위해 문자 폴백에서 제외하고 페르소나 범위를 그대로 강제한다. 전체 지식을 매번 선형 순회하던 검색은 특징별 역색인으로 공통 특징이 있는 후보에만 코사인/BM25를 계산하도록 바꿨다. 1,200개 독립 문서에서 코사인 계산 20회 미만을 검증하는 `test_hybrid_search.py`를 추가했으며, 법률 자동 QA는 448/456(98.2%, 이전 97.8%보다 개선), 범용 페르소나는 634/653(97.1%, 회귀 없음)을 확인했다.
- 2026-09-01: 추가 고도화 5단계인 **운영 기억 검색 자동 품질 평가** 구현. `quality_eval.py`에 퇴직금·연차·FastAPI·여행보험·자기소개서·최저임금의 기본 골든 케이스와 최대 200개의 관리자 지정 케이스 평가를 추가해 현재 운영 엔진의 최소 점수, 기대 키워드, 금지 키워드, 페르소나 범위, 검색 방식을 결정론적으로 검사한다. `POST /admin/memory-quality-evals/run`은 인증된 요청만 실행하고 합격률·케이스별 원인·출처를 `memory_quality_eval_runs`에 저장하며, `GET /admin/memory-quality-evals`로 이력을 조회한다. 질문 원문은 이력에 저장하지 않고 케이스 이름·검색 결과만 남기며 365일 보존정책과 SQLite→Turso 이관에 포함했다. `memory_quality_eval.yml`이 매주 운영 서버의 기본 6개 케이스를 실행하고 80% 미만이면 GitHub Actions를 실패 처리한다. 기본 케이스는 현재 6/6(100%) 통과하며 API 인증·이력·상한 검증을 추가했다.
- 2026-09-01: "엑셀/구글시트 참고해서 모두 다 구현" 요청 범위에서, 이 스프레드시트에 셀 우클릭 컨텍스트 메뉴 자체가 아예 없다는(행/열 머리글에는 이미 있었음) 격차를 발견해 추가 구현. `cellCtxMenu(r,c)`를 신설해 잘라내기/복사/붙여넣기/내용 지우기(범위 선택 시 "선택 영역 지우기"로 라벨 전환)/행·열 삽입·삭제를 기존 `rowColCtxMenu`와 동일한 `.cm-item`/`.cm-sep` 스타일로 제공, `td`의 `oncontextmenu`에 연결했다. 구현 중 기존 `td` mousedown 핸들러가 좌우 클릭을 구분하지 않아 우클릭도 항상 단일 셀 선택으로 리셋시켜버려(범위 선택 후 우클릭해도 항상 그 범위가 무너짐) 메뉴의 "선택 영역 지우기" 분기를 절대 못 타는 문제를 자체 검증 중 발견 — 우클릭이 이미 있는 선택 범위 안이면 mousedown이 선택을 건드리지 않도록 기존 `inSel(r,c)` 헬퍼로 가드 추가(엑셀/구글시트와 동일하게 범위 밖 우클릭만 단일 선택으로 교체). Delete 키의 범위 지우기 인라인 로직은 `clearSelectedCells()`로 뽑아내 메뉴와 공유. 붙여넣기까지 붙이고 나서 실측 중 더 큰 문제 발견 — 툴바의 기존 "붙여넣기" 버튼도 `document.execCommand('paste')`를 쓰는데, 이는 실제 Ctrl+V(네이티브 이벤트)에서만 허용되고 버튼 클릭 같은 스크립트 트리거에는 최신 브라우저가 조용히 거부(반환값 false, 화면 반응 없음)한다는 것을 Playwright로 확인(Ctrl+V는 정상, 버튼 클릭은 실패) — 사용자 제스처로 호출 가능한 `navigator.clipboard.readText()`(비동기 Clipboard API)로 `doPaste()`를 재작성, 기존 `paste` 이벤트 리스너의 처리 로직(내부 복사 스냅샷 비교 후 수식 보존 붙여넣기 or 일반 텍스트 붙여넣기)을 `applyPastedText()`로 뽑아 재사용. 이어서 복사/잘라내기도 컨텍스트 메뉴에서는 실패하는 것을 발견 — 포커스 불가능한 메뉴 `<div>`를 클릭하면 `document.activeElement`가 `body`로 떨어져(반면 포커스 가능한 `<button>` 클릭은 버튼으로 이동해 우연히 정상 동작했었음) `execCommand('copy'/'cut')`가 엉뚱한 대상에 실행되어 조용히 실패하는 것을 activeElement 로깅으로 직접 확인 — `doCopy()`/`doCut()` 실행 직전 `paste-trap`에 명시적으로 포커스를 되돌리도록 수정. Playwright로 컨텍스트 메뉴의 잘라내기→붙여넣기(값 이동 확인), 복사→붙여넣기, 범위 지우기, 행/열 삽입·삭제 전부와 기존 Korean IME/undo·redo/틀고정/시트간참조/수식버튼 회귀 스위트 전체 재통과 확인, JS 구문 검사 통과
- 2026-09-02: 추가 고도화 6단계인 **기억 유형·접근 범위 분리** 구현. 장기기억과 승인 후보를 `fact/preference/procedure/context/document` 유형 및 `persona/owner/session` 범위로 분리하고, 보수적인 규칙 분류와 관리자 직접 지정 API를 함께 제공한다. 소유자 선호는 owner 범위·기본 365일로 저장해 소유자 답변 시스템 프롬프트에만 참고 데이터로 주입하고 공유 링크에는 노출하지 않는다. 이번 대화·현재 프로젝트 같은 임시 맥락은 session 범위·기본 14일로 저장해 같은 브라우저 세션 검색에서만 회수하며 다른 소유자 세션과 공유 세션에서는 제외한다. 사실·절차·문서는 기존처럼 페르소나 범위에서 검색된다. 범위 필터는 TF-IDF/BM25와 문자 폴백뿐 아니라 규칙/LLM 충돌 검증에도 적용해 다른 세션 내용이 비교 입력으로 넘어가지 않게 했고, 격리·복구·버전 이력·롤백·Turso 이관에서도 유형과 범위를 보존한다. `test_memory_types_scopes.py`로 소유자/공유 격리, 세션 간 격리, 자동 분류, 후보 승인, 격리 복구, 버전 롤백을 검증했다.
- 2026-09-02: 추가 고도화 7단계인 **통합 기억 운영 센터** 구현. 기존 후보 승인·재검증 모달을 승인된 장기기억 검색 및 유형·범위 필터, 유형/범위별 현황, 후보 메타데이터, 변경 이력·롤백, 기본 골든 질문 6개 즉시 평가와 최근 평가 이력, 최근 7일 검색량·저신뢰율·기억 활용률까지 한 화면에서 관리하는 운영 대시보드로 확장했다. UI 스크립트 구문 검사와 API 통합 테스트에서 운영 센터의 핵심 기능 노출 및 기존 기억 안전 API 회귀를 검증한다.
- 2026-09-02: 추가 고도화 8단계인 **실사용 기억 기여도 학습** 구현. 기존 좋아요·싫어요가 사용자 질문 문자열에만 연결돼 실제 답변에 사용된 기억 청크를 개선하지 못하던 한계를 보완했다. 검색 시 실제 컨텍스트에 포함된 승인 기억 ID를 최대 8개까지 검색 이벤트에 기록하고, 질문·세션은 운영 비밀값 기반 HMAC으로만 남긴다. 소유자 피드백은 같은 세션·질문의 최근 24시간 미평가 이벤트 한 건에만 귀속하며 공유 검색은 제외한다. 각 기억의 검색·긍정·부정 횟수와 마지막 사용 시각을 누적하고 베타 사전분포를 적용한 0.5~1.5 범위의 보수적 효용 가중치를 검색 순위에 즉시 반영한다. 자동 삭제는 하지 않으며 통합 기억 운영 센터의 `기억별 실제 기여도`에서 근거 수치를 확인할 수 있다. 격리·복구 및 SQLite→Turso 이관에서도 기여도 값을 보존하고 전용 회귀 테스트로 자동 라우팅, 세션 격리, 중복 피드백 차단, 공유 제외, 원문 비저장을 검증한다.
- 2026-09-02: 예산 스프레드시트 "엑셀/구글시트 고도화" 작업을 이어서 진행 — 셀 서식(굵게/기울임/밑줄/글자색/배경색), 숫자 서식(통화 표시), 셀 병합, 조건부 서식, 정렬, 자동 필터를 Playwright로 실측 검증. 서식류는 `.bold`/`.italic`/`.underline` CSS 클래스로 구현되어 있어(인라인 style이 아님) 처음엔 확인 방법이 틀려 "적용 안 됨"으로 오판할 뻔했으나 class 속성으로 재확인해 정상 동작 확인. 병합·조건부서식·통화서식·필터 활성화는 모두 문제없이 동작. 정렬(`sortByCol`) 검증 중 30행 규모(MIN_ROWS)로 패딩된 시트에서 정렬 후 데이터가 전부 사라진 것처럼 보이는 현상을 발견해 깊이 조사했으나, 이는 실제 버그가 아니라 빈 셀이 숫자 0으로 취급되어 오름차순 정렬 시 맨 앞으로 몰리는 바람에(값이 있는 행이 뒤쪽으로 밀려남) 배열 앞부분만 확인하던 내 테스트 스크립트가 오판한 것이었음(전체 배열을 순회해 값이 실제로 어디 있는지 재확인해 데이터 손실이 없음을 확인). 다만 이 과정에서 진짜 사양 차이 하나를 발견 — 실제 엑셀/구글시트는 정렬 방향(오름차순/내림차순)과 무관하게 빈 셀을 항상 맨 아래로 보내는데, 이 앱은 빈 셀을 0으로 취급해 오름차순에선 맨 위, 내림차순에선 맨 아래로 뒤섞여 배치되고 있었음 — `sortByCol()`의 비교 함수에 "두 값 중 하나라도 빈 셀이면 정렬 방향과 무관하게 항상 뒤로" 규칙을 추가해 실제 엑셀/구글시트 동작과 일치시킴. Playwright로 오름차순·내림차순 모두 값이 있는 행이 항상 앞쪽에, 빈 행이 항상 뒤쪽에 오는 것을 재확인, 기존 IME/formula 버튼/컨텍스트 메뉴 복사·붙여넣기 스모크 테스트 재통과, JS 구문 검사 통과. (별도로, 이 재개 시점에 로컬 git 체크아웃이 실수로 `main` 브랜치의 오래된 커밋(`9d8d83e`)을 가리키고 있어 이전 세션의 커밋들이 사라진 것처럼 보였으나, GitHub API로 직접 확인한 결과 실제 `claude/account-pinned-agents-v6W7p` 브랜치에는 모든 커밋이 정상적으로 남아있었음 — 단순 로컬 체크아웃 문제였고 `git reset --hard origin/...`로 해결, 실제 데이터 유실은 없었음)
- 2026-09-03: **2027년 최저임금 오답·검색 출처 오염 방지** 구현. 내부 최저임금 확정 데이터가 2026년에 멈춰 있고 단순 금액 조회는 결정형 계산기 경로를 타지 않아, 웹검색의 무관한 스니펫과 오래된 KB를 받은 LLM이 “아직 발표되지 않음”으로 답하던 원인을 수정했다. 최저임금위원회 공식 결정현황(2027년 시간급 10,700원·일급 85,600원·월 2,236,300원·2026-08-05 고시)을 계산기와 주요 HR KB에 반영하고, `27년` 축약 연도도 공식 출처가 포함된 직접 응답으로 처리한다. 검증 데이터 밖의 연도는 추정하지 않으며, 웹검색 결과는 질문 핵심어 일치 여부로 필터링하고 `.go.kr` 출처를 우선해 무관한 페이지가 참고자료·학습 기억으로 누적되지 않게 했다.
- 2026-09-03: "추가 고도화 진행할 내용이 있는지 확인" 요청으로 아직 실측 검증하지 않은 나머지 기능들(중복 제거, 텍스트 정렬, 줄바꿈, 소수점 자릿수, 병합 해제, 저장/재접속 지속성, 스냅샷 저장·비교, xlsx 다운로드/업로드)을 Playwright로 점검하다 실제 버그 2건을 발견·수정. **(1) "위 행 삽입"·"왼쪽 열 삽입" 리본 버튼이 실제로는 정반대(아래/오른쪽)로 삽입** — `insertRowAbove()`/`insertColLeft()`가 `insertRowAt(r,'before')`/`insertColAt(c,'before')`로 호출하는데, 정작 `insertRowAt()`/`insertColAt()`는 `pos==='above'`/`pos==='left'`인지만 확인해 분기하는 구조라 `'before'`/`'after'`라는 존재하지 않는 문자열은 항상 else 분기(아래/오른쪽 삽입)로 빠져버렸음(우클릭 컨텍스트 메뉴 쪽은 처음부터 `'above'`/`'left'`를 올바르게 넘기고 있어 문제없었음). Playwright로 "위 행 삽입" 클릭 시 원래 내용이 그대로 있고 새 빈 행이 아래에 생기는 것(반대여야 정상)을 실측 확인 후, 호출부의 문자열을 `'above'`/`'below'`/`'left'`/`'right'`로 맞춰 수정 — 재검증 결과 원래 내용이 정확히 한 칸 아래/오른쪽으로 밀려나는(=새 빈 행/열이 위/왼쪽에 삽입된) 정상 동작 확인. **(2) `/budget/grid/upload`(xlsx·csv 업로드) 응답 계약 불일치로 업로드가 항상 조용히 실패** — 서버(`main.py`)는 `{"rows": len(rows)}`(행 "개수")를 반환하는데, 클라이언트(`budget.html`의 `upload()`)는 `d.rows.map(...)`로 `rows`가 실제 2차원 배열이라고 가정하고 있어 숫자에 `.map`을 호출해 매번 예외가 발생 — `try/catch`가 이를 조용히 삼켜 "업로드 실패" 상태 메시지만 뜨고 원인을 알 수 없었음(정작 서버는 파일을 정상 파싱해 `budget.save_grid(rows)`로 이미 저장까지 완료한 상태라 더 헷갈리는 상황이었음). 이 엔드포인트를 사용하는 곳이 `budget.html` 하나뿐임을 확인 후 서버 응답을 `{"rows": rows, "row_count": len(rows)}`(rows를 실제 배열로, 개수는 별도 필드로 보존)로 수정. 테스트 xlsx 파일(이름/금액 2열 3행)을 만들어 업로드 → 그리드에 정확히 반영되는 것을 재검증. 두 수정 모두 후 기존 IME/수식버튼/컨텍스트메뉴/서식/병합/조건부서식/정렬 회귀 스위트 재통과, JS 구문 검사·`py_compile` 통과. xlsx 다운로드 자체는 정상 동작 확인(처음 500 에러가 났던 건 이 세션의 로컬 테스트 환경에 `openpyxl`이 설치돼 있지 않았던 것뿐 — `requirements.txt`엔 이미 있어 실제 배포 환경엔 영향 없음, 설치 후 재확인해 정상 xlsx 파일이 생성됨을 확인).
- 2026-09-03: **주제별 웹검색 출처 신뢰·최신성·교차검증 정책** 구현. 노동·법률·세무·의료·여행·금융 질문을 분류해 각 분야의 공식기관 및 전문기관 도메인을 우선순위화하고, 공식 출처가 확보된 고위험 주제에서는 일반 개인 페이지를 답변 근거에서 제외한다. 공식 결과가 처음 검색되지 않으면 분야별 대표 공식 도메인으로 한 차례 보완 검색하며, 공식 근거가 없을 때는 독립 도메인 수에 따라 `medium/limited/low` 신뢰도를 부여한다. 최신·현재·오늘 질문은 검색 스니펫의 완전한 날짜를 기준으로 분야별 유효기간을 평가하고 날짜가 없거나 오래되면 최신성 미검증으로 명시한다. 컨텍스트에 출처 등급·날짜 근거·교차검증 규칙·검색 문서 내 지시문 무시 원칙을 주입하고, 참고 링크에도 `공식/공공기관/전문기관/일반` 라벨을 표시한다. 비HTTP·자격증명 삽입 URL을 차단하며, 명시적 웹검색 학습은 고위험 분야에서 공공·전문기관 이상만 장기기억에 저장한다.
- 2026-09-03: "추가 고도화 진행해줘" 요청에 따라 이 스프레드시트에 완전히 빠져있던 엑셀/구글시트 핵심 기능인 **셀 메모(comment)** 를 신설. 우클릭 컨텍스트 메뉴에 "메모 추가/편집"(`prompt()`로 텍스트 입력, 기존 시트 이름 변경·열너비 조정과 동일한 UI 관례)과 메모가 있을 때만 나타나는 "메모 삭제"를 추가, 메모가 있는 셀 오른쪽 위 모서리에 빨간 삼각형 표시(`::after`, 엑셀과 동일한 위치)를 그리고 `title` 속성으로 호버 툴팁을 제공한다(기존 수식 오류 툴팁과 같은 속성을 공유해야 해서 오류 메시지와 메모를 `/`로 이어붙여 함께 표시하도록 통합). 데이터 구조 `cellNotes[sheetName][r_c]`는 기존 `mergedCells`/`conditionalFormats`/`dataValidations`와 완전히 동일한 패턴으로 설계 — 되돌리기/다시하기(`snapState`/`restoreState`), 행·열 삽입·삭제·드래그 이동 시 메모 위치 자동 이동(`shiftRowState`/`shiftColState`/`moveRowState`/`moveColState`의 리매핑 배열에 추가), 시트 이름 변경 시 메모 함께 이전, 저장/불러오기(`save()`/`load()`)에 모두 동일하게 연결했고 서버(`budget_store.py`의 `get_sheets`/`save_sheets`)에도 `cellNotes` 필드를 추가해 영속 저장되도록 함. Playwright로 메모 추가(다이얼로그 입력)→빨간 삼각 마커 표시→우클릭 메뉴가 "메모 편집"으로 전환→기존 값이 프롬프트에 미리 채워짐→수정→저장 후 새로고침해도 유지→삭제까지 전체 흐름 검증, 별도로 행 삽입 시 메모가 올바른 행으로 자동 이동하는 것(`2_0`→`3_0`)도 확인. 기존 IME/수식버튼/컨텍스트메뉴/서식/정렬/업로드 회귀 스위트 전체 재통과, JS 구문 검사·`py_compile` 통과.
- 2026-09-03: **웹검색 핵심 수치 교차검증·사용자 신뢰도 표시** 구현. 검색 스니펫의 수치를 무작정 비교하지 않고 `대상 연도 + 의미 항목(시급·월급·금리·환율·세율·과태료·주가 등) + 단위`로 구조화해 같은 사실 후보만 독립 도메인별로 비교한다. 두 개 이상 도메인이 같은 값을 제시하면 교차확인으로 기록하고, 같은 연도·항목에 서로 다른 값이 있으면 전체 검색 신뢰도를 `conflict`로 낮춰 LLM이 임의 선택하지 않게 했다. 연도가 다른 최저임금처럼 정상적으로 값이 달라지는 경우는 충돌로 보지 않는다. 답변 하단에 검색 근거 품질, 공식·독립 출처 수, 최신성, 교차확인/충돌 수치를 표시해 사용자가 근거 상태를 직접 확인할 수 있으며, CI에서 Python 구문 검사와 일치·충돌·연도 분리 회귀 테스트를 자동 실행한다.
- 2026-09-03: 이어서 "추가 고도화 진행해줘" 요청에 따라 이 스프레드시트에 전혀 없었던 **찾기 및 바꾸기(Ctrl+F/Ctrl+H)** 를 신설 — 엑셀·구글시트 모두의 가장 기본적인 편집 기능인데 코드 전체에 관련 함수가 하나도 없어 완전히 빠져있던 것을 발견. 우측 상단에 뜨는 플로팅 패널(`#fr-panel`, 기존 `openFreezeDialog()`와 동일한 `document.createElement` 방식)에 찾기/바꾸기 입력창, 대소문자 구분·모든 시트에서 찾기 체크박스, 이전/다음/바꾸기/모두 바꾸기 버튼을 배치. 검색은 셀의 원본 내용(raw, 수식이면 수식 텍스트 자체)을 대상으로 하여 찾기·바꾸기가 항상 같은 표현을 가리키도록 함(계산된 표시값 기준으로도 검색 가능하지만, 통화 서식처럼 원본과 표시값이 달라지는 경우 "찾아서 바꾼" 결과가 예측 불가능해지는 문제를 피하기 위한 선택). 전역 키보드 핸들러 최상단에 `#fr-panel` 내부 입력 포커스 시 시트 탐색 단축키(방향키 이동, Ctrl+B 등)가 함께 발동하지 않도록 가드를 추가하고, Ctrl+F/Ctrl+H 모두 동일한 패널을 열도록 연결(패널 안에 두 입력창이 항상 함께 있어 별도 모드 전환 불필요). "데이터" 리본 탭에도 버튼을 추가해 단축키를 모르는 사용자도 찾을 수 있게 함. Playwright로 Ctrl+F 진입 시 포커스 자동 이동, 대소문자 구분 옵션(구분 시 1개만 매치 vs 미구분 시 3개), 모든 시트에서 찾기(활성 시트만 3개 vs 전체 시트 4개), 이전/다음 탐색 시 시트 전환과 셀 선택·스크롤, 현재 항목만 바꾸기(다른 매치는 그대로), 모두 바꾸기 후 되돌리기(Ctrl+Z)로 원상복구까지 전부 실측 검증. 기존 IME/수식버튼/컨텍스트메뉴/서식/정렬/업로드/메모 회귀 스위트 전체 재통과, JS 구문 검사 통과.
- 2026-09-03: **웹검색 수치 충돌 확정 보류·기억 오염 차단** 고도화. `3.7`과 `3.70`처럼 표기만 다른 수치는 동일 값으로 정규화하고, `www`/서비스 하위 도메인을 기관 단위로 묶어 한 기관의 여러 페이지를 독립 교차검증으로 과대 계산하지 않는다. 공공·전문기관 근거가 있을 때 일반 블로그의 다른 값이 공식 검증을 뒤집지 못하도록 신뢰 등급을 반영했다. 실제 신뢰 출처끼리 충돌하면 연도·항목과 출처별 값을 답변 하단에 직접 표시하고 LLM에 임의 선택·평균 금지 및 `확정 불가` 응답을 강제한다. 충돌 수치가 포함된 검색 문서와 그 답변은 수동 검색 학습 및 자동 기억 후보 저장에서 제외해 오답이 재검색 근거로 누적되는 경로를 차단한다.
